# Open questions — the Bend 2 port of Toon

<!-- A spec gap is never resolved by reading the original's source during
     implementation; it is resolved by RUNNING the original on a new case,
     capturing it as a golden, and amending the spec clause. Each entry
     names the case that resolved it. -->

| id | question | spec clause | resolve by (case to add) | resolution | date |
|---|---|---|---|---|---|
| OQ-001 | What text does the original print for a JSON float: when does it switch to exponent form, and how is the exponent written? | S7 | `decfmt_exp_upper_boundary`, `decfmt_exp_lower_boundary`, `decfmt_exp_shapes`, `decfmt_exp_compact_and_expand` | RESOLVED by capture: shortest round-trip digits; with `e` the decimal exponent of the first digit, plain decimal when −5 ≤ e ≤ 15 (an integer value gets `.0`: `100.0`, `1234567890123456.0`), otherwise `d[.ddd]e±X` with an explicit `+`, no zero padding (`1e+16`, `9.9e-6`, `1.2345678901234568e+16`). Same text from both JSON writers. | 2026-09-20 |
| OQ-002 | Is JSON number input correctly rounded to binary64? | S7 | `encnum_long_mantissa`, `encnum_u64_boundary`, `encnum_frac_long`, `encnum_pow10_slow_path`, `encnum_pow10_each`, `encnum_mixed_digits` | RESOLVED by capture: no. `0.3333333333333333333333` encodes as `0.33333333333333337` and `1.00000000000000011102230246251565` as `1.0000000000000002`, while the same tokens on the decode side give `0.3333333333333333`-class correct results (`decnum_long_mantissa` n07 = `1.0`). The encode side is serde_json 1.0.151's default float path; the spec numbers its steps from `serde_json-1.0.151/src/de.rs` and the port reproduces them in software binary64. | 2026-09-20 |
| OQ-003 | How is the `--stats` percentage rounded when the exact ratio sits on a half? | S4.315, S4.316, S7.6, S7.7 | `stats_tie_to_even_down`, `stats_tie_to_even_up`, `stats_f64_below_tie`, `stats_f64_above_tie`, `stats_percent_100` | RESOLVED by capture: the percent is two binary64 operations, D divided by J and then times 100, printed to one decimal from the exact binary value with ties to even (`6.25` prints `-6.2%`, `18.75` prints `-18.8%`); because of the double rounding 23/80 prints `-28.7%` and 49/80 prints `-61.3%`; `-100.0%` is reachable. All eleven `stats_*` encode goldens reproduce by hand computation (S4.317). | 2026-09-20 |
| OQ-004 | Can the compiled Bend binary receive `--help`, `--version`, `--threads`-like words as program arguments? | S8 | every case is run by the harness as `<port> -- <argv>` | RESOLVED by the runtime facts (RUNTIME-FACTS-FOR-PORTERS, argv): the runtime consumes `--help`, `--threads N`, `--gpu X` before `--` and passes everything after `--` through. Goldens hold behind `--`; the bare spelling is DISC-001 (Platform). | 2026-09-20 |
| OQ-005 | Which of clap's unexpected-argument outputs are a fixed pattern and which need clap's similarity engine? | S9 | `usage_unknown_flag`, `usage_unknown_short`, `usage_extra_positional` captured; a near-miss flag (`--encod`) prints `tip: a similar argument exists: '--encode'` and `Usage: toon --encode [INPUT]` | RESOLVED for scope: the fixed patterns are in scope and captured; the similarity tip is excluded in PLAN §3 (external-dependency, debt) and has no case. SUPERSEDED by OQ-A1 (Phase 1): the exclusion was withdrawn, S1.73–S1.75 specify the tip and the `usage_similar_arg*` cases capture it. | 2026-09-20 |
| OQ-006 | Does the original prefix decode failures with `Failed to decode TOON:` as its README says? | S9 | every `toonerr_*` and `fx_dec_*` error case | RESOLVED by capture: no prefix. Decode errors print the bare message (`Missing colon after key`, `Line 3: Blank lines inside list array are not allowed in strict mode`, `Validation error at line 2: …`), exit 1. JSON input errors print `JSON error: Failed to parse JSON: <serde_json message> at line L column C`. | 2026-09-20 |
| OQ-007 | Does decoding print integers as integers, as the README's examples show (`{"id":1,…}`)? | S7 | `happy_readme_users_decode`, `decnum_ints` | RESOLVED by capture: no. Every decoded number is an f64 and prints through the float writer: `1.0`, `42.0`, `1000000.0`. Bug-compatible by default; listed for the owner in DISCREPANCIES as a candidate BugFix (not taken). | 2026-09-20 |
| OQ-008 | What OS error text reaches stderr for a missing file, a directory, an uncreatable output, and is it producible on every Bend lane? | S8, S9 | `usage_missing_file`, `usage_dir_as_input`, `usage_output_bad_dir` | RESOLVED by probe on the interpreter, C and JS lanes (2026-09-20): `File.open` / `File.read_bytes` fail with the pair (errno, strerror text), identical on the three lanes: (2, `No such file or directory`), (21, `Is a directory`; the open succeeds and the first read fails, as in the original), (13, `Permission denied`). The original prints `<strerror> (os error <errno>)`, so the port builds the same text from the pair. No `ErrorText` DISC. | 2026-09-20 |
| OQ-A1 | The similar-argument tip (`tip: a similar argument exists: '--encode'`) was excluded in PLAN §3 as needing clap's similarity engine. Is it needed after all? | S1.71, S1.73–S1.75 | `usage_similar_arg`, `usage_similar_arg_help`, `usage_similar_arg_nope`, `usage_similar_arg_after_commit`, `usage_similar_arg_tie` | RESOLVED by capture and decision (architect): the engine is a closed formula (Jaro similarity above 0.7, the later candidate wins a tie) that reproduced 566 of 566 oracle runs, and the plain unexpected-argument text cannot be chosen without it (`--nope`, `--e`, `--d`, `--stat` all trigger the tip). The exclusion is withdrawn in PLAN §3; the tip is in scope. | 2026-09-20 |
| OQ-A2 | Is the similar-value tip for the two enum options in scope? | S1.84, S1.85 | `usage_enum_similar_case`, `usage_enum_similar_prefix`, `usage_enum_similar_off`, `usage_enum_similar_picks_best`, `usage_enum_similar_tie`, `usage_enum_similar_boundary_30`, `usage_enum_similar_boundary_31`, `usage_enum_not_similar` | RESOLVED: in scope, same formula as OQ-A1; cases captured. | 2026-09-20 |
| OQ-A3 | The program name in clap's `Usage:` lines is the final component of argv[0]. Can the port follow it? | S1.2, S10.19 | none expressible (the harness fixes argv[0]; every golden holds `toon`) | RESOLVED as a divergence: Bend's `IO.args()` never exposes argv[0] on any lane, so the port prints the literal `toon`. Recorded as DISC-002 (Platform). | 2026-09-20 |
| OQ-A4 | Which OS error texts can each Bend lane produce (errno 2, 13, 20, 21, 28, 32)? | S9.4–S9.6, S9.13, S9.15–S9.17 | `usage_missing_file`, `usage_dir_as_input`, `usage_output_bad_dir`, `io_output_is_dir`, `io_input_trailing_slash`, `io_output_dev_full_stats` | RESOLVED: errno 2, 13, 20, 21 and 28 print the original's text on every lane: the six cases named here PASS on the interpreter, c-1t, c-8t and js (`scripts/lanes.sh` at `1230a0d`: PASS 1053/1053 on each lane; `io_input_trailing_slash` is errno 20 `Not a directory`, `io_output_dev_full_stats` is errno 28 `No space left on device` through the `-o` File API). errno 32 (and 28, 11) on STDOUT never reaches the program: the runtime fail-stops with its own line, which is DISC-006 (class Platform), not an `ErrorText` difference of a lane. | 2026-09-20 |
| OQ-A5 | The original aborts (status 134) when stderr cannot be written. Reproducible? | S9.20, S10.18 | none expressible (the harness cannot redirect stderr to a failing device) | RESOLVED as a divergence candidate: a Bend program fail-stops with exit 1 and `bend: a short write on a standard stream` instead. Recorded as DISC-003 (Platform). | 2026-09-20 |
| OQ-A6 | Without `--stats`, a failing write to the `-o` file is silently lost when the output is at most 8192 bytes (success line, exit 0). Keep it? | S9.15, S10.3, S7.11 | `io_output_dev_full_8192`, `io_output_dev_full_8193`, `io_output_dev_full_stats` | RESOLVED by capture and decision (architect): bug-compatible. The port knows the byte length of its output before writing, so it applies the same threshold: a write failure is reported only when the output exceeds 8192 bytes or `--stats` is given. Listed for the owner as candidate C-7. | 2026-09-20 |
| OQ-A7 | Non-UTF-8 argv words. | S1.22, S1.87, S5.107, S9.18 | none expressible (cases.tsv argv is text) | RESOLVED as a divergence candidate: Bend decodes argv with replacement before the program sees it, so the original's lossy-path texts cannot be matched byte for byte. Recorded as DISC-004 (Platform). | 2026-09-20 |
| OQ-A8 | clap styles its texts with ANSI codes when `CLICOLOR_FORCE` is set or the stream is a terminal. | S1.21, S8.3, S8.4 | none expressible (no per-case environment, and the harness captures through pipes) | RESOLVED as a divergence candidate: the port always prints plain text, which is what the original prints into a pipe and what every golden holds. Recorded as DISC-005 (Platform). | 2026-09-20 |
| OQ-B1 | Who owns the value threshold of `number out of range`? | S2.14, S9.61, S4.113, S10.41 | `encnum_overflow_boundary`, `encnum_max_finite_edge_ok`, `encnum_mixed_digits` | RESOLVED: part C owns it (S4.113): the single multiplication by the table entry overflows, so `1.7976931348623158e308` is rejected although it rounds to the largest finite value (S10.41). | 2026-09-20 |
| OQ-B2 | `--indent` is also the TOON input unit, so the Phase 0 cases `jsonout_indent_0/1/4`, their `_expand` twins and `jsonout_root_array_compact` are decode errors and pin no JSON layout. | S5.53, S5.57, S5.61, S10.32 | `jsonout_compact_shapes`, `jsonout_compact_shapes_expand`, `jsonout_indent_1_ok`, `jsonout_expand_indent_1_ok`, `jsonout_indent_4_ok`, `jsonout_expand_indent_4_ok` | RESOLVED by capture: the replacement cases are in the corpus; the original cases stay (names are never retired) and pin the error behavior they turned out to exercise. | 2026-09-20 |
| OQ-B3 | Library-only behavior no CLI path reaches (the streaming writer on malformed event sequences, the value builder's messages, `toon_to_json`, `json_to_toon`, the `Failed to stringify JSON` wrapper). In scope? | S2.55, S5.75, S5.76, S9.77–S9.83 | none through the CLI; a Rust driver would be needed for stage goldens | RESOLVED for scope (architect): excluded, class out-of-scope, debt yes, the same treatment as `EncodeReplacer`; added to PLAN §3. | 2026-09-20 |
| OQ-C1 | Is `null` from a number unreachable from the CLI? | S4.117, S4.130, S4.171 | none known: a counter-example would be a JSON document whose TOON shows `null` where the JSON had a number | RESOLVED as unreachable: argued from the reader's arithmetic (an overflowing multiplication is the error S4.113, and NaN cannot arise, S4.117) and unrefuted by about 30000 literals in Phase 1 plus a Phase 3 campaign of 200000 generated literals run on the original (158965 accepted, 41035 rejected with `number out of range`, 0 printed `null`). The port implements the rule anyway (a non-finite number prints `null`), so a counter-example would change no code, only add a case. | 2026-09-20 |
| OQ-C2 / OQ-E1 | A declared array length above 2^64 − 1 is "not a header" while 2^64 − 1 is a cap error: the boundary is the oracle's 64-bit word. Reproduce? | S4.190, S7.34, S2.135, S10.97 | `toonedge_length_forms`, `toonerr_length_u64_max`, `toonerr_length_u64_max_list`, `toonedge_length_u64_overflow` | RESOLVED by capture and decision (architect): reproduced as observed; the port compares the digit string against 18446744073709551615. | 2026-09-20 |
| OQ-C3 | zmij treats 2^−1022 as an irregular power of two. | S4.131, S4.172 | `decnum_extremes` | RESOLVED: the output for that one value is the same under either reading (`2.2250738585072014e-308`), and no other value is affected. | 2026-09-20 |
| OQ-E2 | Does the upstream reference accept a first-field list whose items sit at item depth + 1? | S10.86 | `toonerr_list_first_field_list_shallow` | RESOLVED as irrelevant to the port: the pinned binary rejects it and the case pins that; only the wording of S10.86 depends on upstream. | 2026-09-20 |
| OQ-P3-1 | In a list item, does the header parse of the item's rest surface its failures when the rest has no `:` outside quotes (so neither S4.222 nor S4.224 applies)? | S4.223 | `toonerr_item_header_cap_quoted_colon`, `toonerr_item_header_inline_unterminated`, `toonerr_item_header_key_escape`, `toonerr_item_bracket_cap_quoted_colon` | RESOLVED by running the original (Phase 3, before the item router was written): yes, every header failure surfaces (the cap, a quoted key's escape error), and a keyed header with a fields segment still takes the inline path. S4.223 amended. | 2026-09-20 |
| OQ-P3-2 | For a quoted key, which comes first: the escape error of the key or the missing colon after its closing quote? | S2.126 | `toonerr_quoted_key_escape_before_colon`, `toonerr_quoted_key_escape_before_colon_item` | RESOLVED by running the original (found by the Phase 3 mutation fuzzer, 7 of 3000 documents differed): the key is unescaped first, so `"a\x"` without a colon reports `Invalid escape sequence: \x`. S2.126 amended; the port's `kv.quoted` reordered. | 2026-09-20 |
| OQ-P4-1 | With a value attached to `--help` or `--version` while C is not empty, is the usage line the bare group line G? | S1.86 | `usage_help_with_value_after_commit`, `usage_version_with_value_after_commit`, `usage_version_with_value` | RESOLVED by running the original (found by the Phase 4 argv fuzzer, 11 of 4000 command lines differed): G is preceded by the flag itself, `Usage: toon --help <INPUT\|…>`, because help and version are not members of G; every other value-less flag prints G alone. S1.86 amended; `C.e86.usage` repaired; 12000 further random command lines agree. | 2026-09-20 |
