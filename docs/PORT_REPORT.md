# Port report: Toon → Bend 2   HOLD   gates on this commit's port/ and goldens/ (1071 cases), this report at the newest commit of main   2026-09-22   bend 2.0.16

<!-- Phase 6 document (SHIP-AND-CERTIFY). Every constant is computed from an
     artifact and pasted; every claim is proved / golden-tested / measured
     with the artifact named; anything else is deleted. Run
     scripts/claims-lint.sh on this file before committing it. -->

## Verdict

HOLD. The port is complete and every lane is green on the re-pinned corpus; ONE constant fails, and one number is missing. On 2026-09-22 the owner ordered every bug fixed instead of reproduced: the original was fixed first (`toon_rust` `694d73b`), the port re-pinned, 486 goldens re-captured and the port changed to match on every lane (spec Amendment A1). That is the largest change since Phase 3, and no non-author round has reviewed it yet:

1. **Convergence met for the tier: fails.** `converge.sh`: `NOT_CONVERGED` (T2 needs the last two rounds clean; the twenty-five non-author rounds, 6 to 30, found 6, 11, 8, 3, 10, 12, 13, 9, 15, 9, 5, 3, 5, 5, 2, 2, 2, 3, 2, 2, 2, 2, 2, 2 and 2 findings (rounds 20 to 30 counted under the adopted rule: new behavior findings of MEDIUM or above) (round 16: 1 HIGH, R16-1, a JSON number the port printed as 0 with exit 0 where the original exits 1, fixed in `5c3ac52`): 2 HIGH in round 7 on stdin, 1 HIGH in round 10 on cited commits that did not build from the public history, 1 HIGH in round 16 (R16-1); across rounds 9 to 30, exactly one unregistered difference in conversion content: R16-1 in round 16, now registered and fixed (the other twenty-one of those rounds found none; round 14: 11638 compared executions, one difference, which is DISC-004 and accurately registered; round 15: 34233, none; round 17: about 248000, none; round 18: about 607000, none; round 19: about 1094000, none; round 20: about 405,000, none; round 21: about 502,700, none; round 22: about 345,000, none; round 23: about 191,000 plus at least 50,000 from diff-fuzz, none; round 24: about 219,600 plus 30,240 from diff-fuzz, none; round 25: about 90,980 plus 30,240 from diff-fuzz, none; round 26: about 78,500 plus 30,240 from diff-fuzz, none; round 27: about 53,300 plus 30,240 from diff-fuzz, none; round 28: about 31,100 plus 30,040 from diff-fuzz, none; round 29: about 42,500 plus 30,040 from diff-fuzz, none; round 30: about 54,700 plus diff-fuzz with new seeds, none); their reports are in `docs/reviews/`). Owner: the author, with a fresh non-author subagent per round. Flips after two consecutive CLEAN rounds, and from round 20 a clean round is one that found NOTHING: every finding a round still counts under the adopted rule is a new BEHAVIOR finding of MEDIUM or above, so the method's "fewer than 3" threshold — calibrated when the count included law-coverage and document findings too — would admit a round carrying two MEDIUM behaviour defects (round 21's R21-2; `scripts/converge.sh` version-matches the threshold to the counting rule and names any row that contradicts itself).
2. **Every performance claim has a pin, cv ≤ 5%, identical sha: holds for what is claimed.** FIVE captures against the original on ordinary inputs are MEASURED and five are REFUSED_CV (nothing is claimed for those). Owner: the author, on a quiet host. These are missing numbers, not failing ones; they block only a speed sentence for those inputs. Two were added on 2026-09-25 in a window a peer session offered, both pinned with stdout sha identical between arms: `perf/evidence/F1.tabular30000-encode.json`, the tabular encode at ratio 0.1786 (the port 5.60× slower), and `perf/evidence/EXP-004.fold-60000-vs-original.json`, the folding ratio at 3.1264 (**the port 3.13× FASTER**, which resolves NE-004 from NO_EVIDENCE). Both had been refused for days for a reason the register recorded as the host and which was in part the INPUT: the tabular capture needed an arm of at least 100 ms and the largest tabular document was 163 KB, on which the original takes 0.00–0.01 s, so `perf/inputs/tabular_30000.json` had to exist first; and NE-004's own do-not-retry clause predicted that a fifth refusal on a quiet host meant the input was too small, and named 60000 keys. The five still refused are `INCUMBENT.decimals`, `.ints`, `.sci`, `.startup` and `.strings`, and on 2026-09-25 reading their ARMS split them and corrected this register's own attribution of all five to the host. In four, the ORIGINAL's arm was 3.45 ms (decimals, cv 21.8%), 5.92 ms (ints, 16.3%), 5.04 ms (sci, 20.0%) and 4.29 ms (strings, 21.9%) while the PORT's arm was 3.2–7.3% on three of them. **That reading was itself overstated and is corrected here**: it said "no host times a 3 ms process to 5%", and this register's own `INCUMBENT.tabular-enc` refutes it — a 6.28 ms original arm MEASURED at cv 4.53%. So a short arm is not disqualifying and the input was not shown to be a binding constraint; the longer inputs are a robustness measure, since a longer arm is less sensitive to load, not a necessary condition. What the evidence does show, as cleanly as this register contains anything, is that LOAD dominates at these arm lengths: `F1.tabular30000-encode` MEASURED an original arm of 115.20 ms at cv 3.54% on a quiet host, and `INCUMBENT.decimals-300000` REFUSED an arm of 118.59 ms at cv 14.94% under load 5.42 — near-identical arms, cv differing fourfold. `perf/inputs/` now carries one input per capture, each the same shape as the small one and longer only in length, sized against the CURRENT oracle (`694d73b`) at 122.0, 126.4, 112.1 and 102.7 ms — seven-sample medians from a sizing probe under load, enough to choose a length and not themselves gated captures. **A capture of the decimals one then isolated the cause, because it changed only one variable**: `perf/evidence/INCUMBENT.decimals-300000.json` put the original's arm at 118.59 ms, above the 100 ms predicate and within 3% of F1's 115.20 ms, and was still REFUSED_CV at cv 14.94% under load 5.42, while the port's arm (1705.84 ms) passed at cv 3.82% with the two stdout sha256 identical. F1 and EXP-004 had each changed the input AND run on a quiet host, so neither of those two captures separates the variables; this pair does. Both arms were the current pins for the first time (the port binary rebuilt from `port/main.bend` at HEAD to `sha256 825e44de69c3a4e6c0d442ea`, byte-identical to the frozen one, against `oracle/toon` at `694d73b`). So these four need a quiet host, which the pair above isolates as the dominant factor, and the new inputs make the arms less load-sensitive rather than making the captures possible. Nothing is claimed from the refused capture, including its arm ratio. The fifth, `INCUMBENT.startup`, is `--version`, has no input to scale, and is **not obtainable by this measurement design at all** (`perf/NEGATIVE-EVIDENCE.md` NE-038): both arms are about 0.95 ms, which is one `fork`/`exec` and one scheduler placement, and `cv` is the spread of the sample distribution rather than the standard error of the median, so neither a quieter host nor more samples reduces it. The same quantity does have a counted number, in a different claim class and needing no quiet host: `perf/COUNTED-PROFILE.md`'s `startup_encode` row, 532,601 port instructions against 504,653, a ratio of **1.06×**. A sixth number, the three ordinary-input ratios against the `opt-level=3` incumbent (bead `toon_bend-0i8`), cannot be captured faithfully at all on this machine: the pinned o3 binary (`sha256 0325f112…`, 862536 bytes, PLAN §2) is not on disk, and the o3 build that is (`b3683f39…`) is a different snapshot of a crate whose release builds PLAN §2 records as not byte-reproducible, so a capture against it would not compare with the 1.989× and 0.773× rows already published.

The DISC register is complete since 2026-09-20: the repository owner delegated the rulings to the author ("You decide on everything. I approve whatever you want to do."), eleven entries are ACCEPTED with a scoped contract each (DISC-014 and DISC-015 were found and ruled on after the delegation, under it), four are RESOLVED (two by repairs of the port, DISC-002 and DISC-010 by the fixes in the original), and the port's one custom effect `Stdin.open` stays.

## Constants (computed, never asserted)

| constant | value | evidence |
|---|---|---|
| 100% of cases pass on every lane | `{"lanes":[{"lane":"interpreter","verdict":"PASS","passed":1127,"failed":0},{"lane":"c-1t","verdict":"PASS","passed":1127,"failed":0},{"lane":"c-8t","verdict":"PASS","passed":1127,"failed":0},{"lane":"js","verdict":"PASS","passed":1127,"failed":0}],"stderr_compared":true,"timeouts_seconds":{"interpreter":60.0,"compiled":5.0,"build":600},"verdict":"PASS"}` on the tree of `e19d8b5` and its 1127 cases (round 30's repairs; the native binary byte-identical to that of `5000b55` and `9c9039e`, sha256 `825e44de…`; `scripts/lanes.sh … --threads 8`, local 2026-09-26 09:12 to 2026-09-26 10:49, one run); the commits after it change documents only; after the goldens were RE-CAPTURED from `toon_rust` `694d73b` (the owner's order to fix every bug; 486 cases changed) and extended by the round-16 cases; c-1t also passes 1103/1103 under `TOON_SPEC=1` (2026-09-24, the binary `825e44de…`) | `scripts/lanes.sh` |
| all laws check | `All terms check.` (unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16 @ `15ae0c8`), 682 laws, re-run whole on 2026-09-26 (9 min 24 s under a 10 GB cap) on the tree of `e19d8b5` (EXP-029, EXP-031, the laws of rounds nineteen to thirty; EXP-032 reverted; run on its pre-rebase twin, whose `port/` is byte-identical) | `(cd port && bun /tmp/bend/bend2/main.ts PROOF.bend)` |
| law coverage | `{"laws": 682, "proofs": 682, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | `scripts/law-coverage.sh` |
| the gates bite | `harness-selftest.sh -- ./oracle/toon`: `{"schema":"p2b.harness-selftest.v1","sha":"3d6f6fb","bend":"bend 2.0.16","host":"Linux-x86_64","lane":"c-1t","mutations":47,"caught":47,"leaked":[],"untestable":[],"controls":1,"controls_passed":1,"false_positives":[],"scratch":"/data/tmp/hst.o6oNvA","verdict":"OK"}` (2026-09-26, 47 mutations and the first CONTROL). A control is the inverse of a mutation: a change that is NOT a lie, which the gate must pass in silence, counted on its own axis so that `mutations`/`caught` keep meaning exactly what they meant before controls existed. C1 hides a findings table with a DIFFERENT count inside an HTML comment while the visible table stays correct: nothing a reader sees is wrong, so a gate that speaks is reading what no reader sees. It exists because round 24 found the reader refusing exactly that shape, reporting "2 findings tables" — the first FALSE POSITIVE in this family, where every earlier finding was a gate being too permissive. **This line and the one it replaces are equally valid.** Their scratch trees differ — the earlier run's lacks the e2e benchmark corpus, 179 MB — because that directory is untracked and ignored (`.gitignore` line 20, zero tracked files under it), so a clone never has it and that run was made from a clone. **It does not affect the verdict: no gate the self-test exercises reads it** (`claims-audit`'s only matches are `perf/evidence/COUNTED.e2e-corpus.*.json` filenames and a comment), so the difference is a disk footprint, not coverage. I reached that through two wrong explanations, both recorded on `toon_bend-lfq` (closed): first that a full disk had cut the copy short, which invented a cause for an absence and which a peer session refuted; then that the gate's verdict depends on which checkout it runs from, which a grep the same peer proposed then disproved. A failed copy is fatal now as a DEFENSIVE change rather than a repair of an observed failure, and the two largest read-only inputs are linked rather than copied, which took a run from 1004 MB of scratch to 378 MB (2026-09-25, the run that added M20 to M22, round 23: the audit refuses a re-spelled report, converge.sh an empty one, the audit an unreachable commit in any evidence file; its `sha` names HEAD, and the run was made on that tree plus the uncommitted repair its first run forced: that run said `"caught":16,"leaked":["M22_evidence_any_file"]` because the reachability rule skipped all-letter runs and M22 plants `deadbee`, so all-letter runs are read now). M14, M15, M18 and M19 assert on `converge.sh`'s MESSAGE rather than its exit code, because that gate is legitimately NOT_CONVERGED on the clean tree; each requires its phrase to be ABSENT on the clean copy and PRESENT after the mutation, so a phrase already in the output cannot pass the test written for it. An earlier run, on the same tree, said `"caught":11,"untestable":["M12_stale_count"]`: M12 mutates a count in a document and checks that `claims-audit.py` goes red, which it cannot show while that gate is ALREADY red — and it was, on the two findings the audit made about round 12 (a stale converge line, and its own regex missing a report's bolded rows). The untestable verdict was the gate reporting honestly that it could not run, not a leak | `scripts/harness-selftest.sh` |
| the laws bite | all 34 hand-written mutants the inventory held at that commit, M01 to M35, in ONE run against the 393-law set (2026-09-22, 73 min): `{"laws_in_proof": 148, "reduced": true, "mutants": 34, "killed": 34, "survived": [], "not_evidence": [], "verdict": "STRONG"}`, the unmutated copy GREEN first, 31 DISTINCT laws doing the killing. Two mutants were rewritten because the code they mutated no longer exists after the bug fixes: M24 (was: writer B stops escaping DEL; now: the one writer escapes DEL again, killed by `writer_b_keeps_del_raw`) and M30 (was: the zmij 1e16 boundary; now: the lower JavaScript boundary, killed by `json_plain_at_kneg5`); M34 now moves the upper boundary to k = 22 and is killed by `json_exponent_at_k22`. Earlier runs are superseded by this one. The verdict is evidence about THIS mutant set, not about the completeness of the laws. `scripts/law-mutation.sh` itself: INCONCLUSIVE on the three modules (no valid textual site) | `scripts/hand-mutants.py` |
| board FULL or DEBT, every exclusion classed | `{"rows": 33, "present": 27, "partial": 0, "missing": 0, "excluded": 6, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | `scripts/parity-board.sh` |
| DISC register complete | 11 ACCEPTED (each with a scoped contract), 4 RESOLVED (DISC-009, DISC-012 by repairs; DISC-002 and DISC-010 by the fixes in the original), 0 OPEN | `docs/DISCREPANCIES.md`; `scripts/converge.sh`: `"open_disc": []` |
| floor unchanged since capture | `{"repeat":2,"stable":1127,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` (re-run 2026-09-26 on the 1127-case corpus of `e19d8b5`; before that 2026-09-23 on the 1074-case corpus of `5c3ac52`; the same verdict at 1071 cases came from the floor row of the same `port-doctor.sh` run as the other gate lines here; an earlier dedicated run at 3 repeats was STABLE too. This row pasted that 3-repeat line under the doctor run's attribution until round 15 (R15-6) | `scripts/floor.sh` |
| MANIFEST unchanged since the clean tail | there is no clean tail yet; last change: the `--repin` re-capture of 2026-09-22 from `toon_rust` `694d73b` (486 cases changed, 0 added; its `recapture:` line names the reason). Before it, every re-capture had only ADDED hashes, except the first re-pin of the same day (`7c1d6e4`, 7 cases of C-10) | git |
| evidence ≤ 24 h old on this commit | the gate lines are from the `port-doctor.sh` run of 2026-09-22 (local 10:25 to 12:10), the proof and the 34-mutant run of the same day, and the stdio probe of the same day; older material in this report is labelled with its own date | `docs/PORT_STATE.md` |
| zero open high-severity findings | round 7's two HIGH findings (stdin re-opened by path) are repaired and have regression rows (`scripts/stdio-probe.py`); round 10's one HIGH finding (cited commits that did not build) is repaired and has a gate (`scripts/clean-build-check.sh`); rounds 8 and 9 had none | PORT_STATE rounds table, `docs/reviews/` |
| convergence met for the tier | `{"tier": "T2", "rounds": 30, "clean": 5, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": [], "unfixed": [], "verdict": "NOT_CONVERGED", "missing": ["clean rounds since last reset 0 < 2"]}`: FAILS | `scripts/converge.sh` |
| every perf claim has a pin, cv ≤ 5%, identical sha | the MEASURED captures named below; three NO_EVIDENCE; **19** files in `perf/evidence/` carry `REFUSED_CV` (`grep -l REFUSED_CV perf/evidence/*.json \| wc -l`; `scripts/claims-audit.py` computes and checks this number now) and not one of them supports a ratio: the five INCUMBENT captures, the one-lever captures of EXP-001 and EXP-002 with two arm files each, and the folding capture (NE-004) are claimed nowhere; the two pinned-build re-captures and three captures against the `opt-level=3` build are refusals that NE-006 names as claiming nothing; EXP-005's is NE-007; EXP-006's is NE-010; `QGU.wide2000.json` is the qgu fix's refused pair, whose claim is its growth table instead. NO row is admitted to the WIN ledger (NE-001, 002, 003, 005, 010, 011, 013 and 015 are provisional, NE-004 and NE-007 are NO_EVIDENCE, NE-006 is a measured loss, NE-014 neutral and reverted) | `perf/evidence/`, `perf/PERF-LEDGER.md`, `perf/NEGATIVE-EVIDENCE.md` |
| claims audit | `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 6, "laws": 682, "cases": 1127, "disc": {"accepted": 11, "resolved": 4, "open": 0}, "verdict": "OK"}` | `python3 scripts/claims-audit.py` |
| claims lint | `claims-lint: 0 hit(s) in 11 file(s)`: this file, `README.md`, `CONTRIBUTING.md`, `docs/PORT_STATE.md`, `docs/PARITY_RUNBOOK.md`, `docs/DISCREPANCIES.md`, `docs/OPEN_QUESTIONS.md`, `perf/*.md` | `scripts/claims-lint.sh` |

## Claims

### Proved
- 41 quantified laws, for every input and under the checker's assumptions: the word loop of the shortest-digit generator runs exactly when the kill-switch is open and the fixed state fits, and its closed arm is the spec loop (`dg_run_with_gate`, `dg_run_words_spec_arm`), the parallel number pre-pass is closed by the kill-switch and changes when a number is printed, never what (`pre_gate_switch`, `pre_raw_is_num`, `pre_txt_is_num`), with key folding off a fold attempt never folds (`fold_off_never_folds`), under the kill-switch no fold context is lean (`fctx_lean_closed_by_switch`), the first failure ends a pass (`argv_stop_is_sticky`, `decode_error_ends_pass`, `decode_root_ends_pass`, `json_error_is_sticky`), in strict mode a line after the complete root and an unmergeable repeated key are reported (`decode_trailing_line_is_error`, `decode_dup_reported_at_end`), lenient mode never reports a scan or body check (`lenient_scan_never_fails`, `lenient_body_never_fails`), the mode flags win (`encode_flag_wins`, `decode_flag_wins`, `stdin_defaults_to_encode`), equal estimates print `No token difference` (`no_saved_line_without_savings`), the expansion cap on values and merges, the kill-switch gate (`twin_gate_switch`, `twin_gate_open`), and the JSON reader's string arm with its fast path closed is the reader's step (`step_in_str_slow_is_step`, EXP-027); `All terms check.` (unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16)
- 641 closed laws, each about ONE value: 346 unit laws (the shortest-digit loop over U32 words against the spec loop (seven) and its fit boundary on each operand (five), EXP-012; the big-natural carry, the divisions and readers of the fast twins against their specification twins, the twins' bounds, the key hash, the balanced buckets, the percent roundings, the JSON number text's four boundaries, the expansion nesting limit, round 13's six on the escape table, UTF-8 surrogates and TAB quoting, the quotient-estimate digit step against the spec loop (six), the hot repeated-key merge against the chain merge (four), the header precheck (four), the JSON writer's empty containers (four), the encoder's edge-White_Space walk (six, EXP-013), the repeated-key failure order across a nested merge (two, round 17), the UTF-8 decoder that only validates (eleven, EXP-018), the TOON text's framing (five, EXP-021), the trailing trim (seven, EXP-022) the string literal's walk and failure order (seven, EXP-024), the scanner over the reversed text (fourteen, EXP-025), the numeric-like recognizer against the lexer (eighteen, EXP-026) the JSON reader's string arm and its guard (fourteen, EXP-027), round 18's six on delimited values, the first bad escape, FF before a quoted key and a header's field cut, the repeated-key check of small and large JSON objects (seven, EXP-029), and round 19's seven on a trailing fraction zero, a final CR, a byte order mark after the first line and an escaped quote before a colon) and 295 captured goldens restated as `run_pure(argv, bytes) == (exit code, stdout, stderr)`; same verdict line
- NOT proved: `fast == spec` for every input, for any of the three twins; anything about the compiled C or the JavaScript build; anything about a non-integer number through the pipeline (the checker does not normalize the shortest-digit generator)

### Golden-tested
- Since EXP-007 (`0131342`) the encoder's number pre-pass is a parallel let: above `--threads 1` the runtime runs it on a worker pool, so `c-8t` is a genuinely parallel lane on every `--encode` case; decoding and every other path stay sequential, and no bang is placed.
- 1071 cases on interpreter, c-1t, c-8t, js at that commit, on the goldens re-captured from `toon_rust` `694d73b` (sha256 `821287ea…` of `oracle/toon`), 2026-09-22, the doctor run of the first constant; c-1t also under `TOON_SPEC=1`. Before the re-pin: 1065 cases on interpreter, c-1t, c-8t, js at `2dee063` (earlier all-lane passes: 1065 at `c7239e2`, 1065 at `4c3cccc`, 1060 at `d80251a`, 1053 at `1230a0d`; of those only `1230a0d` builds from the public history, see Reproduce); gpu MISSING: no bang is placed (text with data-dependent structure); the three compiled lanes also under `TOON_SPEC=1`; MANIFEST today 3381 hashes (the capture of 2026-09-24, `./oracle/toon`, `toon 0.2.4` @ `694d73b`); the lanes line above is of 2026-09-20
- outside the corpus, against the original, 0 differences on conversion content: the author's seeded lenses (`scripts/diff-fuzz.py`: mutate, docs, argv, expand, collide, numbers under `TOON_SPEC=1`, scale) and the non-author rounds (about 122000, 90000 and 948000 compared executions in rounds 7, 8 and 9; round 10 added terminals, environments, thread counts and a fresh clone)

### Measured
- 1.67× / 1.56× / 2.12× against the port's own build of `4bfecef` (EXP-001 tabular encode, EXP-002 tabular decode, EXP-003 9000 decimals; 18 samples per arm in AB/BA pairs; cv ≤ 1.5%; A/A 1.001 / 0.995 / 1.004; stdout sha equal; AMD EPYC-Milan 8 cores, Linux, 1 thread; 2026-09-20); PROVISIONAL, not WIN (NE-001..003)
- against the PINNED original (`toon 0.2.4` @ `f955c67`, the `opt-level="z"` release build, 1 thread each): 0.25× on one object of 16000 keys, 1.27× on 20 rows of 1200 fields, 3.06× on 40000 expanded lines (cv ≤ 4.7%; stdout sha equal; `perf/evidence/EXP-004.*-vs-original.json`), measured on the build of `1230a0d`
- against the STRONGEST build of the original (the `opt-level=3` incumbent PLAN §2 promised, built 2026-09-20; it passes all 1065 goldens): 1.989× on 40000 expanded lines and **0.773× on 20 rows of 1200 fields — the port is slower there**, where the pinned build gave 3.06× and 1.27× (`perf/evidence/INCUMBENT-O3.expand.json`, `.wide-rows.json`; NE-006). What the profile is worth is input-dependent and has no single factor: 1.729× on wide rows and 1.389× on expand, both MEASURED (`.vs-z-wide-rows.json`, `.vs-z-expand.json`), REFUSED on the tabular encode. The two columns are separate captures made with DIFFERENT port binaries and are not an interleaved pair (round 13, R13-1); the re-capture that would make them one is bead `toon_bend-0i8`

- against the pinned original (`toon 0.2.4` @ `f955c67`, release; the port binary sha256 `27787b9b…`, reproduced by `bend port/main.bend -o <dir>/toon` at any commit since `a725d10` — the build is byte-reproducible and the output basename is its only path input; 1 thread, 15 AB/BA pairs): 0.114× on the 1500-row table encode, 0.404× on its decode, 0.0066× on 20000 doubles (cv ≤ 4.7%; `perf/evidence/INCUMBENT.tabular-enc.json`, `.tabular-dec.json`, `.doubles.json`; entry NE-006); five more captures of that run were REFUSED_CV and claim nothing
- EXP-003 alone (one lever per artifact): 2.05× against the all-twins-off arm of the current code (cv 2.7% / 3.9%; A/A 1.019; `perf/evidence/EXP-003.one-lever.ab.json`); the 1.67× and 1.56× above belong to the three levers TOGETHER: EXP-001 and EXP-002 alone were REFUSED_CV twice and are not shown to meet their gates

### Not claimed
- a ratio against the original for integers, one-decimal numbers, strings, scientific notation and startup: REFUSED_CV three times (`perf/evidence/INCUMBENT.*.json`); predicate: a quiet host and inputs ten times larger
- the folding input's ratio: NE-004 (REFUSED_CV three times)
- a WIN for EXP-001..003: NE-001..003 (closed laws only; one artifact carries three levers)
- any ratio for the code of the re-pin: every capture above ran against `f955c67` or `7c1d6e4` and against port builds before the bug fixes, which changed the JSON number reader (the correctly rounded path replaced serde_json's) and the JSON number printer; predicate: `scripts/incumbent-bench.sh --pin` against a build of `694d73b`
- exclusions, each classed in PLAN §3: async streaming, WebAssembly bindings, the `EncodeReplacer` callback, library-only behavior no CLI path reaches, shell completions / tracing / build metadata, native Windows

## Discrepancies

ACCEPTED on 2026-09-20 by the owner's delegation, one line each (id, class, kill-switch or mitigation, cases affected): DISC-001 Platform, runtime flags before `--`, launcher, 0 · DISC-002 Platform, the literal program name, none, 0 (RESOLVED 2026-09-22: the original names `toon` too) · DISC-003 Platform, an unwritable stderr, launcher repairs closed and read-only, 0 · DISC-004 Platform, non-UTF-8 argv, none, 0 · DISC-005 Platform, ANSI styling, none, 0 · DISC-006 Platform, a failed stdout write, launcher repairs closed and read-only, 0 · DISC-007 Platform, closed descriptors on the bare native binary, launcher, 0 · DISC-008 Platform, `-o` mode 0644, none, 0 · DISC-010 Performance, deep nesting, none, 0 (RESOLVED 2026-09-22: both programs stop at 127 levels) · DISC-011 Performance, the runtime's resource floor, the JavaScript build, 0 · DISC-013 Performance, non-integer numbers 45 to 306 times slower (linear), none, 0 · DISC-014 Platform, a `/dev/fd/N` path the caller did not open reaches a descriptor of the runtime, none, 0 · DISC-015 Platform, the sizes of the reads on stdin (packet pipes, SEQPACKET above 32 bytes, O_DIRECT files convert where the original fails), none, 0. RESOLVED by repairs: DISC-009, DISC-012 (the stdin effect; regression rows in `scripts/stdio-probe.py`). Approver of every entry: the repository owner through the delegation quoted above.

## Reproduce (an auditor gets the same lines)

Check out the NEWEST commit of `main`: this report, the scripts it calls and the lines it pastes belong together there. (The gate lines were produced on the tree of `c7239e2`; since then `port/` changed ONLY in `port/LAWS.bend` and `port/PROOF.bend`, neither of which the built program imports: `git diff --stat c7239e2 HEAD -- port/` lists those two files, 148 insertions and 8 deletions, which are 15 new laws with their proofs (round 13's six, round 14's seven and the author-side hunt's two) plus the law file's header. This sentence used to say the change was "in comments of `port/LAWS.bend` only" (round 15, R15-4). Commits `d80251a`, `861abf2` and `4c3cccc`, which earlier versions of this report named, do not build from the public history: `./scripts/clean-build-check.sh 4c3cccc` → FAIL. Round 11 showed that naming `c7239e2` in this recipe was wrong too: `clean-build-check.sh` itself arrived in the NEXT commit, `77a63d9`.) The oracle binary is not in the repository: `docs/PIN.toml` has its commit and sha256, PLAN §2 the build command. WITHOUT it these run: the build, `clean-build-check.sh`, `converge.sh`, `hand-mutants.py`, the claims lint (and `conform.sh`, the proof, the lints of CONTRIBUTING §2). They do NOT: `floor.sh`, `port-doctor.sh`, `stdio-probe.py`, `diff-fuzz.py`, `incumbent-bench.sh` (each says so and exits 2).

```bash
git checkout main
export BEND_NO_TELEMETRY=1 BEND_CLI='bun /tmp/bend/bend2/main.ts'      # bendlang/bend at 15ae0c8
./scripts/floor.sh goldens/cases.tsv goldens --repeat 3 -- ./oracle/toon
./scripts/port-doctor.sh --threads 8 --original ./oracle/toon -- --switch TOON_SPEC=1 --probe '["--encode","cases/inputs/hand/large_tabular_1500.json"]'
./scripts/clean-build-check.sh
./scripts/converge.sh docs/PORT_STATE.md          # exit 1 while NOT_CONVERGED: run it on its own line
python3 scripts/claims-audit.py && python3 scripts/hand-mutants.py
$BEND_CLI port/main.bend -o ./x && python3 scripts/stdio-probe.py -- ./x -- && python3 scripts/diff-fuzz.py scale --runs 16000 -- ./x --
./scripts/incumbent-bench.sh --runs 9 --pin "bend 2.0.16 @15ae0c8; toon 0.2.4 @f955c67" --original ./oracle/toon -e perf/inputs/wide_rows_1200.json --port ./x --threads 1 -- -e perf/inputs/wide_rows_1200.json
./scripts/claims-lint.sh README.md CONTRIBUTING.md docs/PORT_REPORT.md docs/PORT_STATE.md docs/PARITY_RUNBOOK.md docs/DISCREPANCIES.md docs/OPEN_QUESTIONS.md perf/*.md
```
