# Port report: Toon → Bend 2   HOLD   gates on this commit's port/ and goldens/ (1071 cases), this report at the newest commit of main   2026-09-22   bend 2.0.16

<!-- Phase 6 document (SHIP-AND-CERTIFY). Every constant is computed from an
     artifact and pasted; every claim is proved / golden-tested / measured
     with the artifact named; anything else is deleted. Run
     scripts/claims-lint.sh on this file before committing it. -->

## Verdict

HOLD. The port is complete and every lane is green; ONE constant fails, and one number is missing:

1. **Convergence met for the tier: fails.** `converge.sh`: `NOT_CONVERGED` (T2 needs the last two rounds clean; the ten non-author rounds, 6 to 15, found 6, 11, 8, 3, 10, 12, 13, 9, 15 and 9 findings: 2 HIGH in round 7 on stdin, 1 HIGH in round 10 on cited commits that did not build from the public history, none since; 0 unregistered differences in conversion content in rounds 9 to 15 (round 14: 11638 compared executions, one difference, which is DISC-004 and accurately registered; round 15: 34233, none); their reports are in `docs/reviews/`). Owner: the author, with a fresh non-author subagent per round. Flips after two consecutive rounds with fewer than 3 new genuine findings.
2. **Every performance claim has a pin, cv ≤ 5%, identical sha: holds for what is claimed.** Three captures against the original on ordinary inputs are MEASURED and five are REFUSED_CV (nothing is claimed for those). Owner: the author, on a quiet host. These are missing numbers, not failing ones; they block only a speed sentence for those inputs.

The DISC register is complete since 2026-09-20: the repository owner delegated the rulings to the author ("You decide on everything. I approve whatever you want to do."), thirteen entries are ACCEPTED with a scoped contract each (DISC-014 and DISC-015 were found and ruled on after the delegation, under it), two are RESOLVED by repairs, and the port's one custom effect `Stdin.open` stays.

## Constants (computed, never asserted)

| constant | value | evidence |
|---|---|---|
| 100% of cases pass on every lane | `{"lanes":[{"lane":"interpreter","verdict":"PASS","passed":1071,"failed":0},{"lane":"c-1t","verdict":"PASS","passed":1071,"failed":0},{"lane":"c-8t","verdict":"PASS","passed":1071,"failed":0},{"lane":"js","verdict":"PASS","passed":1071,"failed":0}],"stderr_compared":true,"timeouts_seconds":{"interpreter":60.0,"compiled":5.0,"build":600},"verdict":"PASS"}` on this commit's `port/` and `goldens/` (1071 cases, round 15's six included), run in a scratch clone of it because an external `git reset --hard` loop on the working checkout voided the first attempt, inside one `port-doctor.sh` run whose last line is `{"proof":"All terms check.","unsafe":"0","unsafe_annotations":"0","instances":"0","unsafe_count_mode":"explicit-and-instances","bend":"bend 2.0.16","lanes":"PASS","board":"DEBT","floor":"STABLE","switch":"PASS","verdict":"GREEN"}` | `scripts/port-doctor.sh` |
| all laws check | `All terms check.` (unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16 @ `15ae0c8`), 383 laws, re-run 2026-09-22 after round 14 added seven and an author-side hunt two more (the doctor's run of the evening before proved the 374 that existed then, with the same verdict) | `(cd port && bun /tmp/bend/bend2/main.ts PROOF.bend)` |
| law coverage | `{"laws": 383, "proofs": 383, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | `scripts/law-coverage.sh` |
| the gates bite | `harness-selftest.sh -- ./oracle/toon`: `{"schema":"p2b.harness-selftest.v1","sha":"713adfe","bend":"bend 2.0.16","host":"Linux-x86_64","lane":"c-1t","mutations":12,"caught":12,"leaked":[],"untestable":[],"scratch":"/data/tmp/hst.mlPECX","verdict":"OK"}`. The run before it, on the same tree, said `"caught":11,"untestable":["M12_stale_count"]`: M12 mutates a count in a document and checks that `claims-audit.py` goes red, which it cannot show while that gate is ALREADY red — and it was, on the two findings the audit made about round 12 (a stale converge line, and its own regex missing a report's bolded rows). The untestable verdict was the gate reporting honestly that it could not run, not a leak | `scripts/harness-selftest.sh` |
| the laws bite | all 25 mutants in ONE run (2026-09-21, 52 min): `{"laws_in_proof": 129, "reduced": true, "mutants": 25, "killed": 25, "survived": [], "not_evidence": [], "verdict": "STRONG"}`, with the unmutated copy checking GREEN first and 22 DISTINCT laws doing the killing. Round 13 wrote three of them (M24, M25, M26), which survived the full 368-law proof until the laws of 2026-09-20 pinned them; they are killed here inside the full set, by `writer_b_escapes_del`, `utf8_rejects_surrogate` and `tab_in_value_forces_quotes`. This supersedes the earlier complete run over the 22 mutants that existed then (`"mutants": 22, "killed": 22, … "STRONG"`). Round 14 then wrote five more (R14-1) in defs the set did not reach; all five SURVIVED the whole 374-law proof until seven laws of 2026-09-22 pinned them. As M27–M31: `{"laws_in_proof": 136, "reduced": true, "mutants": 5, "killed": 5, "survived": [], "not_evidence": [], "verdict": "STRONG"}`, each by the law written for it; two of the seven laws (`is_ws_not_after_ogham`, `json_exponent_at_k17`) killed no mutant of that run. An author-side hunt before round 15 then tried OTHER entries of the same small tables: two White_Space entries (U+3000, U+00A0) survived the full 381-law proof, and two laws now pin all 25 members and SAMPLE 27 non-members (a closed law cannot range over every code point, so a non-member it does not sample is held by a corpus case, `encstr_non_whitespace_u1234_bare`, which round 15's R15-2 prompted); as M32 and M33 they are killed (`"mutants": 2, "killed": 2, … "STRONG"`). Round 15 (R15-10) then showed `json_exponent_at_k17` and the hunt's `is_ws_documented_non_members` are each the ONLY law killing one of its mutants, now M34 and M35 (`"mutants": 2, "killed": 2, … "STRONG"`); of the laws flagged here, only `is_ws_not_after_ogham` is still not shown to bite on its own, and the point it pins (U+1681) is one of the 27 the non-members law samples. 34 mutants in all, killed across four runs, not in one run of all 34. The verdict is evidence about THIS mutant set, not about the completeness of the laws — round 14 is the second round in a row whose own new mutants survived, which is exactly why. `scripts/law-mutation.sh` itself: INCONCLUSIVE on the three modules (no valid textual site) | `scripts/hand-mutants.py` |
| board FULL or DEBT, every exclusion classed | `{"rows": 33, "present": 27, "partial": 0, "missing": 0, "excluded": 6, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | `scripts/parity-board.sh` |
| DISC register complete | 13 ACCEPTED (each with a scoped contract), 2 RESOLVED, 0 OPEN | `docs/DISCREPANCIES.md`; `scripts/converge.sh`: `"open_disc": []` |
| floor unchanged since capture | `{"repeat":2,"stable":1071,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}`, the floor row of the same `port-doctor.sh` run as the other gate lines here; an earlier dedicated run at 3 repeats was STABLE too. This row pasted that 3-repeat line under the doctor run's attribution until round 15 (R15-6) | `scripts/floor.sh` |
| MANIFEST unchanged since the clean tail | there is no clean tail yet; last change `4c3cccc` (its subject: Round 8, non-author, dirty: 0 HIGH, 4 MEDIUM, 4 LOW; EBADF on stdin ends the input, launcher repairs, 5 cases, 368 laws at that commit) (15 hashes added, 0 changed; every re-capture of this port only ever ADDED hashes) | git |
| evidence ≤ 24 h old on this commit | the gate lines are from the `port-doctor.sh` run of local 2026-09-21 20:46–22:26 (stamped 2026-09-22 UTC), the 25-mutant run and `clean-build-check.sh` of the same evening, and round 14 of 2026-09-21; older material in this report is labelled with its own date | `docs/PORT_STATE.md` |
| zero open high-severity findings | round 7's two HIGH findings (stdin re-opened by path) are repaired and have regression rows (`scripts/stdio-probe.py`); round 10's one HIGH finding (cited commits that did not build) is repaired and has a gate (`scripts/clean-build-check.sh`); rounds 8 and 9 had none | PORT_STATE rounds table, `docs/reviews/` |
| convergence met for the tier | `{"tier": "T2", "rounds": 15, "clean": 5, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": [], "verdict": "NOT_CONVERGED", "missing": ["clean rounds since last reset 0 < 2"]}`: FAILS | `scripts/converge.sh` |
| every perf claim has a pin, cv ≤ 5%, identical sha | the MEASURED captures named below; two NO_EVIDENCE; **16** files in `perf/evidence/` carry `REFUSED_CV` (`grep -l REFUSED_CV perf/evidence/*.json \| wc -l`; `scripts/claims-audit.py` computes and checks this number now) and not one of them supports a ratio: the five INCUMBENT captures, the one-lever captures of EXP-001 and EXP-002 with two arm files each, and the folding capture (NE-004) are claimed nowhere; the two pinned-build re-captures and three captures against the `opt-level=3` build are refusals that NE-006 names as claiming nothing; EXP-005's is NE-007. NO row is admitted to the WIN ledger (NE-001, 002, 003 and 005 are provisional, NE-004 and NE-007 are NO_EVIDENCE, NE-006 is a measured loss) | `perf/evidence/`, `perf/PERF-LEDGER.md`, `perf/NEGATIVE-EVIDENCE.md` |
| claims audit | `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 2, "laws": 383, "cases": 1071, "disc": {"accepted": 13, "resolved": 2, "open": 0}, "verdict": "OK"}` | `python3 scripts/claims-audit.py` |
| claims lint | `claims-lint: 0 hit(s) in 11 file(s)`: this file, `README.md`, `CONTRIBUTING.md`, `docs/PORT_STATE.md`, `docs/PARITY_RUNBOOK.md`, `docs/DISCREPANCIES.md`, `docs/OPEN_QUESTIONS.md`, `perf/*.md` | `scripts/claims-lint.sh` |

## Claims

### Proved
- 14 quantified laws, for every input and under the checker's assumptions: the first failure ends a pass (`argv_stop_is_sticky`, `decode_error_ends_pass`, `decode_root_ends_pass`, `json_error_is_sticky`), lenient mode never reports a scan or body check (`lenient_scan_never_fails`, `lenient_body_never_fails`), the mode flags win (`encode_flag_wins`, `decode_flag_wins`, `stdin_defaults_to_encode`), no `Saved` line without savings, the expansion cap on values and merges, and the kill-switch gate (`twin_gate_switch`, `twin_gate_open`); `All terms check.` (unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16)
- 369 closed laws, each about ONE value: 74 unit laws (the big-natural carry, the divisions and readers of the fast twins against their specification twins, the twins' bounds, the key hash, the balanced buckets, the percent roundings, and round 13's six on writer B's escape table, UTF-8 surrogates and TAB quoting) and 295 captured goldens restated as `run_pure(argv, bytes) == (exit code, stdout, stderr)`; same verdict line
- NOT proved: `fast == spec` for every input, for any of the three twins; anything about the compiled C or the JavaScript build; anything about a non-integer number through the pipeline (the checker does not normalize the shortest-digit generator)

### Golden-tested
- The two native lanes are ONE sequential execution under two labels: the port places no bang and no parallel let, so the runtime never starts a worker pool and `--threads N` changes nothing (2 OS threads at `--threads` 1, 8 and 64, measured by round 10 and re-measured); `c-8t` adds no evidence beyond `c-1t` today and is kept because it would catch a parallel twin the day one is added.
- 1065 cases on interpreter, c-1t, c-8t, js at `2dee063` (earlier all-lane passes: 1065 at `c7239e2`, 1065 at `4c3cccc`, 1060 at `d80251a`, 1053 at `1230a0d`; of those only `1230a0d` builds from the public history, see Reproduce); gpu MISSING: no bang is placed (text with data-dependent structure); the three compiled lanes also under `TOON_SPEC=1`; MANIFEST 3213 hashes captured from `./oracle/toon` (`toon 0.2.4` @ `f955c67`); 2026-09-20
- outside the corpus, against the original, 0 differences on conversion content: the author's seeded lenses (`scripts/diff-fuzz.py`: mutate, docs, argv, expand, collide, numbers under `TOON_SPEC=1`, scale) and the non-author rounds (about 122000, 90000 and 948000 compared executions in rounds 7, 8 and 9; round 10 added terminals, environments, thread counts and a fresh clone)

### Measured
- 1.67× / 1.56× / 2.12× against the port's own build of `4bfecef` (EXP-001 tabular encode, EXP-002 tabular decode, EXP-003 9000 decimals; 18 samples per arm in AB/BA pairs; cv ≤ 1.5%; A/A 1.001 / 0.995 / 1.004; stdout sha equal; AMD EPYC-Milan 8 cores, Linux, 1 thread; 2026-09-20); PROVISIONAL, not WIN (NE-001..003)
- against the PINNED original (`toon 0.2.4` @ `f955c67`, the `opt-level="z"` release build, 1 thread each): 0.25× on one object of 16000 keys, 1.27× on 20 rows of 1200 fields, 3.06× on 40000 expanded lines (cv ≤ 4.7%; stdout sha equal; `perf/evidence/EXP-004.*-vs-original.json`), measured on the build of `1230a0d`
- against the STRONGEST build of the original (the `opt-level=3` incumbent PLAN §2 promised, built 2026-09-20; it passes all 1065 goldens): 1.989× on 40000 expanded lines and **0.773× on 20 rows of 1200 fields — the port is slower there**, where the pinned build gave 3.06× and 1.27× (`perf/evidence/INCUMBENT-O3.expand.json`, `.wide-rows.json`; NE-006). What the profile is worth is input-dependent and has no single factor: 1.729× on wide rows and 1.389× on expand, both MEASURED (`.vs-z-wide-rows.json`, `.vs-z-expand.json`), REFUSED on the tabular encode. The two columns are separate captures made with DIFFERENT port binaries and are not an interleaved pair (round 13, R13-1); the re-capture that would make them one is bead `toon_bend-0i8`

- against the pinned original (`toon 0.2.4` @ `f955c67`, release; the port binary sha256 `27787b9b…`, reproduced by `bend port/main.bend -o <dir>/toon` at any commit since `a725d10` — the build is byte-reproducible and the output basename is its only path input; 1 thread, 15 AB/BA pairs): 0.114× on the 1500-row table encode, 0.404× on its decode, 0.0066× on 20000 doubles (cv ≤ 4.7%; `perf/evidence/INCUMBENT.tabular-enc.json`, `.tabular-dec.json`, `.doubles.json`; entry NE-006); five more captures of that run were REFUSED_CV and claim nothing
- EXP-003 alone (one lever per artifact): 2.05× against the all-twins-off arm of the current code (cv 2.7% / 3.9%; A/A 1.019; `perf/evidence/EXP-003.one-lever.ab.json`); the 1.67× and 1.56× above belong to the three levers TOGETHER: EXP-001 and EXP-002 alone were REFUSED_CV twice and are not shown to meet their gates

### Not claimed
- a ratio against the original for integers, one-decimal numbers, strings, scientific notation and startup: REFUSED_CV three times (`perf/evidence/INCUMBENT.*.json`); predicate: a quiet host and inputs ten times larger
- the folding input's ratio: NE-004 (REFUSED_CV three times)
- a WIN for EXP-001..003: NE-001..003 (closed laws only; one artifact carries three levers)
- exclusions, each classed in PLAN §3: async streaming, WebAssembly bindings, the `EncodeReplacer` callback, library-only behavior no CLI path reaches, shell completions / tracing / build metadata, native Windows

## Discrepancies

ACCEPTED on 2026-09-20 by the owner's delegation, one line each (id, class, kill-switch or mitigation, cases affected): DISC-001 Platform, runtime flags before `--`, launcher, 0 · DISC-002 Platform, the literal program name, none, 0 · DISC-003 Platform, an unwritable stderr, launcher repairs closed and read-only, 0 · DISC-004 Platform, non-UTF-8 argv, none, 0 · DISC-005 Platform, ANSI styling, none, 0 · DISC-006 Platform, a failed stdout write, launcher repairs closed and read-only, 0 · DISC-007 Platform, closed descriptors on the bare native binary, launcher, 0 · DISC-008 Platform, `-o` mode 0644, none, 0 · DISC-010 Performance, deep nesting, none, 0 · DISC-011 Performance, the runtime's resource floor, the JavaScript build, 0 · DISC-013 Performance, non-integer numbers 45 to 306 times slower (linear), none, 0 · DISC-014 Platform, a `/dev/fd/N` path the caller did not open reaches a descriptor of the runtime, none, 0 · DISC-015 Platform, the sizes of the reads on stdin (packet pipes, SEQPACKET above 32 bytes, O_DIRECT files convert where the original fails), none, 0. RESOLVED by repairs: DISC-009, DISC-012 (the stdin effect; regression rows in `scripts/stdio-probe.py`). Approver of every entry: the repository owner through the delegation quoted above.

## Reproduce (an auditor gets the same lines)

Check out the NEWEST commit of `main`: this report, the scripts it calls and the lines it pastes belong together there. (The gate lines were produced on the tree of `c7239e2`; since then `port/` changed ONLY in `port/LAWS.bend` and `port/PROOF.bend`, neither of which the built program imports: `git diff --stat c7239e2 HEAD -- port/` lists those two files, 148 insertions and 8 deletions, which are 15 new laws with their proofs (round 13's six, round 14's seven and the author-side hunt's two) plus the law file's header. This sentence used to say the change was "in comments of `port/LAWS.bend` only" (round 15, R15-4). Commits `d80251a`, `861abf2` and `4c3cccc`, which earlier versions of this report named, do not build from the public history: `./scripts/clean-build-check.sh 4c3cccc` → FAIL. Round 11 showed that naming `c7239e2` in this recipe was wrong too: `clean-build-check.sh` itself arrived in the NEXT commit, `77a63d9`.) The oracle binary is not in the repository: `docs/PIN.toml` has its commit and sha256, PLAN §2 the build command. WITHOUT it these run: the build, `clean-build-check.sh`, `converge.sh`, `hand-mutants.py`, the claims lint (and `conform.sh`, the proof, the lints of CONTRIBUTING §2). They do NOT: `floor.sh`, `port-doctor.sh`, `stdio-probe.py`, `diff-fuzz.py`, `incumbent-bench.sh` (each says so and exits 2).

```bash
git checkout main
export BEND_NO_TELEMETRY=1 BEND_CLI='bun /tmp/bend/bend2/main.ts'      # bendlang/bend at 15ae0c8
./scripts/floor.sh goldens/cases.tsv goldens --repeat 3 -- ./oracle/toon
./scripts/port-doctor.sh --threads 8 --original ./oracle/toon -- --switch TOON_SPEC=1 --probe '["--encode","cases/inputs/hand/large_tabular_1500.json"]'
./scripts/clean-build-check.sh
./scripts/converge.sh docs/PORT_STATE.md          # exit 1 while NOT_CONVERGED: run it on its own line
python3 scripts/claims-audit.py && python3 scripts/hand-mutants.py
$BEND_CLI port/main.bend -o ./x && python3 scripts/stdio-probe.py -- ./x -- && python3 scripts/diff-fuzz.py scale --runs 16000 -- ./x --
./scripts/incumbent-bench.sh --runs 9 --pin "bend 2.0.16 @15ae0c8; toon 0.2.4 @f955c67" --original ./oracle/toon -e perf/inputs/wide_rows_1200.json --port ./x --threads 1 -- -e perf/inputs/wide_rows_1200.json
./scripts/claims-lint.sh README.md CONTRIBUTING.md docs/PORT_REPORT.md docs/PORT_STATE.md docs/PARITY_RUNBOOK.md docs/DISCREPANCIES.md docs/OPEN_QUESTIONS.md perf/*.md
```
