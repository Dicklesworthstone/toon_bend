# Port state — Toon → Bend 2

<!-- Read FIRST on every resume; rewritten at the END of every session.
     Facts only, each with the command that produced it. -->

## Where we are

| field | value |
|---|---|
| phase | 4 parity gate, re-opened by the bug-fix re-pin of 2026-09-22: the owner ordered every bug found in either program FIXED ("ANy bugs you find in EITHER toon_rust (my project) or toon_bend MUST be properly FIXED"). The original was fixed first (`toon_rust` `694d73b`), the port re-pinned to it, 486 of 1071 goldens re-captured, and the port changed to match on every lane (commit `94a9ecf`; spec Amendment A1–A3). NOT converged: rounds 6 to 30 were not clean; rounds 16 to 30 were the non-author reviews of the re-pinned code; rounds 20 to 30 were on the frozen tree under the adopted counting rule |
| tier | T2 |
| bend | `bend 2.0.16` (checkout `15ae0c8`, run as `bun /tmp/bend/bend2/main.ts`); drift vs the previous pin: none, same pin since Phase 0 (VERSION-DRIFT) |
| original | `toon_rust` `694d73b8ffb160e211ea672cab805099270e1ef1` (`toon 0.2.4`), `oracle/toon` sha256 `821287eaf1a6d320ee1ef7604e74e3cefaa42b69445c96abb9dd310782d04030`; the builds of `7c1d6e4` and `f955c67` are kept beside it as `oracle/toon.7c1d6e4` and `oracle/toon.f955c67` |
| last updated | 2026-09-26 by Claude (Claude Code session, author of phases −1 to 5 and of the bug-fix re-pin; rounds 6 to 30 are non-author subagents; their reports are in `docs/reviews/`) |

## Last gate outputs (paste, do not paraphrase)

| gate | command | result | date |
|---|---|---|---|
| proofs | `bend port/PROOF.bend` on the tree of `ae73cae` plus the eleven escape/unescape laws of toon_bend-tqz that the next commit adds (693 laws; 9 min 10 s, serialized on the host's heavy lock) | All terms check. (unsafe 0 = 0 @unsafe + 0 template instances; bend 2.0.16) | 2026-09-26 |
| lanes | `scripts/lanes.sh goldens/cases.tsv goldens port/main.bend --threads 8` on the tree of `e19d8b5` (1127 cases; local 2026-09-26 09:12 to 2026-09-26 10:49; round 30's repairs: laws, three captured cases, harness and documents; `port/` differs from `5000b55` only in `LAWS.bend` and `PROOF.bend`, which `main.bend` does not import, and the native binary is byte-identical, sha256 `825e44de…`; run on its pre-rebase twin, whose `port/`, `cases/` and `goldens/` are identical) | {"lanes":[{"lane":"interpreter","verdict":"PASS","passed":1127,"failed":0},{"lane":"c-1t","verdict":"PASS","passed":1127,"failed":0},{"lane":"c-8t","verdict":"PASS","passed":1127,"failed":0},{"lane":"js","verdict":"PASS","passed":1127,"failed":0}],"stderr_compared":true,"timeouts_seconds":{"interpreter":60.0,"compiled":5.0,"build":600},"verdict":"PASS"} | 2026-09-26 |
| parity | `scripts/parity-board.sh docs/FEATURE_PARITY.md` | rows=33 present=27 partial=0 missing=0 excluded=6 n/a=0 no-evidence=0 verdict=DEBT | 2026-09-22 |
| floor | `scripts/floor.sh goldens/cases.tsv goldens --repeat 2 -- ./oracle/toon` | {"repeat":2,"stable":1127,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"} | 2026-09-26 |
| kill-switch | `TOON_SPEC=1 <binary> --gpu off -- ["--encode","cases/inputs/hand/large_tabular_1500.json"]  vs  env -u TOON_SPEC <binary> --gpu off -- ["--encode","cases/inputs/hand/large_tabular_1500.json"]  (binary built from port/main.bend into a temp dir)` | PASS: identical stdout, stderr and exit with and without TOON_SPEC=1 on '["--encode","cases/inputs/hand/large_tabular_1500.json"]' | 2026-09-22 |

**The four lanes PASS on this tree at 1127 cases** (`e19d8b5`, round 30's repairs, whose native binary is byte-identical to that of `5000b55` and `9c9039e`, in one `lanes.sh` run; before it `e33925e` at 1124 cases, round 29's repairs; before that `a86d9d4` at 1117 cases, round 28's repairs; before that `25d410d` at 1110 cases, round 27's repairs; before that `5000b55` at 1103 cases, round 26's repairs; before that `2a9f889` at 1093 cases, round 25's repairs; before that `82eceb3` at 1086 cases, round 24's repairs; before that `cc1ea20` at 1083 cases, round 23's repairs, the interpreter lane by its own re-run after the disk-full failures of that `lanes.sh` run; before that `9733983` at 1078 cases, round 21's repairs; before that `5856d1b` at 1077 cases, round 20's repairs: EXP-029, the round-19 repairs and EXP-031 on top of the published `2c33e64`, with EXP-032 reverted after the run on its tree FAILED two cases on the js and interpreter lanes, NE-036); the commits after it touch no `port/` code or `goldens/` file (`git diff 9c9039e HEAD -- port/` is empty). Earlier four-lane PASS runs, each on its own tree and corpus as it stood at that commit: the tree of `cbdfba6` (EXP-025 to 027, local 2026-09-23 16:28 to 18:15); the tree of `706f00e` (EXP-013 to 024; the run was made on its pre-rebase twin, whose `port/` code is byte-identical, local 13:35 to 16:02); `fbebe58` (1076 cases, EXP-012 and the round-16 repairs); `8d32b6e` (1071 cases); `5c3ac52` (1074 cases, another session's run); `5c3ac52` plus EXP-012 (1074 cases, 2026-09-22 23:27 to 2026-09-23 01:57 local).

The five rows are the table of ONE run, `BEND_CLI='bun /tmp/bend/bend2/main.ts' ./scripts/port-doctor.sh --threads 8 --original ./oracle/toon -- --switch TOON_SPEC=1 --probe '["--encode","cases/inputs/hand/large_tabular_1500.json"]'`, local 2026-09-22 10:25 to 12:10 (UTC−4), in a scratch clone (the external `reset --hard` loop on the working checkout), last line `{"proof":"All terms check.","unsafe":"0","unsafe_annotations":"0","instances":"0","unsafe_count_mode":"explicit-and-instances","bend":"bend 2.0.16","lanes":"PASS","board":"DEBT","floor":"STABLE","switch":"PASS","verdict":"GREEN"}`.

What changed after that run, and why each row still stands:

- `port/*.bend`, 2026-09-22 evening: the qgu fix (`051ec2e`), EXP-007 (`0131342`), EXP-006/008 (`3f5553a`), the zgb fix with EXP-009 (`d4be858`) and EXP-011 (`8d32b6e`) were gated together on the tree of `8d32b6e`: `All terms check.` over the 416 laws at that commit (6 min 54 s) and `scripts/lanes.sh … --threads 8` (local 18:15 to 20:16) PASS on the interpreter, c-1t, c-8t and js at the 1071 cases at that commit; no four-lane run exists of any of those changes alone. Superseded by the runs on `fbebe58` in the table above.

- `port/*.bend` outside the laws: two comment lines (`f64.bend` `json.is_plain`'s range comment, `cli.bend` a clause tag S4.319 → S1.158). The binary rebuilt from commit `94a9ecf`'s tree passes `scripts/conform.sh … --lane c-1t` 1071/1071, and 1071/1071 with `TOON_SPEC=1`.
- `port/LAWS.bend` and `port/PROOF.bend`: two laws renamed (`writer_b_escapes_del` → `writer_b_keeps_del_raw`, `writer_b_escapes_c1` → `writer_b_keeps_c1_raw`, their verdicts having been reversed by the fix) and two added (`json_plain_at_kneg5`, `json_exponent_at_kneg6`). `main.bend`'s imports never reach these files, so the lanes, floor and kill-switch rows are unaffected; the proof was RE-RUN whole: `All terms check.`, 393 laws at that commit, unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16.

Notes on these lines:

- proofs: 693 laws = 47 quantified + 351 closed unit laws + 295 closed whole-pipeline `golden_<case>` laws. `./scripts/law-coverage.sh` → `{"laws": 693, "proofs": 693, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}`. Laws whose subject the fix removed were restated on the live defs, and laws whose behavior the fix changed were replaced, each with a comment naming the old law (`docs/PROPOSED_ARCHITECTURE.md` A11).
- law admission: ONE run of all 36 hand mutants, M01 to M37, 2026-09-23 (pasted in `89bba8a`, whose inventory it ran), `python3 scripts/hand-mutants.py` → `{"laws_in_proof": 233, "laws_sha256_12": "c55f99164d90", "laws_listed_in": "<tmp>/laws-in-proof.txt", "reduced": true, "mutants": 36, "killed": 36, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (about 100 min at load 12-15). M36 fell to `num_text_json_is_show_json` and M37 to `dec_done_renders_with_the_json_printer`, the two laws added after round 16 named the EXP-007 decode half as unmutated. The digest is over the sorted names of the laws actually in the proof: a law silently missing (the working checkout is reset from outside this session) previously made a SURVIVED verdict indistinguishable from a weak law set.
- law admission, round 20: all 43 hand mutants (the 36 of the first run plus the seven from M38 on, the sites of R20-1 and R20-3), M01 to M44, in ONE run, 2026-09-24 (pasted in `c5f3b46`, whose inventory it ran), `python3 scripts/hand-mutants.py` → `{"laws_in_proof": 359, "laws_sha256_12": "8ec9282df760", "laws_listed_in": "/data/tmp/hand-mutants.q3yk981a/laws-in-proof.txt", "reduced": true, "mutants": 43, "killed": 43, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (about 1 h 50 min at load 4-9).
- law admission, round 21: the ten mutants round 21 added to the inventory (M45 to M54, the sites of R21-1 and R21-4), one run, 2026-09-24, `python3 scripts/hand-mutants.py M45 M46 M47 M48 M49 M50 M51 M52 M53 M54` → `{"laws_in_proof": 367, "laws_sha256_12": "e77be1b1ae88", "laws_listed_in": "/data/tmp/hand-mutants.lkyyd5jq/laws-in-proof.txt", "reduced": true, "mutants": 10, "killed": 10, "survived": [], "not_evidence": [], "verdict": "STRONG"}`; the 43 of the round-20 run were not re-run against the laws added since.
- law admission, all of M01 to M54 in ONE run, 2026-09-24 (pasted in `230a2cf`, whose inventory it ran), `python3 scripts/hand-mutants.py` → `{"laws_in_proof": 367, "laws_sha256_12": "e77be1b1ae88", "laws_listed_in": "/data/tmp/hand-mutants.3c9tec58/laws-in-proof.txt", "reduced": true, "mutants": 53, "killed": 53, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (3 h 33 min of proof time summed over the base and the 53 mutants; the run was paused once by SIGSTOP for another session's window, and resumed).
- law admission, round 22: `python3 scripts/hand-mutants.py M55 M56 M57 M58 M59` → `{"laws_in_proof": 371, "laws_sha256_12": "f923e244b5a6", "laws_listed_in": "/data/tmp/hand-mutants.6u2p1dnh/laws-in-proof.txt", "reduced": true, "mutants": 5, "killed": 5, "survived": [], "not_evidence": [], "verdict": "STRONG"}`; in that run M58 mutated the wrong arm of `row.lock` (a row longer than the header, killed by `golden_fx_enc_arrays_objects_01`); the inventory now names the reviewer's arm (a row shorter than the header), and `python3 scripts/hand-mutants.py M58` → `{"laws_in_proof": 371, "laws_sha256_12": "f923e244b5a6", "laws_listed_in": "/data/tmp/hand-mutants.n9t3be19/laws-in-proof.txt", "reduced": true, "mutants": 1, "killed": 1, "survived": [], "not_evidence": [], "verdict": "STRONG"}`, killed by `encode_short_row_in_header_order_is_list_form`.
- law admission, round 23: `python3 scripts/hand-mutants.py M60 M61 M62 M63 M64 M65 M66 M67 M68 M69 M70 M71 M72 M73` → `{"laws_in_proof": 384, "laws_sha256_12": "ae9a286a6d1c", "laws_listed_in": "/data/tmp/hand-mutants.k1coeqj7/laws-in-proof.txt", "reduced": true, "mutants": 14, "killed": 14, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (2026-09-25; a first run of the same set died on ENOSPC at M64 while the host's disk was full, after M60 to M63 were KILLED, and was re-run whole once the disk was freed). H1 and H2 are not in the inventory (no law can pin the length cap, see round 23).
- law admission, round 24: `python3 scripts/hand-mutants.py M74 M75 M76 M77 M78 M79 M80 M81 M82 M83 M84` → `{"laws_in_proof": 392, "laws_sha256_12": "5efd36a77949", "laws_listed_in": "/data/tmp/hand-mutants.9gvmq6hn/laws-in-proof.txt", "reduced": true, "mutants": 11, "killed": 11, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (2026-09-25).
- law admission, round 25: `python3 scripts/hand-mutants.py M85 M86 M87 M88 M89 M90 M91 M92 M93 M94 M95` → `{"laws_in_proof": 403, "laws_sha256_12": "7c4fb3e1b09b", "laws_listed_in": "/data/tmp/hand-mutants.mylwj8lf/laws-in-proof.txt", "reduced": true, "mutants": 11, "killed": 11, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (2026-09-25).
- law admission, round 26: `python3 scripts/hand-mutants.py M96 M97 M98 M99 M100 M101 M102 M103` → `{"laws_in_proof": 412, "laws_sha256_12": "3644d616d6f2", "laws_listed_in": "/data/tmp/hand-mutants.c54dyqsr/laws-in-proof.txt", "reduced": true, "mutants": 8, "killed": 8, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (2026-09-25).
- law admission, round 27: `python3 scripts/hand-mutants.py M104 M105 M106 M107 M108` → `{"laws_in_proof": 417, "laws_sha256_12": "d7212620225f", "laws_listed_in": "/data/tmp/hand-mutants.u4x2lvq3/laws-in-proof.txt", "reduced": true, "mutants": 5, "killed": 5, "survived": [], "not_evidence": [], "verdict": "STRONG"}`
- law admission, round 28: `python3 scripts/hand-mutants.py M109 M110 M111 M112 M113 M114 M115 M116 M117 M118 M119` → `{"laws_in_proof": 427, "laws_sha256_12": "ecf9592ec30b", "laws_listed_in": "/data/tmp/hand-mutants.1qofivni/laws-in-proof.txt", "reduced": true, "mutants": 11, "killed": 11, "survived": [], "not_evidence": [], "verdict": "STRONG"}`
- law admission, round 29: `python3 scripts/hand-mutants.py M120 M121 M122 M123 M124 M125` → `{"laws_in_proof": 433, "laws_sha256_12": "96129311bcf7", "laws_listed_in": "/data/tmp/hand-mutants.iqrlm89p/laws-in-proof.txt", "reduced": true, "mutants": 6, "killed": 6, "survived": [], "not_evidence": [], "verdict": "STRONG"}`
- law admission, round 30: `python3 scripts/hand-mutants.py M126 M127 M128 M129` → `{"laws_in_proof": 437, "laws_sha256_12": "9d444524fd59", "laws_listed_in": "/data/tmp/hand-mutants.auo8ih08/laws-in-proof.txt", "reduced": true, "mutants": 4, "killed": 4, "survived": [], "not_evidence": [], "verdict": "STRONG"}`
- `python3 scripts/port-lint.py port/*.bend --laws port/LAWS.bend` → `{"files": 10, "findings": 53, "errors": 0, "warnings": 0, "infos": 53, "by_rule": {"PL-02": 53}, "laws": "port/LAWS.bend", "verdict": "OK"}` (the infos are recursions over nesting depth, which the decoder now bounds by 127).
- `python3 scripts/spec-lint.py docs/EXISTING_Toon_STRUCTURE.md goldens/cases.tsv` → `spec-lint: 716 clauses, 1127 cases, 1127 cases cited, 0 finding(s)` (this line had stood at 1071 since before round 20: the cases of rounds 20 and 21 were cited by no clause, and nothing re-ran it; both are cited now, on S6.10 and S4.36).
- `python3 cases/build-cases.py --check` → `{"cases": 1127, "verdict": "OK"}`; `./scripts/cases-lint.sh goldens/cases.tsv` → `{"cases": 1127, "errors": 0, "notes": 20, "classes_missing": "", "verdict": "OK"}`.
- `./scripts/pin-check.sh docs/PIN.toml` → `pin-check: YELLOW` (only `manifest_version`, as before: `golden-capture.sh` writes no `version:` line); `original_commit` GREEN with `legacy/Toon` → `/dp/toon_rust` fast-forwarded to `694d73b`.
- what no case can express: `python3 scripts/stdio-probe.py -- <native port binary> --` → `{'rows': 27, 'same': 13, 'fixed': ['stderr is /dev/full, a conversion error'], 'new': [], 'verdict': 'PASS'}` with 13 KNOWN rows (DISC-003, 006, 007, 014, 015). The FIXED row matches now because the original no longer aborts on a failing stderr (DISC-003 amended).
- the documents' own numbers: `python3 scripts/claims-audit.py` → `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 8, "laws": 693, "cases": 1127, "disc": {"accepted": 11, "resolved": 4, "open": 0}, "verdict": "OK"}` (every count and every reference of the claim-bearing documents checked against the repository)
- `./scripts/claims-lint.sh README.md CONTRIBUTING.md docs/PORT_REPORT.md docs/PORT_STATE.md docs/PARITY_RUNBOOK.md docs/DISCREPANCIES.md docs/OPEN_QUESTIONS.md perf/*.md` → `claims-lint: 0 hit(s) in 11 file(s)`.

## What the bug-fix re-pin changed (2026-09-22)

- `toon_rust` (`/data/projects/toon_rust_bugfix`, pushed to `main` and `master`; `/dp/toon_rust` fast-forwarded): `d968de1` (write errors, `-o -`, stderr, correctly rounded JSON input, JavaScript number text, one escape style, error prefixes, BOM, `--stats`, `--indent 0`, dotfiles, help text), `a8f7e45` (no silent line loss; strict errors after spec v4 §14; lenient keeps every item; header grammar), `7184fe1` (fold check sees the first field; no leading-zero quoting), `528dd48` (nesting limit 127), `0ee264c` (negative flag values; usage name `toon`), `7c81f94` (docs), `694d73b` (repeated keys merge like path expansion; expansion within the limit). Its gates: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`.
- The port: every change on all four lanes; the spec's Amendment A1 (superseded clauses), A2 (13 new clauses) and A3 (the status of every S10 row: FIXED, or KEPT with the reason: the format's rule, the reference implementation's identical behavior, or a serde_json / clap diagnostic).
- KEPT, not bugs of `toon` (A3): serde_json's message wording and positions (S10.23–S10.27, S10.29, S10.31, S10.47, S10.52), clap's parsing diagnostics (S10.10, S10.12–S10.14), control characters written raw (S10.60, spec §7.2 and the reference agree), the folding corners the reference writes identically (S10.62, S10.63, S10.66, S10.74, S10.76), `--indent` as both units on decode (S10.32, the reference agrees), the blank-lines fixture message (S10.86, the reference agrees), the strict tab check (S10.94, the reference agrees).

## Owner decisions

- 2026-09-22: every bug in either program is FIXED, never reproduced (the order quoted above). It replaces the author's ruling of 2026-09-20 that kept C-1..C-11 bug-compatible.
- 2026-09-20 (delegation, "You decide on everything. I approve whatever you want to do."): DISC-001, 003–008, 011, 013, 014, 015 ACCEPTED with scoped contracts; DISC-009 and DISC-012 RESOLVED by repairs; DISC-002 and DISC-010 RESOLVED on 2026-09-22 by the fixes in the original. The custom effect `Stdin.open` stays. License: the original's MIT text with its rider; remote `https://github.com/Dicklesworthstone/toon_bend`, public.
- **DECIDED 2026-09-26 by the owner (`toon_bend-txp`): only a difference between the port and the original decides convergence.** The owner's words, verbatim, in reply to the reality check that recommended option 4 of the entry below ("count only real port differences toward approval, and track checker and mutation findings separately as work that must be fixed"), together with the database repair and the disk cleanup: "I approve it all". What it means, as implemented in `scripts/review_report.py` (`PORT_RULE_ROUND = 32`): from round 32 a finding is classed PORT (a stdout, stderr or exit-code difference from the pinned original on some lane, or a wrong proof verdict), HARNESS (a gate that misreads), CORPUS (a hand mutant that survives the corpus and the proof), LAW-COVERAGE or DOCUMENT; only a HIGH or MEDIUM PORT finding makes a round dirty; every other finding is still recorded and must still be repaired (the rounds table's `fixed` column, which `converge.sh` checks), and the class BEHAVIOR, which named both kinds at once, is refused. Applied FROM round 32, the first round briefed under it, and NOT to rounds 20 to 31: re-counting them would turn the verdict CONVERGED on the day of the decision, which is the appearance both agents refused to create; convergence therefore needs two new clean rounds (32 and 33) under the T2 rule. The owner may extend it to earlier rounds; that is a one-line change and would be recorded here. The database repair and the disk cleanup need the exact commands confirmed before they run (RULE 1), and are not done yet.
- **DECIDED 2026-09-26 (the entry above; this was PENDING from 2026-09-25), raised by the author: "converged" is unreachable a second time, one level up from R15-1, and the fix is architectural rather than a ruling.** The facts first. Rounds 20 to 30 found 2, 2, 2, 3, 2, 2, 2, 2, 2, 2 and 2 new MEDIUM behavior findings — a FLAT rate over eleven rounds — and **every one of those rounds found ZERO differences between the port and the original**, on a `port/` tree frozen throughout; rounds 27 and 28 have each reported the same two-and-zero, so they are named here and left out of that list until their rows are filed. **A plant of the author's is part of this evidence and held only one edge of its target**, which is worth recording beside the argument: `M36_mutant_count_overstated` pins the mutant-selection exemption against OVERSTATEMENT, and round 28 found the same exemption excusing PROSE totals ("N mutants in all" beside two ids) rather than only a pasted `"mutants": N`. An exemption has more than one dimension — the direction of the error and the set of counts it covers — and a plant that holds one edge does not hold the others. T2 needs two consecutive rounds at zero. At a flat rate of two, patching does not get there, which is exactly the shape the owner ruled on for mutant space on 2026-09-23 ("mutant space is unbounded … T2 is not reachable by patching"), now recurring against the GATES instead of the laws. The sharper fact: the first finding of each of the last four rounds is the SAME FAMILY in the SAME file — R24-1 (`review_report.py` read Markdown as plain lines: a table in an HTML comment, a fence or an indented block), R25-1 (it did not read a table in a blockquote or a list item), R26-1 (it folded NFKC over the whole report BEFORE parsing, so fullwidth `｜` and `｀` became a table and a fence) and R27-1 (inline HTML strikethrough in a cell read as plain text while `~~x~~` is dropped). Round 28 then made it FIVE consecutive rounds, and its shape is the sharpest illustration yet: R27-1's refusal covered the findings BODY, so R28-1 moved one row up and put `<del>sev</del> | sev` in the HEADER, choosing which column the gate read. **Round 29 made it six, and then changed the KIND of fix, which is the most important development this entry has to report.** Instead of matching one more encoding, the round-29 repair makes the header a fixed CONTRACT: a table whose header mentions `sev` or `class` in any letters-only case-folded spelling is the candidate, there must be exactly one, its header must be exactly `| id | sev | class | what | spec |` — which every brief has stated and every filed report of rounds 20 to 30 already meets — and any Unicode Cf character (zero-width space, joiner, bidi control) in any table cell is an error. That is the same move as the artifact proposal above, at a smaller scale and arrived at independently: stop asking what a renderer would show and state exactly what the record must be. **It yields a falsifiable prediction, which is the right way to judge it — and the round-29 reviewer narrowed my first statement of it, correctly, so it is recorded in their narrower form.** A clean round 30 on this family would be evidence that the contract closed the HEADER surface, not the whole family: body cells still go through `_text` (struck text dropped, code kept), so a new BODY-cell shape would be the same family in a place a header contract does not reach, and a seventh instance there would count as the family rather than as the contract failing. So the prediction is: if round 30 finds no HEADER instance, the contract bounds that surface and the remaining question is whether the body surface generates its own sequence. If round 30 finds a seventh in the header, a contract does not bound even that, and the artifact is the remaining move. **The reviewer then closed the body half too, and it lands with round 29's push rather than being in this tree yet, so it is recorded as announced and NOT cited by hash — the hash they first gave was typed before the commit existed and will change again on their rebase.** The change compares the header and the three counted cells as SOURCE text, interpreting no markup in them: `id` a bare `R<n>-<k>`, `sev` exactly `HIGH`, `MEDIUM` or `LOW`, `class` starting with a bare class word followed by end-of-cell or a space, with its prose note still free. All 45 sev cells and every class cell of rounds 20 to 29, the reports filed when this was written, already meet it and every report re-parses with unchanged counts. That makes round 30's prediction cover BOTH surfaces of the report, and it names what is left: the rounds-table row of this file, which `converge.sh` reads, is then the only free-text surface any count depends on. **ROUND 30 ANSWERED IT, and the answer is split, so it is recorded exactly as the reviewer asked.** Both contracts HELD: the reviewer found nothing on the header surface and nothing on the counted cells. And the family produced a SEVENTH instance anyway, on a THIRD surface that neither contract claimed — the completeness check "every id named has a row", which read ids from RENDERED inline text only, so an id inside an HTML block or comment, broken by U+200B or a soft hyphen, or spelled with a Cyrillic or Greek R, was never "named" while the page showed it. The repair reads the whole SOURCE outside fenced and indented code blocks with every Cf character removed, so a hidden id is FOUND and can only demand more rows, and an id-shaped `<letter><round>-<k>` whose letter is not ASCII is an error. Code blocks stay excluded as QUOTATION with that stated as a declared limit of the round-31 brief, because the brief asks reviewers to show their decoys and round 30's own report quotes `R30-99` decoys in a fence. **The part that bears on the artifact proposal above is not comfortable and belongs here rather than in a footnote: the artifact does NOT escape this, because its prose cross-check — every id in the artifact present in the prose and every `R<n>-<k>` in the prose present in the artifact — is a substring search over the same human-written text, and inherits the same homoglyph and zero-width problem.** So the artifact would bound the TABLE surface and would need exactly round 30's source normalisation for its prose side. The claim that it "retires the family" was too strong; what it retires is the rendering of the findings table. The family's real shape is now visible after seven instances: it is not about tables, it is about ANY surface where a gate reads a human-written document, and each surface needs its own contract. What no contract can do is stated by the reader itself and is not in dispute: no parser tells a complete, well-formed report written by the table's own author from a real one. Each was found immediately after the previous one was repaired, because the family's space — ways a human-written Markdown cell can render differently from how a parser reads it — is effectively unbounded: Unicode folding, inline HTML, block structure, entity references, zero-width and bidi controls. **The architectural fix retires the family instead of patching its next encoding:** let a review emit its findings as a machine-readable artifact (one JSON object per finding, or a TSV) as THE record the gates read, keep the prose report for humans, and reduce the gate's check on the prose to one that needs no rendering semantics at all — every id in the artifact appears in the prose text and every `R<n>-<k>` in the prose appears in the artifact, by plain substring search on the raw bytes. A JSON array cannot carry a fullwidth decoy, an HTML strike, a hidden fence, a second table or a blockquote, so the question "how would a browser render this cell" stops being one any gate must answer. It is strictly STRONGER than today's reader, not weaker, and it does not touch the counting rule or the tier: convergence would still need two clean rounds. **Tested rather than argued, against the seven reports filed when this was written, rounds 20 to 26:** the artifact is DERIVABLE from each of them mechanically, a reader of the artifact reproduces the current reader's counted total EXACTLY on every one (2, 2, 2, 3, 2, 2, 2, the rounds table's own numbers), and the rendering-free residual check — every id in the artifact present in the prose and every `R<n>-<k>` in the prose present in the artifact, by substring search on raw bytes — passes both directions on all seven. So this is a projection of what the reader already computes into a form that rendering cannot forge, and the transition costs nothing for rounds already filed. **What that test does NOT show, stated because the temptation is to claim it:** those artifacts were generated FROM the prose, so the exercise does not re-verify the past parses; it shows the schema is sufficient and the cross-check holds. From round 28 on a reviewer would AUTHOR the artifact, which is where the strength comes from. **The risk I am obliged to name: proposing a change to how convergence is determined, immediately after a run of rounds that keep blocking convergence, IS the gate-self-weakening shape** even when the argument is sound. Two mitigations, both the project's own: the new reader must be demonstrably stronger, with M23, M25, M31 and round 27's decoys all re-planted against it and still caught, plus a new plant for an artifact that disagrees with its prose; and the decision is the owner's, with today's verdicts left on the record either way. What this does NOT claim: that clean rounds follow. A reviewer can still find gate holes elsewhere — the author found three today in the rounds table, the DISC register and the counts table — but this family has produced four of the last eight MEDIUM findings, and it is the only one of ITS kind that regenerates itself after every repair.
  **There is a SECOND flat family, raised by the round-27 reviewer, and both belong in front of the owner together or the picture is half true.** Under the adopted rule a hand mutant that survives the whole corpus is a BEHAVIOR finding (a corpus gap), and the space of semantic mutants is unbounded exactly as renderer tricks are: a reviewer writes new ones in new arms, a 1103-case corpus cannot cover every arm of every def, some survive, the survivors are pinned with cases and laws, and the next round writes new ones one arm over. Verified from the reports rather than taken on report: the MEDIUM corpus-gap finding appears as R20-1, R21-1, R23-4, R24-3, R25-3, R26-3 and R27-3, i.e. in every round from 23 on, and the number of NEW surviving mutants per round is 4, 3, 7, 10 and 7 — RISING, not falling. Beside it a LOW LAW-COVERAGE finding appears in EVERY round (R20-3, R21-4, R22-4, R23-5, R24-4, R25-4, R26-4): new mutants the corpus kills but no law does. That one is uncounted under the rule, and it is evidence that the same generator never empties. **So the two counted findings of a typical round are one from each family, and neither is about the port**, which has had no difference from the original since round 16. Convergence under the rule as written therefore needs a round in which a reviewer writes fresh mutants and NONE survives the corpus — a statement about corpus density, which may never be true. The reviewer states plainly that they are NOT changing that rule, for the reason that applies to me as well: loosening a counting rule while it blocks convergence is the self-weakening shape. A bounded version the owner MAY choose, offered by them as an option and endorsed by neither of us: a surviving mutant counts as BEHAVIOR only in a def that `scripts/hand-mutants.py`'s inventory does not already cover with one lawful mutant per arm, which makes that inventory the finite target. One addition to the artifact contract above, also theirs: the artifact carries the reviewed commit, so that stops being a table cell too. **Blocks:** the meaning of "converged", and whether rounds 28 and beyond keep re-finding either family. **Owner:** the repository owner.
- NOT decided: the deletion of `./-` and `docs/AGENTS.scaffold.md` (RULE 1 needs the owner's exact command; bead `toon_bend-msb`).
- DECIDED 2026-09-23 (was PENDING since 2026-09-22): whether a hand-written mutant that survives only because a behavior is golden-tested rather than law-pinned counts as a genuine finding for convergence (R15-1). The owner's word, 2026-09-23, decided under `toon_bend-0fl` (the author's options (a) strict + freeze, (b) classed counting, (c) both, recommendation (c)): "you decide". The author decided **(c)**:
  - **Counting, from round 20 on: the porting skill's recommended counting rule, adopted as written** (porting-to-bend2 `references/PARITY-GATE.md`, "The recommended counting rule", rules 1 to 6). A round is dirty only for a NEW behavior finding of MEDIUM or above (a byte, stderr or exit difference from the pinned original, a wrong proof verdict, a lying gate), or a law finding on a row the board labels `proved`. A surviving hand mutant that the corpus CATCHES is a law-coverage item; one the corpus MISSES too is a behavior finding, a corpus gap, repaired by a case captured from the oracle (so the MEDIUM survivors of rounds 17, 18 and 19 would still have made those rounds dirty). Document findings keep their own column. A hit outside a committed inventory (`scripts/hand-mutants.py`'s mutant sites) is ONE finding against the inventory's generator. The author first wrote a looser rule (every survivor a non-resetting coverage finding) and withdrew it the same hour: a rule looser than the method's own recommendation, chosen to make the gate reachable, would weaken the gate. Rows 6 to 19 stay as recorded and are not re-scored: they reviewed code that has changed since.
  - **Code freeze:** from the push that carries EXP-031/032, only repairs of review findings land in `port/` until two consecutive non-author rounds on the frozen tree are clean; performance levers wait on a branch, and landing one re-opens the count.

## Open items

| id | what | blocks | owner |
|---|---|---|---|
| rounds | T2 needs the last two rounds clean; rounds 6 to 30 found 6, 11, 8, 3, 10, 12, 13, 9, 15, 9, 5, 3, 5, 5, 2, 2, 2, 3, 2, 2, 2, 2, 2, 2 and 2 (rounds 20 to 30 counted under the adopted rule: new behavior findings of MEDIUM or above). Round 16 (`toon_bend-uoq`, closed) reviewed the re-pinned decoder, number paths and JSON writer and found one HIGH (R16-1, fixed in `5c3ac52`) and two mutants that survived corpus and proof; R16-2 (`D.dup.keep` keeping the LAST repeated-key failure) is fixed by the quantified law `dup_keep_is_first` with the cases toonerr_dup_first_of_two_wins{,_expand}, and the round's report is filed as `docs/reviews/round-16.md`; it was not clean, so T2 still needs two clean rounds in a row | convergence, SHIP | a non-author reviewer for round 17 |
| 1T→8T scaling (`toon_bend-6ac`) | OPEN 2026-09-26, and the first step is ANSWERED in the negative: the evidence that exists cannot be reported, because its own ledger says it is not a claim. `perf/evidence/QGU.scaling.json` and `ZGB.scaling.json` are INPUT-SIZE scaling, not thread scaling, and each says "growth WITHIN an arm is the claim, not the ratio between arms" (QGU adds that the cv-gated capture of that ratio was REFUSED). The only 1T-against-8T figure in the repository is `perf/NEGATIVE-EVIDENCE.md`'s parallel-pre-pass entry — `flights_200k --encode`, 9 interleaved pairs, 1 thread min 4.277 s against 8 threads min 3.025 s, 1.414× by min and 1.443× by median — and that entry states "The cv was 15.5% and 35.3%, so it is NOT cv-gated and does not change the verdict above (ratios NO_EVIDENCE)". EXP-007's 1.4× and 3.6× are the same orientation figures, and the 3.6× is a throughput ceiling across eight PROCESSES, not the port's own thread scaling. So PLAN §5's criterion needs one cv-gated interleaved capture of 1T against 8T on the current binary (`sha256 825e44de69c3a4e6c0d442ea`). **It is cheaper than the five refused INCUMBENT captures and needs no preparation**: a same-binary comparison has no short arm — both are seconds long — where those five needed new inputs because the ORIGINAL's arm was 3–6 ms. Predicate: load below 1, several GB free, no review round executing. The informative part to report with it is EXP-007's ceiling, the single shared heap (`span_fade`/`term_drop`), which is why eight separate processes reach 3.6× where one process at 8 threads reaches 1.4× | README's Performance section meeting PLAN §5 | author |
| perf | every MEASURED capture ran against `f955c67` or `7c1d6e4` and against port builds before the fix (the JSON number reader and printer changed). The FIRST capture with both arms on the current pins was made 2026-09-25 and was REFUSED_CV, so it claims nothing: `perf/evidence/INCUMBENT.decimals-300000.json`, the port rebuilt from `port/main.bend` at HEAD to `sha256 825e44de69c3a4e6c0d442ea` (byte-identical to the frozen binary, same output basename) against `oracle/toon` at `694d73b`, both stdout sha256 identical, the port's arm 1705.84 ms at cv 3.82% and the original's 118.59 ms at cv 14.94% under load 5.42. Retry predicate, for all four of the input-bound refusals at once: the same four commands at load below 1, with `perf/inputs/{decimals1_300000,ints_300000,sci_80000,strings_300000}.json` (committed 2026-09-25, sized so the original's arm clears the 100 ms predicate); the tabular capture is the precedent that a ~115 ms original arm gates on a quiet host | any speed sentence for this code | author, `scripts/incumbent-bench.sh --pin` against a build of `694d73b` (`toon_bend-pao`, closed) |
| NE-001..032 | PROVISIONAL: NE-001, 002, 003, 005, 010 (EXP-006), 011 (EXP-008), 013 (EXP-009), 015 (EXP-011), 016 (EXP-012), 018 (EXP-013), each with an orientation above its gate on a host at load 10-13 but no cv-gated capture; COUNTED_WIN (cachegrind instruction counts, deterministic, a separate claim class that meets no gate written in time): NE-022 (EXP-018), 023 (EXP-019), 024 (EXP-021), 025 (EXP-022), 028 (EXP-024), 029 (EXP-025), 030 (EXP-026), 031 (EXP-027), 032 (EXP-029); NEUTRAL and parked: NE-017 (EXP-014), 021 (EXP-015); MEASURED_LOSS NE-006; NO_EVIDENCE NE-004, NE-007; NEUTRAL and reverted NE-014 (EXP-010); NEGATIVE NE-012 (a borrow that the compiler does not infer); EXP-007's entry is NE-008 | a WIN claim | author, on a quiet host (load below 1) (`perf/NEGATIVE-EVIDENCE.md`) |
| hot types | `String` and `Json` are refcounted everywhere; borrow inference lends 2 of 496 segments, and `term_drop`/`span_fade` are 35-45% of a large decode. Since 2026-09-23 the mechanism is counted per site: every list cell built on top of another passes through `rfc_wrap` and is later dropped, and the allocation profile in `perf/EXPERIMENTS.md` (above EXP-018) attributes each wrap to its def; the remaining cells per byte on decode are the scanner's line copies (the text is reversed, then each line copied reversed and reversed back), the JSON writer's final reversal and the reader's string accumulation | the next large speed step | bead `toon_bend-0uw` |
| `./-` and `docs/AGENTS.scaffold.md` | two stray files from Phase 0/1 | nothing; deletion needs the owner's exact command (RULE 1) | the repository owner |
| round 15's row | RECONCILED 2026-09-24: the row recorded 9 findings and 8 fixed; the ninth, R15-1, was the pending owner decision on how a surviving mutant counts, resolved on 2026-09-23 ("Owner decisions"), so the row now says 9 fixed. `scripts/converge.sh` has checked this since `887a9a7` (R21-2's second check) | nothing; `converge.sh` reports no unfixed round | the author of the rounds table |
| EXP-033 | carded AND **WITHDRAWN** 2026-09-24, never built, `port/` never touched (`perf/NEGATIVE-EVIDENCE.md` NE-037). Two findings from reading four defs: `x.json` is a two-arm match on one constructor, not the walk the card assumed, so fusing it removes one dispatch per object rather than a traversal; and dropping `x.norm(map)` is unsound because `xo.set` stores `XObj` values and `lk.of` re-opens an `XLeaf{JObj}` into one, so a finished object's map legitimately holds objects still under construction. Two measurement errors were corrected with it: expansion's own logic is 14.8% of the delta, not the 7.9% first published (a def's match arms are hoisted into their own `_K<n>` worklist entries, so the base symbol under-counts it); and the profile behind it was taken WITHOUT `--threads 1`, so the encoder's parallel number pre-pass made every count irreproducible and inflated — re-measured at one thread the expansion delta is 605,239,282 (+41.1%) and the port pays 23.7× the oracle, not 33.8×, which cross-validates against NE-027's independent 605,259,269 for the same quantity. `perf/COUNTED-PROFILE.md` now names NO next lever for path expansion, which is the honest reading | nothing | `toon_bend-hhb`, closed 2026-09-24 |

| rounds-row prose | REPAIRED 2026-09-25 in the one shared reader and both gates, each shape falsified in a scratch clone: `review_report.check()` now takes the row's prose, and a finding id of the round that the ROW names must be a row of the report's findings table (the false clean below is refused: the gate says the rounds row names a finding its report's findings table does not list), in BOTH gates because both call it; `converge.sh` now refuses a report on disk with no row (a planted report for a round with no row is named, as claims-audit already did); `claims-audit.py` now refuses a non-numeric round cell. One correction to the entry below: `converge.sh` ALREADY refused a `24b` row (`round number must be a positive integer`, re-run 2026-09-25); only the audit was silent. Not enforced, deliberately: prose that OMITS one of the report's ids (shape b) misstates no count and no verdict. Originally OPEN, found by the author 2026-09-25 while building round 25's mutations, the same class as R24-1 (a gate that lies), MEDIUM. `scripts/review_report.py` enforces "every `R<n>-<k>` named in the REPORT has a row in that round's findings table", and the rounds row of this file is not the report: **nothing cross-checks the ids its prose names against that table.** Reproduced two ways in a scratch copy: (a) the round-24 row's prose gains `R24-5 (a MEDIUM behaviour finding recorded here only)` with `new` left at 2 — both gates silent; (b) the prose drops R24-1 while the report keeps its row — both gates silent. It reaches a false clean: with a legal report whose findings table has zero rows, the round-24 row set to `new` 0, `fixed` 0, `clean` yes and its prose still naming R24-1 as MEDIUM BEHAVIOR, `scripts/converge.sh` counted the round CLEAN (`clean` 6, was 5; `clean_tail` 1) and only the two-clean-**tail** requirement still blocked, so one more such row would print CONVERGED. `scripts/claims-audit.py` objected only that numbers pasted in `docs/PORT_REPORT.md` had gone stale (`clean=5` vs 6, then `clean_tail=0` vs 1); refreshing those by hand moved the objection to the next stale paste and never reached the prose, so the property is checked by nothing. The repair must land in BOTH callers of the shared reader, not one: R21-2 was reopened as R22-2 for exactly that reason. A SECOND, lesser gap in the same family, found the same way and repaired together: a report on disk whose round has no row in the rounds table at all. `scripts/claims-audit.py` DOES refuse it, naming the report file and saying the rounds table has no row for it, and `scripts/converge.sh` does not — it iterates over rows, so it computed `rounds 24, clean 5, clean tail 0` with a planted report of two MEDIUM BEHAVIOR findings for the next round sitting beside it. The suite therefore covers the property and the one gate whose whole job is the convergence verdict does not, so omitting a dirty round's row keeps the clean tail intact for anyone running that gate alone, as `docs/PARITY_RUNBOOK.md` line 28 does. A THIRD, of the same shape: a rounds-table row whose round cell is not a bare number (`24b`) is silently ignored by BOTH gates, so a row a reader sees as "2 findings, not clean" needs no report, contributes no finding and cannot make the tail dirty. Refusing it is safe — the real rounds table has 26 lines and not one non-numeric round cell — so the repair is to add it to `converge.sh`'s malformed list rather than to skip it. Two shapes in the same sweep were already covered and are recorded so they are not re-hunted: a SECOND row for the same round is caught by `converge.sh` ("duplicate round 24"), and a report whose ids are named in prose only is caught inside the report by `scripts/review_report.py` | nothing now; a planted mutation in `scripts/harness-selftest.sh` would keep it that way | a4, who owns `scripts/harness-selftest.sh` |
| orphaned harness scripts | 2026-09-26: an audit for harness code nothing invokes — the "finished but unwired" case — found three of 44 scripts referenced by nothing at all: `scripts/arch-lint.py`, `scripts/bisect-laws.py`, `scripts/merge-deltas.py`. **`arch-lint.py` was RED on its first run of this phase and is now green and wired in**: it found thirteen spec clauses implemented and TAGGED in `port/` with no row in the module map (`S4.230`, `S4.231`, `S9.148`–`S9.155`, `S1.156`–`S1.158`), so the code had not drifted but `docs/PROPOSED_ARCHITECTURE.md` had, silently, for all of Phase 4/5. Three module-map rows now name those defs, `clauses 554 homed 554 verdict PASS`, and AGENTS.md's gate table carries the command so it cannot go unrun again. `bisect-laws.py` finds WHICH law makes `PROOF.bend` slow or hang, which is exactly the problem round 26 hit when a candidate law's reduced proof sat at 22 GB for 20 minutes; it is named in AGENTS.md beside that trap now. `merge-deltas.py` (623 lines) compiles `references/DELTAS.md` deltas into register rows, and **that file does not exist in this port** — it belongs to a workflow this port never adopted, so it is scaffolding, and removing it needs the owner's exact command under RULE 1 | nothing for the first two; the third is an owner decision | author; `merge-deltas.py` the repository owner |
| plants vs the round-29 header contract | OPEN 2026-09-26, author's, to settle the moment round 29 is pushed. An exact-header contract changes which rule fires FIRST for several plants of `scripts/harness-selftest.sh`, and a plant that is caught by a different rule than the one it names proves nothing about its own target — the M31 lesson, where a plant reported LEAK while the gate was red for another reason. The reviewer flagged one case (a struck HEADER `~~sev~~ \| sev` is now an error, where it used to be a control); the same logic reaches further. **`M38_duplicate_sev_column` was the one at risk, and the reviewer confirmed it: the duplicate-column rule NO LONGER EXISTS** — the round-29 rewrite removed it because the exact-header contract subsumes it, and the phrase appears nowhere in `review_report.py` at their commit, so M38 as written would have LEAKED. Their call, which I take: keep the plant and re-point it rather than dropping it, because a duplicated `sev` header is still a distinct lie shape (a reader sees two columns, the gate picks one) and the contract is now the only rule it can reach. Their shape test gives the firing behaviour per header, which is what makes each plant assertable on its own rule: `<del>sev</del> \| sev` fires the inline-HTML error AND the contract error, so M37's HTML phrase stays reachable and passes for its own rule; `sev \| sev`, `Severity:`, a struck `~~sev~~` header and a reordered `class \| sev` fire ONLY the contract error; and a Cf character fires both its own error and the contract error in a HEADER, so the Cf plant belongs in a BODY cell with an exact header, where it fires "an invisible format character (Unicode Cf)" beside the sev-vocabulary error and no contract error. `M23`, `M25` and `M37` are expected to stay caught but possibly by the contract rather than by the rule each was written for, which is worth knowing now rather than at the next leak. The check to run after the push is per plant: read the gate's WHOLE output in the retained scratch tree and confirm the message names the rule the plant targets, then re-point the assertion or re-shape the plant so each trips only its own rule — which is what the reviewer and I did for M37 and M38 in the first place. **One item on that list is a comment of mine that becomes FALSE with the push and is deliberately left alone until then**: `M34`'s comment records `~~LOW~~ MEDIUM` in a sev cell as a control that correctly reads MEDIUM, and under the source-text contract it is an error ("the sev cell … is not exactly HIGH, MEDIUM or LOW"). Editing it now would make it false today to be true tomorrow, which is the wrong order; it is corrected when the push lands, with the rest of this list. A backticked id also becomes an error then, so any plant relying on one is on the list too | nothing; the suite is green today and the risk arrives with the push | author |
| law-mutation sweep | UNBLOCKED 2026-09-25 and now producing evidence, after four defects that each hid the next. `scripts/law-mutation.sh` had reported INCONCLUSIVE "no valid textual site" on every module it was pointed at, and the reason was the harness rather than the laws. (1) Its operators were arithmetic swaps while this port's twenty-plus corpus gaps were verdict flips, guard weakenings and character-class boundaries; six evidence-derived operators were added, and probed across `port/*.bend` they find sites in 117 defs (`true2false`), 180 (`false2true`), 55 (`conjdrop`), 55 (`and2or`), 30 (`or2and`) and 602 (`litsucc`). (2) The default cap was 90 s: a mutant the laws CATCH is refuted fast, but a SURVIVOR runs the whole proof (7 min 52 s over 662 laws at that commit), so every survivor — the only outcome that is a finding — came back TIMEOUT. Default now 900 s. (3) A timeout was reported as a red baseline, which sends a reader after a broken law when a flag is wrong; the two are now separated and the remedy named. (4) **A MODULE cannot be compiled to C at all**, having no entry point, so a clean module type-checks and then says exactly `Error: no main to run` with exit 1 — a passed check with nothing to execute — and the script counted it as "unclassified compile failure". Every mutant outside `main.bend` therefore came back ERROR in under a second. Defects 2 and 4 both fail in the REASSURING direction: one turns a finding into a timeout, the other into a compile error, and neither ever says the laws are weak. Same def and mutants before and after the fourth fix: `errors=4 valid=0 INCONCLUSIVE` became `true2false KILLED LAWS.expand_depth_127_accept 58.37s`, `false2true KILLED LAWS.expand_segments_253_not_ok 70.08s`, `valid=2 killed=2 survived=0 STRONG`. **The sweep is a costed plan, regenerable rather than pasted:** the target set is the defs a law NAMES intersected with those carrying a verdict-flip or guard site — 111 named, 38 with a site, 48 mutants — and it is grouped by module because a def can only be mutated in the file that holds it and each invocation pays its own baseline, so six batches (`f64` 12 defs, `cli` 9, `text` 7, `json` 6, `decode` 2, `encode` 2) pay that baseline six times instead of 38. A SURVIVED in that set is a CANDIDATE law-weakness finding: it is a finding unless the mutant is shown equivalent by reading the def, and a corpus pass alone does not show that (passing 1124/1124 is consistent with an equivalent mutant and with a corpus gap alike). Round 30 (R30-6) ran text, json, decode and encode; its three survivors, `write` and `write_ln` true2false and `fold` conjdrop, were shown equivalent from their defs; outside the set a survivor is only a corpus observation | nothing; the first batch is evidence and the remaining five are hours of proof runs on a quiet host | author |
| `br` read-only opens | OPEN 2026-09-25, needs the owner: every `br` read-only command (`br ready`, `br list`) refuses with `Database error: database is busy (recovery in progress)`, so the tracker cannot be read or marked. **Nothing is lost and the database is healthy**: `br doctor --json` reports `sqlite.integrity_check` ok, `jsonl.parse` 93 records, `counts.db_vs_jsonl` "Both have 93 records", `sync.metadata` "Database and JSONL are in sync" with `dirty_issues` 0, and a rollback-only WRITE probe SUCCEEDED. Exactly two checks error, `sync.merge_pending` and `db.read_only_open_observational`, both with that one message — so it is the READ-ONLY open path that refuses, not the data. `.beads/issues.jsonl` carries all 93 records, 12 not closed and 59 comments, including the round-23 comment on `toon_bend-xs1`. Earlier in this session I blamed the disk-full outage; that was wrong and is withdrawn — the lock files are dated 2026-09-20 06:20, five days before it, and the WAL was written 40 minutes AFTER the disk was freed with a successful commit in between. A mutating open would likely recover the WAL, but `br doctor`'s own remediation says "Mutating commands must remain disabled until pending merge state can be inspected", and a flush that exported a DB whose WAL had not replayed could drop another agent's comment, so no mutating `br` command is run here. The decision is the owner's | marking beads; the session-protocol `br sync --flush-only` step | the repository owner |
| ACCEPTED DISC without its required lines | REPAIRED 2026-09-25, found by the author sweeping the DISC register the same way. AGENTS.md permits a deliberate divergence "only as a `DISC-` entry … with a class, a kill-switch, the affected cases, a measured impact and the owner's approval". `scripts/converge.sh` checked the heading's class and status and, for a RESOLVED entry, a real `Resolution:`; `scripts/claims-audit.py` checks the impact DENOMINATOR against the case count. **The PRESENCE of those four lines was checked nowhere.** Each was deleted on its own from `DISC-001` (ACCEPTED), so no count moved and no count check could fire, and all three of `Approver:`, `Kill-switch:` and `Affected cases:` left both gates silent with `open DISC 0` — a divergence licensed with no recorded owner approval. Repaired in `converge.sh`, scoped to ACCEPTED (the status that sentence is about and the one that licenses a live difference), rejecting a missing line and a placeholder value alike. Verified BOTH ways before landing: all fifteen entries carry all four lines with real values, so the real register gains no finding (`open DISC 0` unchanged), and each of the three plants is now named exactly ("DISC-001 is ACCEPTED without a `Approver:` line"). Planted as `M30_accepted_disc_no_approver`. Unlike R21-2 this needed ONE gate, and the reason is recorded beside the mutation rather than assumed: `converge.sh` computes the register's completeness and no second gate asserts that property by another route, since the audit's DISC checks are counts and denominators, which are different claims | nothing; repaired and planted | author |
| exclusion class vs PLAN §3 | OPEN 2026-09-25, found by the author sweeping the parity-board family, LOW and DOCUMENT-class: `scripts/parity-board.sh` requires an `excluded` row to name a class from its eight-word vocabulary, and PLAN §3 is the authoritative classed exclusion table, but **nothing compares the two**. Reproduced by changing ONE existing row's class word and nothing else, so the counts are identical and no count check can fire: the `async-stream` row was made to claim `infeasible-numeric` where PLAN §3 assigns `out-of-scope`, and the board printed `PLAN §3: infeasible-numeric …` from the row's own notes, kept `verdict=DEBT` and exited 0, with `claims-audit.py` silent. The board PRINTING a class is not the board CHECKING it — my first probe read that printed line as a catch, and only the verdict settled it. It changes no count and no verdict, which is why it is LOW; what it misstates is the JUSTIFICATION, and AGENTS.md makes the class the thing that separates an exclusion from a missing feature ("An exclusion without a class is not an exclusion"), so a WRONG class is the same defect wearing a legitimate word. **Deliberately not repaired reflexively:** only 3 of the 6 excluded rows carry a distinctive backticked key (`async-stream`, `wasm`, `EncodeReplacer`); the other three are prose (`library-only behavior no CLI path reaches (OQ-B3)`, `shell completions, tracing, build metadata`, `native Windows behavior`), so a keyed cross-check would cover half the rows and skip the rest — a check reporting a number without saying which rows it covered — and a prose matcher risks a false `MALFORMED` that blocks every commit, which is worse than the hole. The sound repair is to give each `excluded` board row and each PLAN §3 row a shared stable id, or require the notes to cite PLAN §3's row verbatim, and then compare classes on an exact key | nothing; no count or verdict depends on it | author, with a planted mutation once the rows have a shared key |
| markdown-it-py | CLOSED 2026-09-25. AGENTS.md's "Tools the gates need" table carries the row, and `scripts/review_report.py` names the missing package and its install command in the error. I proposed a GATE for the dependency as well and then refused my own proposal under the creation gate: it would fire in exactly the circumstances the existing error already fires in, and would tell the reader the same sentence, so it gates nothing that is not already gated. The failure announces itself by name; a second announcement is ceremony. Reopen only if a report ever parses WRONG rather than erroring when the parser is absent or a different version. Original entry:  R25-1 rewrites `scripts/review_report.py` onto a real Markdown parser and fails CLOSED without it, so every review report becomes an error on a host that lacks it. `markdown-it-py` 3.0.0 is installed here and is declared NOWHERE: AGENTS.md's "Tools the gates need" table lists `bun`, `clang`, `python3`, `rg`, `jq` and `cargo` only, and no gate checks for it. A reviewer on a clean clone would read a red convergence gate as a finding about the port. Needs the AGENTS.md row and a named-dependency error, once R25-1 is pushed | a third party reproducing the convergence gate | author, after R25-1 lands |
| PLAN §1's algebraic properties (`toon_bend-tqz`) | PARTLY DONE 2026-09-26, and PLAN §1 amended in place rather than left standing: the escape/unescape ROUND TRIP is proved in the form the amendment states — six QUANTIFIED laws (`escape_step_backslash|quote|lf|cr|tab|plain`: escaping a byte of a class and feeding the escaped text back through the unescaper's loop leaves it in the state the raw byte would have produced, for ANY accumulated text, ANY carried bad character and ANY continuation) plus five closed classifier laws (`escape_class_of_*`). A law quantified over the BYTE is refused by the checker (`U32.is_eq` on an abstract scalar: the EXP-016 wall, which is what closed `toon_bend-j6n`), so the plain class is stated on a representative byte and the whole-string composition stays golden-tested (`encstr_escapes_in_all`, `encstr_quoting`, `decstr_quoted`, `decstr_keys`), never called proved. Three inventory mutants were added for it (M130 the unescaper's table, M131 the escaper's, M132 the classifier), each killed by the law named in its entry. QUOTING SAFETY needs the same per-byte fact. The FOLD/EXPAND INVERSE is not an inverse pair on its whole domain at all — the format loses the difference for quoted dotted keys — so the promise as written cannot hold and the restricted domain is not yet written down | those two remain open on `toon_bend-tqz`; nothing else | author |

## Find-fix rounds (Phase 4/5)

| round | lens | new genuine findings | fixed | clean? | date |
|---|---|---|---|---|---|
| 1 | author: mutation fuzz of corpus documents against the original (9000 TOON, 4000 JSON) | 1 | 1 | yes | 2026-09-20 |
| 2 | author: argv fuzz against the original (clap emulation; 4000 then 12000 command lines) | 1 | 1 | yes | 2026-09-20 |
| 3 | author: structure-aware generated documents, every option, encode then decode (4000), and the 45 cases proposed by the pass-2 reader and by Phase 3 | 0 | 0 | yes | 2026-09-20 |
| 4 | author: lane hazards, inputs of 10^5 and 10^6 elements on C and JS against the original (25 shapes) | 1 | 1 | yes | 2026-09-20 |
| 5 | author: kill-switch parity and twin-vs-twin numbers (1005 cases under both settings; 127838 generated numbers, both directions, against the original) | 1 | 1 | yes | 2026-09-20 |
| 6 | non-author hostile review (subagent): descriptor and platform states, inputs large in one dimension, a review of every claim in the documents (non-author) | 6 | 6 | no | 2026-09-20 |
| 7 | non-author hostile review (subagent): the new key carriers, scale, numbers in both directions and under `TOON_SPEC=1`, argv and files, lane agreement (about 122000 compared executions) (non-author) | 11 | 11 | no | 2026-09-20 |
| 8 | non-author hostile review (subagent): balanced buckets and the repeated-key map, stdin and descriptors beyond the probe, signals and resources, 40 random spec clauses, a fresh-seed floor (about 90000 compared executions) (non-author) | 8 | 8 | no | 2026-09-20 |
| 9 | non-author hostile review (subagent): round trips through all 1632 option combinations, hand-made and mutated TOON, JSON error positions at every truncation, files and in-place behavior, every Unicode scalar value in 32 positions, a fresh-seed floor (947646 compared executions) (non-author) | 3 | 3 | no | 2026-09-20 |
| 10 | non-author hostile review (subagent): a fresh clone of the PUBLIC repository built and gated by the documents' own commands, an audit of every number and claim in nine documents, terminals and environments (7955 executions), threads and determinism, the interpreter lane off the corpus, a fresh-seed floor (non-author) | 10 | 10 | no | 2026-09-20 |
| 11 | non-author hostile review (subagent): a fresh clone at the new HEAD running every documented command, a claims audit of about 200 statements, `--stats` at volume, 1742 real-world JSON files, hand-written TOON with human mistakes, 24238 argv permutations, the launcher under dash, bash, busybox and in sandboxes (about 74500 compared executions) (non-author) | 12 | 12 | no | 2026-09-20 |
| 12 | non-author hostile review (subagent): a fresh clone at `83f68bb` built and gated by the documents' own commands, 4802 spec-clause predictions, the JavaScript and interpreter lanes, 160 filesystem and argv states, descriptor states beyond the probe, and a claims audit of every number in eleven documents (13298 compared executions of its own generators plus 10038 of the repository's fuzz lenses) (non-author) | 13 | 13 | no | 2026-09-20 |
| 13 | non-author hostile review (subagent): a fresh clone at `1f184e8` built on both lanes, this session's own repairs re-tested (the `opt-level=3` numbers re-captured, the three missing-oracle guards, the byte-reproducible build, ORIGIN.md against `git log`), eight NEW hand-written law mutants, nesting depth on three lanes, metamorphic round trips over the corpus, 35 environment states, and a claims audit (about 421000 compared executions) (non-author) | 9 | 9 | no | 2026-09-20 |
| 14 | non-author hostile review (subagent): a fresh clone at `6d7cfcc`, five NEW hand-written semantic mutants in defs the existing set never touches, eleven deliberate falsifications of the claim-checking gates themselves, spec-clause prediction over 207 of them, a fresh-seed `diff-fuzz`, descriptor states, the README examples and every gate the documents prescribe (11638 compared executions; 0 HIGH, 5 MEDIUM, 10 LOW; **1 difference, and it is DISC-004, whose entry the reviewer confirmed ACCURATE** — zero unregistered conversion differences) (non-author) | 15 | 15 | no | 2026-09-21 |
| 15 | non-author hostile review (subagent): a fresh clone at `37633aa`, 23 deliberate falsifications of the claim gates (the reviewer's own falsify.py), 24 NEW hand-written mutants in ten defs no mutant reached, the spec's new notation and its S1.32 amendment run against the original, the doctor table's row-by-row standing, and every number the session added (34233 compared executions; 0 HIGH, 5 MEDIUM, 4 LOW; **0 unregistered differences in conversion content**) and R15-1, the owner decision on surviving mutants, resolved on 2026-09-23 (non-author) | 9 | 9 | no | 2026-09-22 |
| 16 | non-author hostile review (subagent): a fresh clone at `b8ce8ab` plus `4902734`, the raw/defer boundary of the EXP-007 number pre-pass swept at 13416 inputs x threads 1/2/8/64 with and without `TOON_SPEC=1` (107328 executions), and mutants of the twins and the repeated-key path (1 HIGH, 2 MEDIUM, 2 LOW; the HIGH was a JSON number the port printed as 0 with exit 0 where the original exits 1) (non-author) | 5 | 5 | no | 2026-09-23 |
| 17 | non-author hostile review (subagent): a clone at `722991e` aimed at EXP-012's word loop at the 2^64 guard (about 800000 doubles modelled on its fixed state), the round-16 repairs (`num.safe` bounds, the first repeated-key failure) and the dispatch gates, seven new mutants; about 248000 compared executions on c-1t, c-8t and js with and without `TOON_SPEC=1`, 0 differences from the original (non-author) | 3 | 3 | no | 2026-09-23 |
| 18 | non-author hostile review (subagent): a fresh clone at `78a2588` aimed at the five allocation levers of 2026-09-23 (EXP-018, 019, 021, 022, 024), EXP-013 and R17-1's repair, and unpinned dispatches: about 604,000 compared executions on the native build and 2,850 on the JS lane, 0 differences from the original; ten hand mutants, five of which (in the chunk join, the delimited-value splitter, the literal's first bad escape, the key's leading FF and the header's field cut) passed the corpus and the whole 518-law proof; the row counts those five behavior findings, the two document findings are in the bullet below (non-author) | 5 | 5 | no | 2026-09-23 |
| 19 | non-author hostile review (subagent): a fresh clone at `7d6f79e` aimed at EXP-025, 026 and 027 and round 18's repairs: about 1,094,000 compared executions on the native build (32 on the interpreter), 0 differences from the original; sixteen hand mutants, four of which (the numeric-like table's phase 4 on a '0', the scanner's walk not starting at a line end, a byte order mark dropped on every line, the colon search's escape) passed the corpus and the whole 571-law proof; one document finding (evidence files citing a replaced commit) (non-author) | 5 | 5 | no | 2026-09-23 |
| 20 | non-author hostile review (subagent): a fresh clone at `55ec958` (the frozen tree: EXP-029, round 19's repairs, EXP-031) under the adopted counting rule: about 405,000 compared executions on the native build, 13,904 on the JS lane and 28 on the interpreter, 0 differences from the original; two BEHAVIOR findings of MEDIUM (R20-1: a mutant that survived the corpus AND the whole proof, the key set built without the 9th member that builds it; R20-2: claims-audit's R19-5 rule checked one evidence file of 32, a gate that lied), one LAW-COVERAGE (six mutants the corpus caught but no law did) and one DOCUMENT finding ("8th" where the code switches at the 9th) (non-author) | 2 | 2 | no | 2026-09-24 |
| 21 | non-author hostile review (subagent): a fresh clone at `c5f3b46` (the frozen tree, round 20's repairs) under the adopted counting rule: about 502,700 compared executions on the native build, 15,260 on the JS lane and 44 on the interpreter, 0 differences from the original; round 20's R20-1 repair held (about 113,000 executions aimed at it); two BEHAVIOR findings of MEDIUM (R21-1: a mutant of `has_prim.go` that survived the corpus AND the whole 604-law proof, a reordered row with a nested value written as a table with `null`; R21-2: no gate enforced the counting rule, a round marked clean with two counted findings passed `converge.sh` and claims-audit), one LOW behavior (R21-3: claims-audit's reachability rule missed other shapes), one LAW-COVERAGE (nine mutants the corpus caught but no law did) and one DOCUMENT finding (a stale mutant count) (non-author) | 2 | 2 | no | 2026-09-24 |
| 22 | non-author hostile review (subagent): a fresh clone at `2bde968` (the frozen tree, round 21's repairs) under the adopted counting rule: about 345,000 compared executions on the native build, about 17,000 on the JS lane and 28 on the interpreter, 0 differences from the original; round 21's R21-1 and R21-4 repairs held; two BEHAVIOR findings of MEDIUM (R22-1: `converge.sh` and claims-audit accepted rounds labelled non-author that have no review report, so two fabricated rows printed CONVERGED; R22-2, R21-2's audit half reopened: the counted-row test was case-sensitive and skipped a report that yields 0), one LOW behavior (R22-3: COUNTED evidence shapes the reachability rule did not read), one LAW-COVERAGE (five mutants the corpus caught but no law did) and one DOCUMENT finding (the pre-round-20 clean rule still stated) (non-author) | 2 | 2 | no | 2026-09-24 |
| 23 | non-author hostile review (subagent): a fresh clone at `57c7b36` (the frozen tree, round 22's repairs) under the adopted counting rule: about 191,000 compared executions on the native build from new generators, at least 50,000 from `diff-fuzz` with new seeds, 12,896 on the JS lane and 36 on the interpreter, 0 differences from the original; round 22's repairs held for the shapes it named; three BEHAVIOR findings of MEDIUM (R23-1: an EMPTY report file, or the lens spelled with U+2011, evaded R22-1's repair and two fabricated rows printed CONVERGED; R23-2: the counted-row reader recognised one layout and read 16 of 18 others as zero findings; R23-4: four hand mutants that survived the whole corpus AND the whole 616-law proof), one LOW behavior (R23-3: sixteen reachability shapes) and one LAW-COVERAGE (twelve mutants the corpus caught but no law did) (non-author) | 3 | 3 | no | 2026-09-24 |
| 24 | non-author hostile review (subagent): a fresh clone at `4dd7812` (the frozen tree, round 23's repairs) under the adopted counting rule: about 219,600 compared executions on the native build from new generators (key folding at flatten depths 0-5, the number printer's boundaries, `--stats` ties, `-o` targets, `--indent` 1-16 with TABs), 30,240 from `diff-fuzz` with new seeds, about 28,300 on the JS lane and 22 on the interpreter, 0 differences from the original; round 23's repairs held for every shape it named; two BEHAVIOR findings of MEDIUM (R24-1: the report reader read Markdown line by line, so a findings table hidden in an HTML comment, a fence or an indented block stood in for a visible one re-headed with `finding` and `severity` cells, and two fabricated rows printed CONVERGED; R24-3: three header-parser mutants that survived the whole corpus AND the whole 629-law proof), one LOW behavior (R24-2: five reachability shapes) and one LAW-COVERAGE (eight mutants the corpus caught but no law did) (non-author) | 2 | 2 | no | 2026-09-25 |
| 25 | non-author hostile review (subagent): a fresh clone at `9880134` (the frozen tree, round 24's repairs) under the adopted counting rule: about 90,980 compared executions on the native build from new generators (list arrays, tab and pipe delimiters, argv orderings with -o and --stats, number boundaries, deep nesting, real -o files), 30,240 from `diff-fuzz` plus 40,000 number literals with new seeds, 13,796 on the JS lane and 22 on the interpreter, 0 differences from the original; round 24's repairs held for every shape it named; two BEHAVIOR findings of MEDIUM (R25-1: the report reader, a hand-written imitation of a Markdown renderer, missed a findings table in a blockquote or under a list item and took non-tables for tables, so two fabricated rows printed CONVERGED; R25-3: seven mutants that survived the whole corpus AND the whole 637-law proof), one LOW behavior (R25-2: three reachability shapes), one LAW-COVERAGE (four mutants) and one DOCUMENT finding (S4.225's probes predated the re-pin) (non-author) | 2 | 2 | no | 2026-09-25 |
| 26 | non-author hostile review (subagent): a fresh clone at `daa102b` (the frozen tree, round 25's repairs) under the adopted counting rule: about 78,500 compared executions on the native build from new generators, 30,240 from `diff-fuzz` plus 40,000 number literals and scale runs with new seeds, about 18,700 on the JS lane and 30 on the interpreter, 0 differences from the original; round 25's repairs held for its own shapes; two BEHAVIOR findings of MEDIUM (R26-1: the report reader folded the text before parsing it, so fullwidth backticks and pipes became a fence or a table the renderer never shows and two fabricated rows printed CONVERGED; R26-3: ten mutants that survived the whole corpus AND the whole 648-law proof), one LOW behavior (R26-2: duplicate JSON keys hid a commit from the reachability rule), one LAW-COVERAGE (five mutants) and one DOCUMENT finding (README's split of the laws) (non-author) | 2 | 2 | no | 2026-09-25 |
| 27 | non-author hostile review (subagent): a fresh clone at `67506eb` (the frozen tree, round 26's repairs) under the adopted counting rule: about 53,300 compared executions on the native build from new generators, 30,240 from `diff-fuzz` plus 40,000 number literals with new seeds, about 7,470 on the JS lane and 26 on the interpreter, 0 differences from the original; round 26's repairs held for their own shapes; two BEHAVIOR findings of MEDIUM (R27-1: the report reader dropped Markdown strikethrough but read inline-HTML strikethrough, so a cell rendering MEDIUM was counted LOW; R27-3: seven mutants that survived the whole corpus AND the whole 657-law proof), one LOW behavior (R27-2: one unparseable JSONL line sent a whole evidence file to the raw fallback, where an escaped commit is only fragments) and one LAW-COVERAGE finding (five mutants) (non-author) | 2 | 2 | no | 2026-09-25 |
| 28 | non-author hostile review (subagent): a fresh clone at `6dd844d` (the frozen tree, round 27's repairs) under the adopted counting rule: about 31,100 compared executions on the native build from new generators (including 6,000 at 8 threads with `TOON_SPEC=1` and 399 `-o` runs comparing the written files), 30,040 from `diff-fuzz` plus 40,000 number literals with new seeds, about 8,100 on the JS lane and 24 on the interpreter, 0 differences from the original; two BEHAVIOR findings of MEDIUM (R28-1: round 27's HTML refusal covered only the findings body, so `<del>sev</del>` in the header chose the column that was read; R28-3: six mutants that survived the whole corpus AND the whole 662-law proof), one LOW behavior (R28-2: the mutant-selection exemption also excused prose totals) and one LAW-COVERAGE finding (nine mutants) (non-author) | 2 | 2 | no | 2026-09-26 |
| 29 | non-author hostile review (subagent): a fresh clone at `e6640bb` (the frozen tree, round 28's repairs) under the adopted counting rule: about 42,500 compared executions on the native build from new generators (11,000 of them at 8 threads with `TOON_SPEC=1`), 30,040 from `diff-fuzz` plus 80,000 number literals with new seeds, about 4,400 on the JS lane and 14 on the interpreter, 0 differences from the original; two BEHAVIOR findings of MEDIUM (R29-1: the reader refused two identical severity headers but not `Severity:` or `sev` with a zero-width space beside `sev`; R29-3: three mutants that survived the whole corpus AND the whole 672-law proof), one LOW behavior (R29-2: a pasted mutant count below the inventory passed whenever two ids appeared anywhere on its line) and one LAW-COVERAGE finding (five mutants) (non-author) | 2 | 2 | no | 2026-09-26 |
| 30 | non-author hostile review (subagent): a fresh clone at `2fabd59` (the frozen tree, round 29's repairs) under the adopted counting rule: about 37,400 compared executions on the native build at 1 thread and 17,330 at 8 threads with `TOON_SPEC=1`, 84,800 number literals and `diff-fuzz` with new seeds, about 3,070 on the JS lane and 12 on the interpreter, 0 differences from the original; the first round to run `scripts/law-mutation.sh` (text, json, decode and encode: 29 mutants, 25 killed, 3 equivalent survivors, 1 timeout); two BEHAVIOR findings of MEDIUM (R30-1: the report reader's completeness check missed ids in HTML blocks, broken by invisible characters, or spelled with a Cyrillic R; R30-4: three decoder mutants that survived the whole corpus AND the whole 678-law proof), two LOW behavior (R30-2: honest headers such as `base vs mutant` read as a second findings table; R30-3: three shapes of mutant paste the audit did not scope), one LAW-COVERAGE and one DOCUMENT finding (non-author) | 2 | 2 | no | 2026-09-26 |

What each round found (every finding was resolved by RUNNING the original, then a clause, a case and a repair; "fixed" counts findings repaired OR registered as a DISC):

- round 1: OQ-P3-2, a quoted key is unescaped before the colon after its closing quote is required (7 of 3000 mutated documents differed); S2.126 amended, `toonerr_quoted_key_escape_before_colon*`.
- round 2: OQ-P4-1, `--help=x` / `--version=x` with a non-empty committed list print the flag before the group line (11 of 4000 command lines differed); S1.86 amended, `usage_help_with_value_after_commit`, `usage_version_with_value*`.
- round 4: on the JS lane Base's `List.length` overflowed the machine stack on a 200000-field tabular header (`bend: memory fault`), while the C lane and the original answered; every `List.length`, `String.length` and `++` over input-controlled text is now a loop (`T.llen`, `T.str_len`, `T.cat`), and TOON number tokens are clamped to 800 significant digits plus a sticky digit.
- round 5: the short-integer fast twin accepted 15-digit texts, and 281474976710656 (in `encnum_ints`) is past Nat's 2^48 − 1; the run with `TOON_SPEC=1` passed and the run without it failed, which is what the switch is for; the bound is 14 digits now.
- round 6 (report `docs/reviews/round-06.md`): the native binary hung on a closed stdin (DISC-007, launcher); `-o` files get mode 0644 (DISC-008); quadratic time in the keys of one object (repaired: hashed key carriers, EXP-004); exit codes on unwritable standard streams (DISC-003, DISC-006); a non-blocking stdin (DISC-009); a spec error in S9.20. Its claims review is answered item by item in the commits `1230a0d` and `e3f5540` (law counts and wording, board citations, fuzzers in `scripts/`, ledgers, plan amendments).
- round 7 (report `docs/reviews/round-07.md`, 2 HIGH): stdin was re-opened by PATH: an inherited offset ignored (wrong bytes, exit 0) and a socket refused (DISC-012, repaired by the stdin effect; DISC-009 RESOLVED by the same repair); list buckets quadratic on keys that collide in the 16 hash bits, and a chain walk per repeated key (repaired: AA-tree buckets, a map of last values; 7 cases); the runtime's resource floor (DISC-011); a second closed-descriptor hang (DISC-007 amended, launcher); three DISC texts corrected.
- round 8 (report `docs/reviews/round-08.md`, 0 HIGH): EBADF on stdin must end the input (repaired); read-only stdout/stderr (DISC-003, DISC-006, launcher); the cost of non-integer numbers (DISC-013); two launcher defects (repaired); DISC wording for the JavaScript build; S1.81's cluster rule (amended, 5 cases); a limit of `js-lane.py` (documented).
- round 9 (report `docs/reviews/round-09.md`, 0 HIGH, 1 MEDIUM, 2 LOW; 0 differences in conversion content in 947646 compared executions): a path under `/dev/fd` whose number the caller did not open reaches a descriptor of the runtime (DISC-014, ACCEPTED); DISC-007's description of the bare binary was incomplete (completed); thirteen impact lines of the register carried an old corpus size (refreshed). Three findings is not fewer than three: the round is recorded as NOT clean. No code changed.
- round 10 (report `docs/reviews/round-10.md`; 1 HIGH, 3 MEDIUM, 6 LOW; 0 behavioral differences between port and original in any lens): the documents cited commits (`d80251a`, `861abf2`, `4c3cccc`) that do NOT build from the public history, because `.gitignore` (`port/*.c`, `port/*.js`) kept the stdin effect's two twins untracked until `a725d10` (repaired: every gate line now names a buildable commit, and `scripts/clean-build-check.sh` builds and tests an exported commit); two README examples failed as written (fixed; every example was run); the launcher needed `dirname` from `PATH` (repaired: parameter expansion); `CONTRIBUTING.md`, `.github/CODEOWNERS` and `docs/PARITY_RUNBOOK.md` were unfilled scaffold files with false statements (filled); `c-1t` and `c-8t` are one sequential execution (now said wherever the lanes are named); stale counts in README, PORT_REPORT and this file; `perf/PERF-LEDGER.md` rows that its own admission rules refuse (moved to NE-005, provisional); AGENTS.md's build time and script provenance; the law file's header and the board's legend; a traceback of `pin-check.sh` in a clone without the oracle (now a RED row).
- round 13 (report `docs/reviews/round-13.md`; 0 HIGH, 1 MEDIUM, 8 LOW; **0 differences in conversion content in about 421000 compared executions** on the native, JavaScript, interpreter and launcher lanes): the MEDIUM is against this session's own repair — "1.389× the pinned build" was generalized into "flatters the port by about 1.4× on this workload", and the profile's advantage is input-dependent (re-measured here: 1.729× on wide rows, cv 2.0% / 4.2%, 42 samples; 1.389× on expand; REFUSED on the tabular encode), while the two README columns were captured hours apart with DIFFERENT port binaries and are not an interleaved pair (two attempts to re-capture the pinned side with the current binary were REFUSED_CV, cv 10.5% / 13.2%; bead `toon_bend-0i8`). The eight LOW: `scripts/ORIGIN.md` named seven edited scripts where ten differ from the skill's copies (rewritten from `git log`, and it now says so); three stale document numbers (the self-test line, `10 file(s)` for the claims lint, DISC-013's range in two more places); `claims-audit.py` checked only numbers WITH a decimal point and exempted any line naming `perf/evidence/`, so a row falsified to `9999 ms | 111 ms` passed (integers are checked now, the exemption is gone, and the reviewer's own falsification is caught: 3 of 3 values); `floor.sh` accepted the oracle under another basename and then reported 62 false UNSTABLE `usage_*` cases, because clap prints argv[0]'s basename (it refuses by name now, exit 2); NE-006's tally contradicted its own list; and — the one that mattered — **three of the reviewer's eight new law mutants SURVIVED the whole 368-law proof**: no law pinned writer B's escape table (S5.64), UTF-8 surrogate rejection (S2.2) or the TAB quoting rule (S4.12–S4.16). Six closed laws now pin them, the three mutants are permanent members of the set (M24, M25, M26) and each is KILLED by the law written for it. The port itself needed no change in any of the nine.
- round 12 (report `docs/reviews/round-12.md`; 0 HIGH, 2 MEDIUM, 11 LOW; 0 differences in conversion content in 13298 compared executions on four lanes): the launcher's round-11 repair spent the CALLER's descriptor 9, so `-e /dev/fd/9 9<file` failed where the original converts (repaired in `f45de76`: the stderr probe no longer opens 9; re-verified this session on both halves of the reviewer's repro); DISC-015's Resolution described the original wrongly in two of its own sub-cases (corrected in `713adfe`); and eleven statements in the documents that the repository itself refutes — the folding capture's refusal count, a REFUSED_CV count that no reading of `perf/evidence/` gives, "two commits after" for a commit that is the next one, three of five scripts that did NOT refuse a missing oracle although the report said each does, NE-003's cv range, CONTRIBUTING's "quoted in each entry" against DISC-009, `scripts/port.sh` still linting `docs/*.md perf/*.md README.md` (so its `claims` gate was RED on the published tree), DISC-013's range excluding its own largest measurement, three MEASURED ratios in no register at all, those ratios attributed to a commit that does not build, and the `opt-level=3` incumbent PLAN §2 promised and nobody had built. All thirteen are repaired: the three scripts refuse and exit 2 now, `port.sh claims` is `0 hit(s) in 11 file(s)`, the ratios have entry NE-006, and the `opt-level=3` incumbent exists — it is 1.389× the pinned build, which turns the published 3.06× into 1.989× and the published 1.27× into **0.773×, a loss**
- round 11 (report `docs/reviews/round-11.md`; 0 HIGH, 4 MEDIUM, 8 LOW; 0 differences in conversion content): the launcher asked `/dev/fd/2` whether stderr was open and lost every diagnostic where `/proc` is not mounted (repaired: `command exec 9>&2`); the SIZES of the reads on stdin are observable on packet pipes, SEQPACKET sockets and O_DIRECT files (DISC-015, ACCEPTED; two probe rows); PORT_REPORT's recipe named a commit older than the scripts it calls (repaired: the recipe names the newest commit of `main`); the README quoted capture numbers whose evidence files had not been committed (repaired by `b2ff04e`, before the report arrived; the README's numbers are generated from the committed files); the claims lint had four different file lists (one now); `stdio-probe.py` and `diff-fuzz.py` ended in a traceback without the oracle (a message and exit 2 now); the board's header carried a constant command text and date; seven stale cross-references; a README line that read a file that does not exist; statements about scripts that are not in the repository and a no-edit rule that was not kept (`scripts/ORIGIN.md` lists the seven local edits); three inconsistencies between documents; `TOON_BEND_BIN` naming a directory or an empty executable (the launcher refuses both).
- round 17 (report `docs/reviews/round-17.md`; 0 HIGH, 1 MEDIUM behavior, 2 LOW document; 0 differences from the original in about 248000 compared executions): R17-1, a mutant of `hot.merge` that let a failure found inside a nested merge replace an earlier failure of the same merge passed the corpus and the proof (the reviewer's input reports `"d"` where the original reports `"b"`): repaired by the laws `hot_dups_outer_failure_before_nested` (the hot merge against `dups` on that shape; the mutant is killed) and `decode_outer_dup_before_nested_dup` (the reviewer's input through `run_pure`). R17-2, `div_p10` is no longer called by the program since `94a9ecf` yet was described as a live fast twin: the comments in `port/LAWS.bend` and `port/f64.bend` now say so (its laws stay). R17-3, the README counted "three fast twins": it now names them.
- round 18 (report `docs/reviews/round-18.md`; 0 HIGH, 2 MEDIUM and 3 LOW behavior findings (all hand mutants that nothing caught), 2 LOW document findings; 0 differences from the original in about 607,000 compared executions): R18-1, the chunk join of EXP-019 had no gate (the corpus's stdin is a regular file, read once; the proof does not import `main.bend`): two `scripts/stdio-probe.py` rows now pipe 404,126 bytes in many chunks and fail on both of the reviewer's mutants. R18-2 to R18-5 (a backslash inside quotes before the delimiter, in an inline array, a tabular cell and under `|`; the FIRST of two bad escapes; FF before a quoted key; a backslash outside quotes in a header's field segment): six closed goldens through the whole pure core, each of the four mutants killed by the law written for it in a reduced proof. R18-D1, the lanes and proof rows cited `494ef82` and `9eb07dc`, scratch commits a rebase replaced before the push: the rows now cite published trees, and `scripts/claims-audit.py` finds any "tree of" citation not reachable from HEAD (it found these, and two more of the author's, `208c0c2`, in the ledger). R18-D2, NE-023's evidence could not reach a second chunk: amended, with the new probe rows.
- round 19 (report `docs/reviews/round-19.md`; 0 HIGH, 1 MEDIUM and 3 LOW behavior findings, all hand mutants that nothing caught, 1 LOW document finding; 0 differences from the original in about 1,094,000 compared executions): R19-1, a '0' after a fraction digit (the numeric-like table's phase 4, class 0) sent to the sink leaves `"1.50"` unquoted, which the original reads back as the number 1.5 (NE-030 had claimed "every refusal path" and was corrected); R19-2, a CR at the very end of a text without a final LF; R19-3, a byte order mark after the first line; R19-4, the colon search's escape (the twin of R18-2's splitter): seven closed laws with bytes from the pinned original, each of the four mutants killed in a reduced proof. R19-5, six evidence files named `494ef82`, a replaced scratch commit, which the "tree of" rule could not see: the files name the published `706f00e` now, and `scripts/claims-audit.py` requires every commit a COUNTED evidence file names to be reachable from HEAD (a falsification with the old hash is caught).
- round 20 (report `docs/reviews/round-20.md`; the first round under the adopted counting rule and on the frozen tree `55ec958`; 2 MEDIUM behavior findings, so NOT clean): R20-1, a corpus gap: EXP-029 builds the key set when the 9th member joins, and mutant N1 (the set built without that member) survived the corpus and the whole 593-law proof; repaired by three laws (`encode_repeated_key_the_member_that_builds_the_set`, `_the_builder_and_the_next`, `_the_builder_nested`) and the captured case `encstr_duplicate_keys_after_the_set_is_built` (prior MANIFEST hashes unchanged). R20-2, a gate that lied: the R19-5 rule of `scripts/claims-audit.py` had been moved, by the author's own edit that added the corpus rule, into the next loop, where it read a stale variable and checked one evidence file of 32; moved back, extended to the commit in a file NAME, and falsified both ways (a replaced commit in `COUNTED.canada.decode.json` and a file named `COUNTED.e2e-corpus.494ef82.json` are each reported under their own name). R20-3 (law coverage, one finding against `scripts/hand-mutants.py`): laws for `}`, `[`, `{`, edge whitespace, exponents with a 0 and a digit after a leading 0 kill all six mutants, and the inventory gains M38 to M44. R20-4 (document): "the 8th" corrected to the 9th in `port/json.bend`, `port/LAWS.bend`'s comment, the EXP-029 card and NE-032.
- round 21 (report `docs/reviews/round-21.md`; the second round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean): R21-1, a corpus gap: mutant R5 (`has_prim.go`'s arm returning `True{}`) wrote a reordered row holding an object as a table with `null`, surviving the corpus and the whole proof; repaired by the laws `encode_reordered_row_with_nested_object_is_list_form` and `_array_is_list_form` and the captured case `enc_reordered_row_with_nested_values_is_list_form` (prior MANIFEST hashes unchanged; the mutant's binary fails the case). R21-2 (a gate that lied: `converge.sh` and claims-audit accepted a round marked clean while two counted findings stood) and R21-3 (reachability shapes the audit missed) are repaired by a4 in `scripts/converge.sh` and `scripts/claims-audit.py`. R21-4 (law coverage, one finding against `scripts/hand-mutants.py`): eight laws kill all nine mutants and the inventory gains M45 to M54. R21-5 (document): the inventory's docstring no longer states a count.
- round 22 (report `docs/reviews/round-22.md`; the third round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane): R22-1, a gate that lied: nothing required a round labelled non-author to have a report, so two fabricated rows made `converge.sh` print CONVERGED; both `scripts/converge.sh` and `scripts/claims-audit.py` now report such a row, and `converge.sh` also counts the report's MEDIUM-or-HIGH behavior rows against the table from round 20 on (falsified: fabricated rows 22 and 23 are reported; a report re-spelled in lower case against a `0 | 0 | yes` row is reported). R22-2 (R21-2's audit half, reopened): the audit's counted-row test is case-insensitive, accepts BEHAVIOUR, and compares the counts even when the report yields 0 (falsified with the reviewer's title-case and BEHAVIOUR spellings); `scripts/harness-selftest.sh` gains M18 (a non-author row with no report) and M19 (counted rows re-spelled `Medium | Behaviour`), and catches 19 of 19. R22-3: the reachability rule reads every `COUNTED*` file, `.json` and `.jsonl`, walks the whole JSON (lists, `runs[]`, nested objects) for commit-like keys, reads a hash followed by a suffix in the file name, and reports unparseable evidence (falsified: nine planted shapes, each reported). R22-4 (law coverage, one finding against `scripts/hand-mutants.py`): four laws (`encode_json_backspace_and_form_feed_escapes`, `encode_underscore_initial_keys_are_bare`, `encode_short_row_in_header_order_is_list_form`, `encode_array_of_objects_as_list_item_is_list_form`) kill all five mutants in a reduced proof, each refused by the law written for it, and the inventory gains M55 to M59. R22-5 (document): the convergence rule below now states the round-20 rule.
- round 23 (report `docs/reviews/round-23.md`; the fourth round on the frozen tree under the adopted counting rule; 3 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R23-1 and R23-2, gates that lied again: every repair since round 20 had added a SHAPE the report reader knew, and every reader keyed on one layout read any other as zero findings. The repair changes the kind of check, not the list of shapes: `scripts/review_report.py` is now the ONE reader both `converge.sh` and claims-audit import, and it fails CLOSED. From round 20 every rounds-table row needs a report that parses completely (a reviewed-commit header HEAD contains; exactly one findings table found by its header names `id`, `sev`, `class`; every row's cells readable after markup is removed; every `R<n>-<k>` named anywhere in the report present in that table), and the lens is folded (NFKC, every dash) before any non-author test. Falsified with the reviewer's own scripts: both R23-1 routes and all 18 layouts of R23-2 (each given a valid header, so the verdict comes from the table reader) are reported by both gates. What no gate can do, and this file says so rather than implying otherwise: tell a complete, well-formed report written by the table's own author from a real one; the gates check that the record is complete and consistent, not who wrote it. R23-3: reachability fails closed over every file under `perf/evidence/` (every 7-40-digit hex run is a commit HEAD contains, a pin of `PIN.toml` or PLAN §2, or a digest: a whole 12-, 16- or 64-digit value under a digest-named key with no commit-named key above it, a UUID, or, in a file name, the prefix of a digest a ledger records); all 18 of the reviewer's shapes are reported. The residual limit is written in the code: a commit abbreviated to exactly 12 or 16 digits stored as a digest-named value reads as a digest. The six COUNTED notes that named the replaced scratch commit `494ef82` in prose now describe it without the hash. `harness-selftest.sh` gains M20 (the audit half of M19, which a4 verified by hand), M21 (an empty report) and M22 (an unreachable commit in a note in a subdirectory). R23-4, four corpus gaps: five captured cases (`encstr_key_later_char_range_ends`, `toonerr_empty_field_between_names`, `toonerr_empty_last_field_name`, `toonedge_bracket_then_equals_is_key`, `toonedge_quoted_key_cr_escape`; prior MANIFEST hashes unchanged) and five laws. R23-5 (law coverage): eight laws for ten of the twelve mutants; H1 and H2 (the length cap 100000000) have NO law, because a closed law on that boundary makes the checker build 10^8 in unary, and stay pinned by `toonerr_length_at_cap` on every lane. The inventory gains M60 to M73, the reviewer's own mutant texts. The self-test's first run with M20 to M22 found a hole in this round's own repair (M22 LEAKED: the reachability rule skipped hex runs made only of letters, and M22 plants `deadbee`); all-letter runs are read now, and the re-run catches 22 of 22. The four lanes pass at 1083 cases on the tree of `cc1ea20`, the interpreter lane by a re-run: in the `lanes.sh` run it failed 89 cases while the host's disk was 100% full (another job's scratch filled it), and three of those cases, then the whole lane, pass once space was freed. Two cases of rounds 20 and 21 had been cited by no clause of the spec (spec-lint's pasted line had not been re-run since 1071 cases); they are cited now.
- round 24 (report `docs/reviews/round-24.md`; the fifth round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R24-1, a gate that lied in round 23's own repair: the one report reader read the raw file line by line, so a findings table a reader never sees (inside an HTML comment, a code fence, or an indented block) stood in for the visible one, which, re-headed `finding | severity`, was not recognised; with every row copied into the hidden decoy, every count agreed and two fabricated rows printed CONVERGED. The reader now reads only what the RENDERED report shows (comments, fenced and indented code removed first; block HTML refused; no other table may list the round's ids in its first column): the reviewer's decoy scripts (comment, fence, indented, and the two-row variant) and a4's planted M23 are refused by both gates, and a4's control C1 (a hidden table with a DIFFERENT count) stays silent, as it must. R24-2: reachability now follows symlinked subdirectories, reads the RAW text (an unquoted hex-shaped JSON number had become a float), certifies a file name only by a ledger digest, exempts `0x` only at address length, and refuses runs over 40 digits unless 64 or 128 long; every shape of the reviewer's is reported (checked one by one: its script's own summary column filters for the other planted hash). R24-3, three corpus gaps in the header parser: three captured cases (`toonerr_semicolon_after_fields`, `toonedge_space_and_tab_before_header_colon`, `toonedge_quoted_field_not_expanded`; prior MANIFEST hashes unchanged) and three laws. R24-4 (law coverage): five laws for the eight mutants. Each of the eleven is refused by a law in a reduced proof, and the inventory gains M74 to M84.
- round 25 (report `docs/reviews/round-25.md`; the sixth round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R25-1, the report reader again: each of rounds 22, 23, 24 and 25 found another way the hand-written reader and a Markdown renderer disagree (this round: a table in a blockquote or under a list item was missed or thrown away, while a short delimiter row, a setext heading and a `<pre>` block were read as tables). So the reader no longer imitates a renderer: `scripts/review_report.py` parses the report with markdown-it-py (CommonMark with GitHub tables and strikethrough), reads tables wherever they render, and treats every report as an error where that parser is missing. On the reviewer's nine test reports it now agrees with the renderer on every one; both decoys are refused, and the honest report whose table sits under a list item, which the old reader refused, is read. R25-2: reachability reads a JSON file as its decoded structure (`\u` escapes), keeps numbers as their source text, removes only digest-keyed values (blanking a digest's text erased the same text standing as a commit), and exempts a pin only as itself or an abbreviation; every shape of the reviewer's is reported, each checked by name. R25-3, seven corpus gaps (four in the decoder, three argv number words): seven captured cases and seven laws. R25-4: four laws; the `-o=` mutant is pinned on the argv tokenizer, since the pure core does not model the output path. Each of the eleven is refused by its own law in a reduced proof; the inventory gains M85 to M95. R25-5: S4.225's probes were re-run on the pin and amended (over-indented, not `Missing colon after key`), with the case note. The reviewer reports breaking RULE 1 once in its own scratch directory (`rm -f` of its copy of round 24's script, the original untouched), recorded in its report.
- round 26 (report `docs/reviews/round-26.md`; the seventh round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R26-1, a gate that lied in round 25's own repair: `scripts/review_report.py` folded the whole report (NFKC) BEFORE markdown-it parsed it, so fullwidth backticks became a code fence and fullwidth pipes a table that no renderer shows; the raw text is parsed now and only the extracted cell text is folded. The reviewer's decoy is refused by both gates. R26-2: the reachability rule decodes JSON with every key/value pair kept, so a duplicate `commit` key no longer hides the first value; falsified with a4's composed plant (the unreachable commit `\u`-escaped so no raw run reaches seven digits), named in both key orders, with a reachable-only control quiet. R26-5: the closed-law count pattern now also matches "N are closed instances", which is how README states it; the audit then reported README's stale 571 at once. R26-3, ten corpus gaps: ten captured cases and six laws; R26-4: three laws. Five of the fifteen mutants have NO law, and each is pinned by a captured case instead: N10 and N11 (the nesting limit needs a 16 KB input), and J1 to J4 (exponent arithmetic; J4's law was written and then dropped before any commit, because the checker had not refuted its mutant after 180 s under a 6 GB cap: refuting it prints a fraction through the software float). An earlier attempt at the reduced proof ran that law unmutated for 20 minutes at 22 GB on the shared host before the author killed it. Each of the nine lawful mutants is refused by its own law in a reduced proof, and the inventory gains M96 to M103 (N4 is two edits, which the inventory cannot express).
- round 27 (report `docs/reviews/round-27.md`; the eighth round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R27-1, a gate that lied in round 26's own repair: `scripts/review_report.py` read `<del>LOW</del> MEDIUM` as LOW, which a browser renders as MEDIUM; any inline HTML in a findings row is now refused rather than rendered by guesswork (no report filed before it has any, and their counts are unchanged). Falsified on a clone of `25d410d` with the reviewer's own decoy: `<del>`, `<s>` and `<span hidden>` are each refused and `converge.sh` stays NOT_CONVERGED, while the `~~LOW~~ HIGH` control still counts 1. R27-2: evidence JSONL is decoded one line at a time, and a line that is not JSON is scanned raw and with its `\u` escapes decoded; a4's plant (one bad line plus a commit with every third character escaped) is caught, as is its control. R27-3, seven corpus gaps: seven captured cases; five of the seven, with R27-4's E6, have a law each (checked alone and against its mutant under a 6 GB / 180 s cap), and the inventory gains M104 to M108. Seven mutants have NO law by design and are pinned by captured cases instead: N17, N18 and N19 (16 KB nesting inputs), J5, J10 and J11 (exponents the checker would reduce in unary), and D4 (no reviewer input; two corpus cases catch it).
- round 28 (report `docs/reviews/round-28.md`; the ninth round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R28-1, a gate that lied in round 27's own repair: the inline-HTML refusal covered the findings body only, and a header `| id | <del>sev</del> | sev | class |` renders one `sev` column while the reader took the struck one. `scripts/review_report.py` now refuses inline HTML in ANY table cell, and a findings table with two columns a reader could take for one field (`sev | sev`, `sev` beside `severity`); every report filed before it re-parses with unchanged counts, and the reviewer's decoy re-run on a clone of `a86d9d4` is refused with `converge.sh` NOT_CONVERGED. R28-2: only a run's own pasted `"mutants": N` is a selection now; the fix at once exposed README's stale mutant total (it still named the inventory as of round 26, five short) and an unmarked historical run in PORT_REPORT, both corrected. R28-3, six corpus gaps: seven captured cases (the last overlong UTF-8 value of three and four bytes, a C0 continuation byte, both ends of the shortest-digit interval with an odd significand, and an i64::MIN indent). Ten laws, each checked alone and against its mutant under a 6 GB / 180 s cap, cover U2, U3, U4, U6, Ce, Cc, Cd, K2, K3, K4 and I1 (inventory M109 to M119). F2 and F3 are pinned by the captured cases, and B12 and F1 by the corpus that already catches them: their whole-pipeline laws did not finish in 180 s.
- round 29 (report `docs/reviews/round-29.md`; the tenth round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R29-1, the sixth round in a row of one family (the report reader deciding what a page shows): it matched header SPELLINGS, so a `Severity:` or a `sev` with an invisible U+200B beside `sev` chose the column read. `scripts/review_report.py` no longer matches: exactly one table may mention sev or class in any spelling, its header must be exactly `| id | sev | class | what | spec |` (the contract every brief states, which every filed report meets), and a Unicode Cf character in any table cell is refused. The header and the three cells the count rests on are read as SOURCE text, no markup interpreted: a bare `R<n>-<k>`, exactly HIGH, MEDIUM or LOW, and a class that starts with a bare class word (struck, emphasised, linked or entity-spelled cells are refused). Every report filed before it re-parses with unchanged counts, and the reviewer's three decoy modes, re-run on a clone of `e33925e`, are refused. R29-2: each pasted `"mutants": N` is judged by the text since the previous paste (the ids its own `hand-mutants.py` command lists, else a range `M<a> to M<b>` named there, else the whole inventory), the first two exactly. It caught a real error: PORT_REPORT said all 36 mutants beside a paste of 34, and the inventory at that commit held 34 (M01 to M35); four past runs now state their scope, each checked against the inventory at its commit. R29-3: seven captured cases, three for the corpus gaps (bracket content that is only a delimiter, a character after a quoted key, U+DFFF) and three for behaviours only a law had pinned (N4, K1, U11). Six laws, each checked alone and against its mutant under a 6 GB / 180 s cap, cover H5, H6, U10, N1, E3 and C2 (inventory M120 to M125). F6 and F7 stay pinned by the corpus that catches them: their laws would print through the software float.
- round 30 (report `docs/reviews/round-30.md`; the eleventh round on the frozen tree under the adopted counting rule; 2 MEDIUM behavior findings, so NOT clean; no difference between the port and the original on any lane). R30-1, the report reader again but on a third surface: round 29's header and cell contracts held, and the finding was in the completeness check (every id the report names has a row), which read ids from rendered inline text only. It now reads the SOURCE outside code blocks (prose, inline code, HTML blocks and comments) with every Unicode Cf character removed, so a hidden id is found rather than missed, and an id spelled with a non-ASCII letter is an error; code blocks stay quotation (the brief asks decoys to be shown there), a declared limit. The reviewer's seventeen decoy files, re-run on the reader of `e19d8b5`: every hiding shape refused, every honest header parsed. R30-2: a header is a findings candidate when one of its WORDS starts with `sev` or `class`. R30-3: a command's ids are all its `M<n>` arguments, a command earlier in the same paragraph scopes a paste, and a whole-inventory run must name the commit it was pasted in, whose inventory the audit reads from git (the four past whole runs now name theirs, each checked). R30-4: three captured cases (an escaped quote in a quoted field name, a duplicate field name, spaces after a fields colon); four laws, each checked alone and against its mutant under a 6 GB / 180 s cap, cover X1, X11, X12 and X2 (inventory M126 to M129); X10 stays pinned by the twelve corpus cases that catch it (a law would need a 128-deep input). R30-6: a SURVIVED in the law-mutation set is a candidate, and a finding unless the mutant is shown equivalent by reading the def (a corpus pass alone does not show it); this round's three survivors were shown equivalent from their defs.

Convergence (computed by `scripts/converge.sh docs/PORT_STATE.md`): T1 ≥ 3
rounds and ≥ 1 clean at the end; T2 ≥ 5 and ≥ 2 consecutive clean at the end and ≥ 1 non-author round; T3
≥ 10 with the last two rounds clean; a clean round has no unresolved finding from that round and, from round 20 on
(the adopted counting rule), ZERO counted findings (a counted finding is a MEDIUM-or-HIGH behavior finding; before
round 20, < 3 new genuine findings); a dirty or reopened
round resets the clean streak. Every OQ is resolved or excluded; no OPEN DISC. Current: NOT_CONVERGED (the last TWENTY-FIVE rounds, 6 to 30, are not clean; under the adopted counting rule rounds 20, 21, 22, 24, 25, 26, 27, 28, 29 and 30 had 2 MEDIUM behavior findings each and round 23 had 3; no DISC and no OQ is open).


## Next action (one line, executable)

`git clone https://github.com/Dicklesworthstone/toon_bend /data/tmp/review_R31/clone && git -C /data/tmp/review_R31/clone rev-parse HEAD` — then round 31 attacks THAT clone as a fresh non-author reviewer under the adopted counting rule, on the tree still FROZEN (only repairs land): first whether round 30's repairs hold (ids read from the source outside code blocks; honest headers; the audit's scoping of mutant pastes; the laws of R30-4 and R30-5 and the inventory M126 to M129), then `scripts/law-mutation.sh` over the cli, f64 and bignat defs round 30 did not reach, then paths no recent round aimed at, on every lane including js and the interpreter; one heavy Bend job at a time on this shared host, under 3 GB of disk.
