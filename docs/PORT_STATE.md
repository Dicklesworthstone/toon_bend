# Port state — Toon → Bend 2

<!-- Read FIRST on every resume; rewritten at the END of every session.
     Facts only, each with the command that produced it. -->

## Where we are

| field | value |
|---|---|
| phase | 4 parity gate (NOT converged: EIGHT non-author rounds in a row were not clean: 6, 11, 8, 3, 10, 12, 13 and 9 findings (rounds 10, 11, 12 and 13 audited the documents and a fresh clone; rounds 9 to 13 found 0 differences in conversion content); every DISC is ACCEPTED or RESOLVED since the owner's delegation) and 5 performance (three number levers PROVISIONAL, the key carriers ledgered, and a fourth lever REFUSED and reverted: EXP-005, NE-007, 2026-09-21); the reference port is complete and green on every lane |
| tier | T2 |
| bend | `bend 2.0.16` (checkout `15ae0c8`, run as `bun /tmp/bend/bend2/main.ts`); drift vs the previous pin: none, same pin since Phase 0 (VERSION-DRIFT) |
| last updated | 2026-09-21 by Claude (Claude Code session, author of phases −1 to 5; rounds 6 to 13 are non-author subagents; their reports are in `docs/reviews/`) |

## Last gate outputs (paste, do not paraphrase)

| gate | command | result | date |
|---|---|---|---|
| proofs | `bend port/PROOF.bend` | All terms check. (unsafe 0 = 0 @unsafe + 0 template instances; bend 2.0.16) | 2026-09-22 |
| lanes | `scripts/lanes.sh goldens/cases.tsv goldens port/main.bend --threads 8` | `{"lanes":[{"lane":"interpreter","verdict":"PASS","passed":1065,"failed":0},{"lane":"c-1t","verdict":"PASS","passed":1065,"failed":0},{"lane":"c-8t","verdict":"PASS","passed":1065,"failed":0},{"lane":"js","verdict":"PASS","passed":1065,"failed":0}],"stderr_compared":true,"timeouts_seconds":{"interpreter":60.0,"compiled":5.0,"build":600},"verdict":"PASS"}` | 2026-09-22 |
| parity | `scripts/parity-board.sh docs/FEATURE_PARITY.md` | rows=33 present=27 partial=0 missing=0 excluded=6 n/a=0 no-evidence=0 verdict=DEBT | 2026-09-22 |
| floor | `scripts/floor.sh goldens/cases.tsv goldens --repeat 2 -- ./oracle/toon` | `{"repeat":2,"stable":1065,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | 2026-09-22 |
| kill-switch | `TOON_SPEC=1 <binary> --gpu off -- ["--encode","cases/inputs/hand/large_tabular_1500.json"]  vs  env -u TOON_SPEC <binary> --gpu off -- ["--encode","cases/inputs/hand/large_tabular_1500.json"]  (binary built from port/main.bend into a temp dir)` | PASS: identical stdout, stderr and exit with and without TOON_SPEC=1 on '["--encode","cases/inputs/hand/large_tabular_1500.json"]' | 2026-09-22 |

**The dates in those five rows are the gate's own, and the gate stamps UTC.** The run spanned local 2026-09-21 20:46 to 22:26 (UTC−4), so it prints 2026-09-22 while this file's prose, its commits and the ledgers use local dates. The rows are pasted exactly as printed and are NOT edited to agree with the prose.

The five rows are the table of ONE run: `BEND_CLI='bun /tmp/bend/bend2/main.ts' ./scripts/port-doctor.sh --threads 8 --original ./oracle/toon -- --switch TOON_SPEC=1 --probe '["--encode","cases/inputs/hand/large_tabular_1500.json"]'`, started on the tree of commit `2dee063` (the commit that reverted EXP-005 and put the port back on the spec twin) and finished in 1 h 40 m. Its last lines: `doctor: GREEN` and `{"proof":"All terms check.","unsafe":"0","unsafe_annotations":"0","instances":"0","unsafe_count_mode":"explicit-and-instances","bend":"bend 2.0.16","lanes":"PASS","board":"DEBT","floor":"STABLE","switch":"PASS","verdict":"GREEN"}`.

Every input the run gated is byte-identical at the commit this file is written on, so the table describes THIS tree and not only the one it started from: `git diff --stat 2dee063 HEAD -- port/ goldens/ docs/FEATURE_PARITY.md` is empty, and `oracle/toon` is untouched. The commits between them changed documents (`README.md`, `docs/PORT_REPORT.md`, this file, `perf/EXPERIMENTS.md`, `perf/NEGATIVE-EVIDENCE.md`) and two scripts the doctor does not run (`scripts/claims-audit.py`, `scripts/hand-mutants.py`). `port/` is also byte-identical to its pre-lever state: `git diff --stat 3b67865 HEAD -- port/` is empty.

Two notes a reader should not have to reconstruct. (1) This run OVERLAPPED the 25-mutant `hand-mutants.py` run below for its last 40 minutes, on a host also running another agent's interpreter lane (load 5-7 throughout, 11 GB of 30 free at the tightest). That is a timing hazard and not a correctness one, and it did NOT bite: no case on any lane hit its budget (interpreter 60 s, compiled 5 s), and the heavy `large_*` and `determinism_*` cases were already past on the interpreter lane before the second run started. (2) The previous table in this place was started on `c7239e2`; that row is superseded, not deleted, and the earlier run's provenance is in this file's git history.

Notes on these lines:

- lanes: four complete all-lane runs passed: on the trees of `1230a0d` (1053 cases), `d80251a` (1060), `4c3cccc` (1065) and `c7239e2` (1065, the doctor's row above). ONLY the first and the last of those commits build from the public history: `d80251a`, `861abf2` and `4c3cccc` lack the stdin effect's two twins, which `.gitignore` hid until `a725d10` (round 10's HIGH finding), so an auditor reproduces the line at `c7239e2` or at any commit after it. The single interpreter failure of the runs before them was the HARNESS: `scripts/interp-lane.sh` captured stderr in a plain file, where `-o /dev/stderr` overwrites (case `flag_output_dev_stderr`); the wrapper uses a pipe now. `./scripts/conform.sh` also passes 1065/1065 on c-1t, c-8t and js with `TOON_SPEC=1`.
- proofs: 374 laws = 14 quantified + 65 closed unit laws + 295 closed whole-pipeline `golden_<case>` laws; unsafe 0 = 0 `@unsafe` + 0 template instances, bend 2.0.16 @ `15ae0c8`; 4 min 04 s. `./scripts/law-coverage.sh` → `{"laws": 374, "proofs": 374, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}`.
- law admission: `./scripts/law-mutation.sh port <defs> --file <module>` → INCONCLUSIVE on `bignat.bend`, `f64.bend` and `text.bend` (its textual operators find no valid site in this code style). `python3 scripts/hand-mutants.py` holds 25 mutants now. The last COMPLETE run of the whole set was earlier, over the 22 that existed then (the fast twins, the key carriers and the repeated-key map, reduced proof of 123 laws): `{"laws_in_proof": 123, "reduced": true, "mutants": 22, "killed": 22, "survived": [], "not_evidence": [], "verdict": "STRONG"}`. The three added on 2026-09-20 are round 13's own (R13-9), which SURVIVED the whole 368-law proof before the laws below existed; `python3 scripts/hand-mutants.py M24 M25 M26` → `{"laws_in_proof": 129, "reduced": true, "mutants": 3, "killed": 3, "survived": [], "not_evidence": [], "verdict": "STRONG"}` (M24 by `writer_b_escapes_del`, M25 by `utf8_rejects_surrogate`, M26 by `tab_in_value_forces_quotes`). A run of all 25 together is NOT yet evidence: it was started on 2026-09-20 and stopped by the host, which was critically low on memory (26 of 30 GB used, 21 GB of swap), so it produced no line. Bead `toon_bend-sp5`. Its FIRST pass had 7 survivors of 16, and the pass over the 12 mutants of round 7's new code had 1: each is pinned by a closed law now (`div_pow10_*`, `int_fit_*`, `int_text_*`, `short_of_digits_*`, `kt_collision_*`, `ks_bucket_*`, `golden_encstr_duplicate_keys*`). What NO law can pin: `Nat` is unbounded in the checker, so the 2^48 − 1 bound of the compiled lanes is pinned only on the guard defs (`int_fit_refuses_2p48`, `int_text_15_digits_refused`) and by the goldens.
- the documents' own numbers: `python3 scripts/claims-audit.py` → `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 1, "laws": 374, "cases": 1065, "disc": {"accepted": 13, "resolved": 2, "open": 0}, "verdict": "OK"}` (every count and every reference of the claim-bearing documents, computed from the repository: the review rounds' most repeated finding).
- the gates themselves: `./scripts/harness-selftest.sh -- ./oracle/toon` (twelve deliberate lies, each in a scratch copy: an edited golden, a missing golden, an empty manifest, a renamed case, the original run as the port, an unproved law, a proof of no law, a deferral word in a ledger, an intention in this file, a `present` row without evidence, a silent re-capture) → `{"schema":"p2b.harness-selftest.v1","sha":"77a63d9","bend":"bend 2.0.16","host":"Linux-x86_64","lane":"c-1t","mutations":12,"caught":12,"leaked":[],"untestable":[],"scratch":"/data/tmp/hst.qVkbdB","verdict":"OK"}`. An earlier run said UNTESTABLE for five of them: the script copied `goldens/` without `cases/`, where this port keeps the stdin files; it copies `cases/`, `.beads/`, `bin/` and the three root documents and links `oracle/` now. `./scripts/clean-build-check.sh HEAD` → `{"ref":"4213127","export":"/data/tmp/clean-build.d5gfh3","native":"built","js":"built","conform_c1t":{"passed":1065,"failed":0},"verdict":"PASS"}` (2026-09-21, the commit whose doctor table is pasted above; the earlier run of this gate said the same for `77a63d9`, and `4c3cccc` → FAIL at the native build: round 10's finding). The gate exports TRACKED FILES ONLY, so this is the line that answers round 10's HIGH finding for the commit this file cites.
- `python3 scripts/port-lint.py port/*.bend --laws port/LAWS.bend` → `{"files": 10, "findings": 41, "errors": 0, "warnings": 0, "infos": 41, "by_rule": {"PL-02": 41}, "laws": "port/LAWS.bend", "verdict": "OK"}` (the 41 infos are recursions whose depth is a nesting depth, a limb count bounded by the 800-digit clamp, at most 17 digits, 16 hash bits, or the height of a balanced bucket).
- `python3 scripts/spec-lint.py docs/EXISTING_Toon_STRUCTURE.md goldens/cases.tsv` → `spec-lint: 703 clauses, 1065 cases, 1065 cases cited, 0 finding(s)`.
- `python3 cases/build-cases.py --check` → `{"cases": 1065, "verdict": "OK"}`; `./scripts/cases-lint.sh goldens/cases.tsv` → `{"cases": 1065, "errors": 0, "notes": 20, "classes_missing": "", "verdict": "OK"}`. Corpus: 1005 → 1053 (Phase 4) → 1060 (round 7) → 1065 (round 8); every re-capture added hashes and changed none (MANIFEST 3195 hashes).
- `./scripts/pin-check.sh docs/PIN.toml` → `pin-check: RED` (2026-09-21; it was YELLOW until this session and this file said so). TWO rows are not green. (1) **`original_commit` RED**: `original source is not tracked, or HEAD 6422239f73002c22761e3f014651fdded908d1db does not match pin f955c67015d45330935b3ae4d62178d2bb44a481`. `legacy/Toon` is a symlink to `/dp/toon_rust`, a SIBLING repository other agents work in, and its HEAD moved onto a dependabot merge. Nothing in this port changed and **no evidence is affected**: the goldens rest on the binary `oracle/toon`, whose sha256 is `980f2b26484f424b74cb0a0b61062183d0038d45101590a38953a82611879a6d`, exactly PIN.toml's value, with `original_version` GREEN (`toon 0.2.4`), `manifest_hashes` GREEN (`3195 hashes and capture inputs verified`) and `case_count` GREEN. The pinned commit `f955c67` still EXISTS in that repository (`git -C /dp/toon_rust cat-file -t f955c67` → `commit`), so the pin is verifiable and the oracle rebuildable; returning that checkout to the pin is NOT this port's to do (AGENTS.md: never disturb another agent's tree). Bead `toon_bend-4k5`. (2) `manifest_version` YELLOW, as before: `golden-capture.sh` writes no `version:` line into MANIFEST, so that row cannot turn green without a hand edit of `goldens/`, which is forbidden; identity is carried by `original_version` GREEN and the executable sha256 in MANIFEST's provenance.
- what no case can express: `python3 scripts/stdio-probe.py -- <port>` (27 descriptor-state rows against the original; the temp-directory field `work` is cut from each line): native binary `{"rows":27,"same":13,"known":["DISC-006: stdout is open READ-only","DISC-003: stderr is open READ-only, a success line to write","DISC-015: stdin is a SEQPACKET socket, one message of 41 bytes: the original sees a cut-off document","DISC-015: stdin is a SEQPACKET socket, one message of 45 bytes: the original sees a COMPLETE document and stops","DISC-015: stdin is a SEQPACKET socket, one message of 44 TOON bytes: both exit 0 and the bytes differ","DISC-014: -o /dev/fd/3, which the caller did not open","DISC-007: stdin closed, no INPUT","DISC-007: stdin and stdout closed, INPUT is a file, 66 KB of output","DISC-006: stdout closed, small output","DISC-003: stderr closed, a usage error","DISC-003: stderr closed, a success line to write","DISC-006: stdout is /dev/full","DISC-003: stderr is /dev/full, a conversion error","DISC-003: stderr is /dev/full, a usage error"],"fixed":[],"new":[],"verdict":"PASS"}`; through `bin/toon` `{"rows":27,"same":13,"known":["DISC-015: stdin is a SEQPACKET socket, one message of 41 bytes: the original sees a cut-off document","DISC-015: stdin is a SEQPACKET socket, one message of 45 bytes: the original sees a COMPLETE document and stops","DISC-015: stdin is a SEQPACKET socket, one message of 44 TOON bytes: both exit 0 and the bytes differ","DISC-014: -o /dev/fd/3, which the caller did not open","DISC-006: stdout is /dev/full","DISC-003: stderr is /dev/full, a conversion error","DISC-003: stderr is /dev/full, a usage error"],"fixed":["stdout is open READ-only","stderr is open READ-only, a success line to write","stdin closed, no INPUT","stdin and stdout closed, INPUT is a file, 66 KB of output","stdout closed, small output","stderr closed, a usage error","stderr closed, a success line to write"],"new":[],"verdict":"PASS"}`; JavaScript build (`--timeout 20`) `{"rows":27,"same":13,"known":["DISC-006: stdout is open READ-only","DISC-003: stderr is open READ-only, a success line to write","DISC-015: stdin is a SEQPACKET socket, one message of 41 bytes: the original sees a cut-off document","DISC-015: stdin is a SEQPACKET socket, one message of 45 bytes: the original sees a COMPLETE document and stops","DISC-015: stdin is a SEQPACKET socket, one message of 44 TOON bytes: both exit 0 and the bytes differ","DISC-014: -o /dev/fd/3, which the caller did not open","DISC-006: stdout is /dev/full","DISC-003: stderr is /dev/full, a conversion error","DISC-003: stderr is /dev/full, a usage error"],"fixed":["stdin closed, no INPUT","stdin and stdout closed, INPUT is a file, 66 KB of output","stdout closed, small output","stderr closed, a usage error","stderr closed, a success line to write"],"new":[],"verdict":"PASS"}`. `python3 scripts/diff-fuzz.py scale --runs 16000 -- <port> --` → `{"lens": "scale", "seed": 1, "inputs": 18, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}` (the same lens on the binary of `1230a0d`: `"too_slow": 5`).
- incumbent: `scripts/incumbent-bench.sh` captures are in `perf/evidence/`. MEASURED: EXP-001 1.67×, EXP-002 1.56×, EXP-003 2.12× against the build of `4bfecef` (A/A 1.00; PROVISIONAL, `perf/NEGATIVE-EVIDENCE.md` NE-001..003); EXP-004 against the original: 0.25× (16000 keys), 1.27× (1200-field rows), 3.06× (40000 expanded lines), one NO_EVIDENCE (NE-004). Ordinary inputs against the original (`toon 0.2.4` @ `f955c67`, release; the port's build of `4c3cccc`, 1 thread, 15 AB/BA pairs): 0.114× on the 1500-row table encode, 0.404× on its decode, 0.0066× on 20000 doubles (cv ≤ 4.7%; `perf/evidence/INCUMBENT.tabular-enc.json`, `.tabular-dec.json`, `.doubles.json`); five more captures of that run were REFUSED_CV and claim nothing; the one-lever capture of EXP-003 alone: 2.05× (A/A 1.019); EXP-001 and EXP-002 alone: REFUSED_CV twice, refused medians about 16% and 3% below, so the 1.67× and 1.56× belong to the three levers together.

## Owner decisions (2026-09-20)

The repository owner delegated every open decision to the author: "You decide on everything. I approve whatever you want to do." and asked for a public GitHub repository. The author's rulings, each recorded where it belongs:

- DISC-001..008, 010, 011, 013 (and DISC-014, DISC-015, found by rounds 9 and 11 after the delegation and ruled on under it): ACCEPTED, each with a scoped contract under its `Resolution:` (`docs/DISCREPANCIES.md`). DISC-009 and DISC-012 are RESOLVED by repairs.
- the custom effect `Stdin.open`: it STAYS (DISC-012's approver line; `AGENTS.md`).
- C-1..C-11: every candidate stays bug-compatible (the ruling paragraph above their table).
- license: the original's text verbatim, MIT with its rider (`LICENSE`); remote: `https://github.com/Dicklesworthstone/toon_bend`, public.
- NOT decided by delegation: the deletion of `./-` and `docs/AGENTS.scaffold.md`. RULE 1 of `AGENTS.md` asks for the owner's exact command, and a general approval is not one. `./-` is ignored by git and is not published. It IS named in MANIFEST's capture provenance (an argv word `-` made the capture fingerprint the file of that name), so deleting it must be followed by a re-capture with `--repin`, or `pin-check.sh` reports a changed input.

## Open items

| id | what | blocks | owner |
|---|---|---|---|
| rounds | T2 needs the last two rounds clean; rounds 6 to 13 found 6, 11, 8, 3, 10, 12, 13 and 9 (2 HIGH in round 7 on stdin; 1 HIGH in round 10 on unbuildable cited commits; none in rounds 11, 12 and 13). Since round 9 the findings are about descriptor states, the launcher and the documents, not about conversion bytes. `converge.sh` reads clean tail 0, so rounds 14 AND 15 must both be clean | `converge.sh` | a fresh non-author subagent per round, after the previous round's repairs (round 14 is bead `toon_bend-807`) |
| NE-001..007 | four PROVISIONAL entries (the three number levers, of which only EXP-003 is shown to meet its gate alone; the key carriers), one MEASURED_LOSS (NE-006) and two NO_EVIDENCE: NE-004 (the folding ratio, refused three times) and NE-007 (EXP-005, refused once, source reverted 2026-09-21) | any WIN claim for them | author, on a quiet host; the owner for the binding question (bead `toon_bend-clf`) |
| incumbent | five of eight captures against the original are still REFUSED_CV (the original needs under 6 ms for them) | a speed sentence for those inputs | author, on a quiet host with inputs ten times larger (bead `toon_bend-udw`) |
| pin drift | `pin-check.sh` is RED since 2026-09-21: the sibling checkout `/dp/toon_rust` behind `legacy/Toon` moved off pin `f955c67` onto the dependabot merge `6422239`. The ORACLE BINARY is untouched and sha256-verified against PIN.toml, so no golden, lane, floor or parity line is affected | a GREEN `pin-check`, and any future rebuild of the oracle from source | the repository owner (returning another project's checkout to a commit is not this port's to do); bead `toon_bend-4k5` |
| `./-` and `docs/AGENTS.scaffold.md` | two stray files created in Phase 0/1 (a probe's `-o -` output; a needless copy) | nothing; deletion needs the owner's exact command (RULE 1) | the repository owner |

## Find-fix rounds (Phase 4/5)

| round | lens | new genuine findings | fixed | clean? | date |
|---|---|---|---|---|---|
| 1 | author: mutation fuzz of corpus documents against the original (9000 TOON, 4000 JSON) | 1 | 1 | yes | 2026-09-20 |
| 2 | author: argv fuzz against the original (clap emulation; 4000 then 12000 command lines) | 1 | 1 | yes | 2026-09-20 |
| 3 | author: structure-aware generated documents, every option, encode then decode (4000), and the 45 cases proposed by the pass-2 reader and by Phase 3 | 0 | 0 | yes | 2026-09-20 |
| 4 | author: lane hazards, inputs of 10^5 and 10^6 elements on C and JS against the original (25 shapes) | 1 | 1 | yes | 2026-09-20 |
| 5 | author: kill-switch parity and twin-vs-twin numbers (1005 cases under both settings; 127838 generated numbers, both directions, against the original) | 1 | 1 | yes | 2026-09-20 |
| 6 | non-author hostile review (subagent): descriptor and platform states, inputs large in one dimension, a review of every claim in the documents (non-author) | 6 | 6 | no | 2026-09-20 |
| 7 | non-author hostile review (subagent): the new key carriers, scale, numbers in both directions and under `TOON_SPEC=1`, argv and files, lane agreement (about 122000 compared executions) (non-author) | 11 | 11 | no | 2026-09-20 |
| 8 | non-author hostile review (subagent): balanced buckets and the repeated-key map, stdin and descriptors beyond the probe, signals and resources, 40 random spec clauses, a fresh-seed floor (about 90000 compared executions) (non-author) | 8 | 8 | no | 2026-09-20 |
| 9 | non-author hostile review (subagent): round trips through all 1632 option combinations, hand-made and mutated TOON, JSON error positions at every truncation, files and in-place behavior, every Unicode scalar value in 32 positions, a fresh-seed floor (947646 compared executions) (non-author) | 3 | 3 | no | 2026-09-20 |
| 10 | non-author hostile review (subagent): a fresh clone of the PUBLIC repository built and gated by the documents' own commands, an audit of every number and claim in nine documents, terminals and environments (7955 executions), threads and determinism, the interpreter lane off the corpus, a fresh-seed floor (non-author) | 10 | 10 | no | 2026-09-20 |
| 11 | non-author hostile review (subagent): a fresh clone at the new HEAD running every documented command, a claims audit of about 200 statements, `--stats` at volume, 1742 real-world JSON files, hand-written TOON with human mistakes, 24238 argv permutations, the launcher under dash, bash, busybox and in sandboxes (about 74500 compared executions) (non-author) | 12 | 12 | no | 2026-09-20 |
| 12 | non-author hostile review (subagent): a fresh clone at `83f68bb` built and gated by the documents' own commands, 4802 spec-clause predictions, the JavaScript and interpreter lanes, 160 filesystem and argv states, descriptor states beyond the probe, and a claims audit of every number in eleven documents (13298 compared executions of its own generators plus 10038 of the repository's fuzz lenses) (non-author) | 13 | 13 | no | 2026-09-20 |
| 13 | non-author hostile review (subagent): a fresh clone at `1f184e8` built on both lanes, this session's own repairs re-tested (the `opt-level=3` numbers re-captured, the three missing-oracle guards, the byte-reproducible build, ORIGIN.md against `git log`), eight NEW hand-written law mutants, nesting depth on three lanes, metamorphic round trips over the corpus, 35 environment states, and a claims audit (about 421000 compared executions) (non-author) | 9 | 9 | no | 2026-09-20 |

What each round found (every finding was resolved by RUNNING the original, then a clause, a case and a repair; "fixed" counts findings repaired OR registered as a DISC):

- round 1: OQ-P3-2, a quoted key is unescaped before the colon after its closing quote is required (7 of 3000 mutated documents differed); S2.126 amended, `toonerr_quoted_key_escape_before_colon*`.
- round 2: OQ-P4-1, `--help=x` / `--version=x` with a non-empty committed list print the flag before the group line (11 of 4000 command lines differed); S1.86 amended, `usage_help_with_value_after_commit`, `usage_version_with_value*`.
- round 4: on the JS lane Base's `List.length` overflowed the machine stack on a 200000-field tabular header (`bend: memory fault`), while the C lane and the original answered; every `List.length`, `String.length` and `++` over input-controlled text is now a loop (`T.llen`, `T.str_len`, `T.cat`), and TOON number tokens are clamped to 800 significant digits plus a sticky digit.
- round 5: the short-integer fast twin accepted 15-digit texts, and 281474976710656 (in `encnum_ints`) is past Nat's 2^48 − 1; the run with `TOON_SPEC=1` passed and the run without it failed, which is what the switch is for; the bound is 14 digits now.
- round 6 (report `docs/reviews/round-06.md`): the native binary hung on a closed stdin (DISC-007, launcher); `-o` files get mode 0644 (DISC-008); quadratic time in the keys of one object (repaired: hashed key carriers, EXP-004); exit codes on unwritable standard streams (DISC-003, DISC-006); a non-blocking stdin (DISC-009); a spec error in S9.20. Its claims review is answered item by item in the commits `1230a0d` and `e3f5540` (law counts and wording, board citations, fuzzers in `scripts/`, ledgers, plan amendments).
- round 7 (report `docs/reviews/round-07.md`, 2 HIGH): stdin was re-opened by PATH: an inherited offset ignored (wrong bytes, exit 0) and a socket refused (DISC-012, repaired by the stdin effect; DISC-009 RESOLVED by the same repair); list buckets quadratic on keys that collide in the 16 hash bits, and a chain walk per repeated key (repaired: AA-tree buckets, a map of last values; 7 cases); the runtime's resource floor (DISC-011); a second closed-descriptor hang (DISC-007 amended, launcher); three DISC texts corrected.
- round 8 (report `docs/reviews/round-08.md`, 0 HIGH): EBADF on stdin must end the input (repaired); read-only stdout/stderr (DISC-003, DISC-006, launcher); the cost of non-integer numbers (DISC-013); two launcher defects (repaired); DISC wording for the JavaScript build; S1.81's cluster rule (amended, 5 cases); a limit of `js-lane.py` (documented).
- round 9 (report `docs/reviews/round-09.md`, 0 HIGH, 1 MEDIUM, 2 LOW; 0 differences in conversion content in 947646 compared executions): a path under `/dev/fd` whose number the caller did not open reaches a descriptor of the runtime (DISC-014, ACCEPTED); DISC-007's description of the bare binary was incomplete (completed); thirteen impact lines of the register carried an old corpus size (refreshed). Three findings is not fewer than three: the round is recorded as NOT clean. No code changed.
- round 10 (report `docs/reviews/round-10.md`; 1 HIGH, 3 MEDIUM, 6 LOW; 0 behavioral differences between port and original in any lens): the documents cited commits (`d80251a`, `861abf2`, `4c3cccc`) that do NOT build from the public history, because `.gitignore` (`port/*.c`, `port/*.js`) kept the stdin effect's two twins untracked until `a725d10` (repaired: every gate line now names a buildable commit, and `scripts/clean-build-check.sh` builds and tests an exported commit); two README examples failed as written (fixed; every example was run); the launcher needed `dirname` from `PATH` (repaired: parameter expansion); `CONTRIBUTING.md`, `.github/CODEOWNERS` and `docs/PARITY_RUNBOOK.md` were unfilled scaffold files with false statements (filled); `c-1t` and `c-8t` are one sequential execution (now said wherever the lanes are named); stale counts in README, PORT_REPORT and this file; `perf/PERF-LEDGER.md` rows that its own admission rules refuse (moved to NE-005, provisional); AGENTS.md's build time and script provenance; the law file's header and the board's legend; a traceback of `pin-check.sh` in a clone without the oracle (now a RED row).
- round 13 (report `docs/reviews/round-13.md`; 0 HIGH, 1 MEDIUM, 8 LOW; **0 differences in conversion content in about 421000 compared executions** on the native, JavaScript, interpreter and launcher lanes): the MEDIUM is against this session's own repair — "1.389× the pinned build" was generalized into "flatters the port by about 1.4× on this workload", and the profile's advantage is input-dependent (re-measured here: 1.729× on wide rows, cv 2.0% / 4.2%, 42 samples; 1.389× on expand; REFUSED on the tabular encode), while the two README columns were captured hours apart with DIFFERENT port binaries and are not an interleaved pair (two attempts to re-capture the pinned side with the current binary were REFUSED_CV, cv 10.5% / 13.2%; bead `toon_bend-0i8`). The eight LOW: `scripts/ORIGIN.md` named seven edited scripts where ten differ from the skill's copies (rewritten from `git log`, and it now says so); three stale document numbers (the self-test line, `10 file(s)` for the claims lint, DISC-013's range in two more places); `claims-audit.py` checked only numbers WITH a decimal point and exempted any line naming `perf/evidence/`, so a row falsified to `9999 ms | 111 ms` passed (integers are checked now, the exemption is gone, and the reviewer's own falsification is caught: 3 of 3 values); `floor.sh` accepted the oracle under another basename and then reported 62 false UNSTABLE `usage_*` cases, because clap prints argv[0]'s basename (it refuses by name now, exit 2); NE-006's tally contradicted its own list; and — the one that mattered — **three of the reviewer's eight new law mutants SURVIVED the whole 368-law proof**: no law pinned writer B's escape table (S5.64), UTF-8 surrogate rejection (S2.2) or the TAB quoting rule (S4.12–S4.16). Six closed laws now pin them, the three mutants are permanent members of the set (M24, M25, M26) and each is KILLED by the law written for it. The port itself needed no change in any of the nine.
- round 12 (report `docs/reviews/round-12.md`; 0 HIGH, 2 MEDIUM, 11 LOW; 0 differences in conversion content in 13298 compared executions on four lanes): the launcher's round-11 repair spent the CALLER's descriptor 9, so `-e /dev/fd/9 9<file` failed where the original converts (repaired in `f45de76`: the stderr probe no longer opens 9; re-verified this session on both halves of the reviewer's repro); DISC-015's Resolution described the original wrongly in two of its own sub-cases (corrected in `713adfe`); and eleven statements in the documents that the repository itself refutes — the folding capture's refusal count, a REFUSED_CV count that no reading of `perf/evidence/` gives, "two commits after" for a commit that is the next one, three of five scripts that did NOT refuse a missing oracle although the report said each does, NE-003's cv range, CONTRIBUTING's "quoted in each entry" against DISC-009, `scripts/port.sh` still linting `docs/*.md perf/*.md README.md` (so its `claims` gate was RED on the published tree), DISC-013's range excluding its own largest measurement, three MEASURED ratios in no register at all, those ratios attributed to a commit that does not build, and the `opt-level=3` incumbent PLAN §2 promised and nobody had built. All thirteen are repaired: the three scripts refuse and exit 2 now, `port.sh claims` is `0 hit(s) in 11 file(s)`, the ratios have entry NE-006, and the `opt-level=3` incumbent exists — it is 1.389× the pinned build, which turns the published 3.06× into 1.989× and the published 1.27× into **0.773×, a loss**
- round 11 (report `docs/reviews/round-11.md`; 0 HIGH, 4 MEDIUM, 8 LOW; 0 differences in conversion content): the launcher asked `/dev/fd/2` whether stderr was open and lost every diagnostic where `/proc` is not mounted (repaired: `command exec 9>&2`); the SIZES of the reads on stdin are observable on packet pipes, SEQPACKET sockets and O_DIRECT files (DISC-015, ACCEPTED; two probe rows); PORT_REPORT's recipe named a commit older than the scripts it calls (repaired: the recipe names the newest commit of `main`); the README quoted capture numbers whose evidence files had not been committed (repaired by `b2ff04e`, before the report arrived; the README's numbers are generated from the committed files); the claims lint had four different file lists (one now); `stdio-probe.py` and `diff-fuzz.py` ended in a traceback without the oracle (a message and exit 2 now); the board's header carried a constant command text and date; seven stale cross-references; a README line that read a file that does not exist; statements about scripts that are not in the repository and a no-edit rule that was not kept (`scripts/ORIGIN.md` lists the seven local edits); three inconsistencies between documents; `TOON_BEND_BIN` naming a directory or an empty executable (the launcher refuses both).

Convergence (computed by `scripts/converge.sh docs/PORT_STATE.md`): T1 ≥ 3
rounds and ≥ 1 clean at the end; T2 ≥ 5 and ≥ 2 consecutive clean at the end and ≥ 1 non-author round; T3
≥ 10 with the last two rounds clean; a clean round has < 3 new genuine
findings and no unresolved finding from that round; a dirty or reopened
round resets the clean streak. Every OQ is resolved or excluded; no OPEN DISC. Current: NOT_CONVERGED (the last six rounds are not clean; no DISC and no OQ is open).

## Next action (one line, executable)

`BEND_CLI='bun /tmp/bend/bend2/main.ts' python3 scripts/hand-mutants.py` over all 25 (bead `toon_bend-sp5`), then dispatch round 14 as a fresh non-author reviewer against a clone of `origin/main` at the commit this file names (bead `toon_bend-807`).

Of the two gates that were STOPPED for host memory on 2026-09-20, `port-doctor.sh` has run: it is the GREEN table above, its bead is closed, and `clean-build-check.sh` PASSES on the commit that table names. The 25-mutant `hand-mutants.py` run is EXECUTING as this line is written (12 of 25 KILLED, no survivor yet); the law-admission note above still describes the earlier partial runs and is replaced by this run's JSON line when it ends. Until then, no claim in this file rests on a complete 25-mutant run.

Why round 14 is the one thing that moves the tier: `converge.sh` gives `clean_tail 0`, so T2 needs rounds 14 AND 15 both clean (< 3 new genuine findings each and none left unresolved). Nothing else in this file blocks convergence — no OQ and no DISC is open, the board is DEBT only through its six classed exclusions, and the four gates a reviewer re-runs are pasted above with the commit they were produced on.
