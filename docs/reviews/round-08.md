<!-- Round 8 of the parity gate's find-fix rounds (docs/PORT_STATE.md). The report of a NON-AUTHOR reviewer (a fresh subagent that
     did not write the port), reviewing the binary of commit d80251a's code; copied here VERBATIM from the session's scratch directory so that the
     convergence table rests on something a reader of the repository can open. Paths under /data/tmp/... are the reviewer's
     scratch files and are not part of the repository. What was done about each finding: docs/PORT_STATE.md and the commit
     that names the round. -->

# Round 8 — hostile, non-author parity review of toon_bend (port binary of commit d80251a)

Reviewer: fresh subagent; did not open `legacy/`, did not read the original's source. Date 2026-09-20.
ORIG = `/data/projects/toon_bend/oracle/toon`. PORT = `<scratchpad>/gate3/toon -- …` (native, default threads). SPEC = PORT with `TOON_SPEC=1`.
JS = `python3 scripts/js-lane.py <scratchpad>/gate3/toon.js -- …`. LAUNCHER = `bin/toon` with `TOON_BEND_BIN=<gate3>/toon`.
Everything I wrote is under `<scratchpad>/review_R8/` (scripts, logs, `work/`); nothing in the repository was touched; no file was deleted anywhere.
Harness: `review_R8/h.py` (stdout, stderr, exit code compared byte for byte; every difference is appended to `diffs_<lens>.jsonl`; all of those files are EMPTY).

Headline. On conversion CONTENT: ZERO byte differences in about 90,000 compared executions of my own (plus the repository tools' 15,000 fuzz inputs), about 82,000 of them aimed at the
round 7 repairs (AA-tree buckets, the map of last values, colliding / prefix / astral / escaped / repeated keys in every order,
both directions, strict, lenient, expansion, folding) and 1500 real JSON files found on this host. The new buckets also hold on TIME:
no shape of mine reaches the "> 1 s AND > 40x" bar through collisions or repeats (worst 35x at 3.4 s; on several shapes the original is the slow one). Round 7's two HIGH findings are really fixed on the native binary, through the launcher and on the JS build.
What is left is, again, at the edges: a descriptor that is OPEN but has the wrong access mode (the stdin effect and the launcher
both repair only CLOSED ones), the launcher's own path resolution, a time difference on float-heavy documents that the register
does not hold, and wording errors in DISC-003 / DISC-006 and in one spec clause.

## 1. Findings

Severity: HIGH = wrong bytes or exit code on plausible input; MEDIUM = wrong on contrived input, or a platform/resource/time
difference not in the DISC register; LOW = documentation / DISC / spec / harness inaccuracy. **HIGH: 0. MEDIUM: 4. LOW: 4.**

| id | sev | lens | minimal reproduction | original | port |
|---|---|---|---|---|---|
| R8-1 | MEDIUM | 2 (stdin) | fd 0 is OPEN but not readable (read gives EBADF). Shell: `toon -d 0>/dev/null` (fd 0 = `/dev/null` opened write-only). Python: `subprocess.run(CMD+['-d'], stdin=os.open('w.toon', os.O_WRONLY))`; also `O_WRONLY\|O_APPEND`, the WRITE end of a pipe (`os.pipe()[1]`), an `O_PATH` descriptor, and INPUT spelled `-`. Scripts: `l2_stdin.py`, `l2b.py` (7 rows) | reads an EMPTY input (Rust maps EBADF on stdin to end of file, the same rule the spec gives for a missing fd 0, S9.12): `-d` prints `{}`, exit **0**; `-e` prints `JSON error: Failed to parse JSON: EOF while parsing a value at line 1 column 0`, exit 1 | native, LAUNCHER and JS alike: stderr `Failed to read stdin: Bad file descriptor (os error 9)`, exit **1** (so `-d` differs in stdout, stderr and exit code; `-e` in stderr). Lane difference too: with an `O_PATH` descriptor the JS build equals the original and the native binary does not. Not registered: DISC-012's resolution says descriptor 0 "itself is read" like the original; DISC-007 and the launcher cover a CLOSED fd 0 only (`(exec 3<&0)` succeeds on a write-only descriptor, so nothing is re-opened). `scripts/stdio-probe.py` has no such row. For contrast (same descriptor, INPUT `/dev/stdin` or `/proc/self/fd/0`): both programs re-open the file by path and convert it: SAME |
| R8-2 | MEDIUM | 2 (descriptors) | fd 1 (or fd 2) is OPEN but not writable. Shell: `echo '[1,2]' \| toon -e 1</dev/null`; `echo '[1,2]' \| toon -e -o out.toon 2</dev/null`. Script: `l2_out.py` rows "stdout opened O_RDONLY", "stderr opened O_RDONLY, success line" | EBADF on stdout / stderr is ignored: exit **0**, silent (and `out.toon` written) | native, LAUNCHER and JS: exit **1**; with a read-only stdout stderr also gets `bend: a short write on a standard stream`. DISC-006 / DISC-003 list a CLOSED stdout/stderr and say the launcher then gives "the original's behavior exactly"; an open descriptor of the wrong mode is not listed, the launcher does not repair it, and the JS build (which DISC-006 says exits 0 with a closed stdout) exits 1 here |
| R8-3 | MEDIUM | 1/3 (time) | plausible float-heavy documents; generator `scale_num.py` (seed 88), `-e` then `-d` of the original's TOON; bytes identical in every row. `python3 -c "import random,json;r=random.Random(88);print(json.dumps([r.random()*10**r.randrange(-10,10) for _ in range(80000)]))" \| toon -e` | 80000 random doubles (1.69 MB): encode **0.043 s**, decode 0.104 s. 80000 `{"t":1700000000.123,"v":0.53…}` records (3.85 MB): 0.139 s. 80000 values like `123456e-150` (1.06 MB): 0.049 s / decode 0.242 s. 80000 coordinate pairs (2.0 MB): 0.128 s | the same inputs: **4.80 s (x113)**, decode 4.67 s (x45); **7.29 s (x53)**; **14.96 s (x306)** / decode 15.7 s (x65); 4.00 s (x31, under the bar). At n = 20000: 1.18 s (x154), 1.88 s (x54), 3.72 s (x180). Growth is linear (x4 per x4). `TOON_SPEC=1`: 1.48 / 2.58 / 3.73 s at n = 20000. JS build, 20000 doubles (0.42 MB): 7.1 s against 0.009 s. No `Performance` DISC registers it (DISC-011 is memory and address space); README:157 shows "9000 decimals: 153.1 ms against 3.9 ms" as a refused capture "for orientation only". Round 7 saw the same factor and waived it; by this round's bar (> 1 s and > 40x) it is a finding. Integers are fine (80000 ints: 0.14 s, x8) |
| R8-4 | MEDIUM | 2 (launcher) | the launcher reached through a symlink that lives outside the checkout, `TOON_BEND_BIN` unset: `ln -s <repo>/bin/toon ~/.local/bin/toon; echo '[1,2]' \| toon -e`. Reproduced without touching the repository in a simulated checkout (`l2h.py`: `work/sim/bin/toon` = byte copy of the launcher, `work/sim/port/toon` -> the gate3 binary, symlink in `work/l2launch/userbin/`) | `[2]: 1,2`, exit 0 | run by its own path: `[2]: 1,2`, exit 0. Through the symlink, or found through `PATH`: `toon: no binary at <dir of the symlink>/../port/toon (build it: …)`, exit **127**: `here=$(cd "$(dirname "$0")" && pwd)` is the symlink's directory, the link is never resolved. With `TOON_BEND_BIN` set every symlink / PATH / other-cwd spelling equals the original (19 executions, 0 differences) |
| R8-5 | LOW | 2 (launcher) | `TOON_BEND_THREADS` holding anything the runtime refuses: `0`, `abc`, `-1`, `2x`, `0x4`, `1.5`, `' '`, `--`, `'4 --help'`, an Arabic-Indic digit: `TOON_BEND_THREADS=0 bin/toon --version` | the variable means nothing to it: `toon 0.2.4`, exit 0; `--bogus` exits 2 | `bend: expected a thread count of 1 or more after --threads`, exit 1, for EVERY argv (`--version` 1 instead of 0, `--bogus` 1 instead of 2, a conversion 1 instead of 0). Accepted and equal to the original: empty, `1`, `2`, `8`, `128`, `129`, `1000000`, `18446744073709551617`, `+2`. The launcher's header documents the knob, not this failure mode; no DISC mentions it |
| R8-6 | LOW | DISC accuracy | DISC-003 "in every one of these situations the Bend runtime fail-stops with exit 1"; DISC-006 "`--help` / `--version` with stdout = `/dev/full` or closed → … the port exits 1 with the runtime line on both the C and JS lanes". Script `l2d.py` (fd closed with `preexec_fn`), JS also run directly under `bun` without the Python wrapper | stderr CLOSED: `--bogus` 2, `-e -o f` 0. stdout CLOSED: `--version` 0 | JS build, stderr CLOSED: **2** and **0**, equal to the original (not 1). JS build, stdout CLOSED, `--version` / `--help`: **0**, equal to the original (only `/dev/full` gives 1 on the JS build). Native, fds 1 AND 2 closed, `--bogus`: **2**, not 1 (both descriptors are taken by the runtime's pipe and the write "succeeds"). The two sentences are wrong for the JS build; `scripts/stdio-probe.py` itself reports these JS rows as FIXED |
| R8-7 | LOW | 4 (spec) | S1.81 "…or was followed by an escape, a long-option word or a short cluster of known characters"; S1.7/S1.8 (a cluster is processed left to right, an unknown character gives S1.77). Inputs: `-o -ex`, `-o -e5`, `-o -hx`, `--indent -ex`, `--indent -ox` against `-o -xe`, `-o -5e`, `--indent -xe` (`mini1.py`) | a pending value option followed by a cluster whose FIRST character is a known short option gives the missing-value error S1.81 even when a later character is unknown (`-o -ex` → `error: a value is required for '--output <FILE>' but none was supplied`); first character unknown → S1.77 (`-o -xe` → `unexpected argument '-x'`) | the port equals the original on all 15 spellings (30 executions). The SPEC text predicts S1.77 naming `-x` for `-o -ex`; no case in `goldens/cases.tsv` has a mixed cluster after a value option |
| R8-8 | LOW | harness | `scripts/js-lane.py` with a DIRECTORY as fd 0 (any row of a descriptor probe on the JS lane) | `Failed to read stdin: Is a directory (os error 21)`, exit 1 | through `js-lane.py`: `Fatal Python error: init_sys_streams: <stdin> is a directory, cannot continue`, exit 1: the Python wrapper dies before `bun` starts, so the lane reports a false difference (5 rows of mine). The JS build itself, started directly under `bun`, prints the original's line (`l2b.py`) |

Not findings, stated so nobody re-reports them: (a) every row of `l2_out.py` that creates a regular `-o` file differs ONLY in the
file mode 0644 / 0664 under this host's umask 002 (9 rows: DISC-008). (b) `CLICOLOR_FORCE=1`: styled against plain (DISC-005).
(c) SIGPIPE / EPIPE, `/dev/full`, EFBIG with SIGXFSZ ignored on stdout: only DISC-006's text differs, exit codes equal
(`bash -o pipefail`: `status=1 pipestatus=1 0` for all four programs with `head -c 10`, `head -c 70000`, `true`, `sleep 0.3`).
(d) One race of my own probe: an `O_NONBLOCK` pipe whose writer closes right after the spawn: the original starts within 1 ms and
still meets EAGAIN, the slower-starting port finds the pipe already closed and converts. With a 0.7 s pause (3 shapes x 3 lanes) all SAME.
(e) The original, not the port, is the slow program on: repeated `a:/b:` blocks merged under `--expand-paths safe` (32000 blocks: 37.6 s
against 0.77 s; 128000: > 300 s against 3.4 s), a tabular header of 128000 dotted columns (70 s against 7.0 s), astral-plane keys with
expansion (46 s against 3.0 s), 128000 members with a repeated key under folding (13.6 s against 3.1 s).

## 2. What I ran

All generators are in `review_R8/`, seeded and re-runnable. "executions" = port-side executions compared with the original.
Never more than 2 port processes at once; largest input fed to the port 12.8 MB; `free -g` never showed less than 11 GB available.

| lens | generator (seed) | compared executions | differences |
|---|---|---|---|
| 1 encode, JSON TEXT | `l1_enc.py` (811, 812, 813, 814): 10 key families: 40 + 24 ASCII keys colliding in the low 16 FNV bits; keys that are PREFIXES of each other AND collide (`p`, `p…`, `p……`); the same character in 5 planes (`A`, U+10041, U+20041 …: equal low 16 bits of every scalar, so whole strings collide); combining sequences (`e`+U+0301, U+00E9, `e`+U+10301); 300-character keys differing in the LAST character; the empty key with 6 colliders; dotted keys over colliding segments. Each key spelled raw, ASCII-escaped or fully `\uXXXX`-escaped (surrogate pairs), so keys are equal only after unescaping. Orders: ascending, descending, alternating min/max, random. Repeats: every key 2–4 times, interleaved repeats, one key 3–30 times, values changing type; at 4 depths, inside would-be tabular rows, in list items. Options: folding, every `--flatten-depth` 0–4 and 100, delimiters, indents, `--stats`. Port + SPEC, every 25th on JS; then the original's TOON decoded plain / expand / expand+lenient | 39,312 | 0 |
| 1 decode, TOON text | `l1_dec.py` (821, 822, 823) and `l1_dec_wide.py` (824, 825; pools of 6–14 keys): dotted paths of 1–4 segments over the same families, quoted and unquoted spellings of one key, raw non-identifier keys, repeated keys, nested blocks 3+ levels deep, tabular headers with dotted / quoted fields, list items with first-field blocks; each document under plain, expand, lenient, expand+lenient; every 40th on JS. Outcome mix (seed 824): 99.8% exit 0 plain, 41% exit 0 / 59% conflicts under strict expansion | 42,648 | 0 |
| 1 hand cases | `hand1.py`: 24 documents (duplicate keys inside an object that is later merged, before / after the path, three levels deep, quoted+unquoted+dotted triplets, duplicate tabular fields, type flips) x 4 option sets x native + JS | 192 | 0 |
| 1 time | `scale8.py` 25 shapes x n = 4000, 32000, 128000 (+3 shapes at 256000), `scale8b.py` (colliding keys that ALSO share a 500 / 2000 / 8000-character prefix, so every tree step pays the prefix: 24 rows up to 8.2 MB) | 102 timed and byte-compared rows (1 more where the ORIGINAL exceeded 300 s) | 0 byte differences; 0 rows over the bar. Worst rows at >= 1 s: long-prefix colliders 8.0 MB 3.39 s against 0.095 s (x35.5), 2.0 MB 0.92 s (x52, under the 1 s floor); 256000 plane keys random + repeated (11.9 MB) 9.7 s against 0.44 s (x22); non-colliding long-prefix control x16–x33 |
| 2 stdin | `l2_stdin.py`, `l2b.py`, `l2c.py`: pty (data + ^D, immediate ^D, no trailing LF, master hang-up → EIO); O_WRONLY / O_APPEND / O_RDWR-at-offset / O_PATH files; directory; `/dev/null` both modes; write end of a pipe; `/sys/kernel/notes`; FIFO opened O_RDWR (both HANG, killed after 3 s by my harness); 1-byte writes 20 ms apart through every UTF-8 character (pipe, stream socket); late-closing socket; SEQPACKET with an empty packet; DGRAM; listening and unconnected sockets (EINVAL, ENOTCONN); truncated UTF-8 then EOF; CR / LF split; 289 KB in 4093-byte writes; `head -c 8` first on the same pipe / file then `cat`; INPUT `-`, `/dev/stdin`, `/proc/self/fd/0`, `/dev/fd/0` x pipe, file at offset 3, socket, `/dev/null`, directory, O_WRONLY; O_NONBLOCK with partial data; 4-byte characters across 4096 … 2097152 byte boundaries on pipe / file / socket | 297 (native, launcher, JS) | R8-1 (7 rows x lanes); 5 false JS rows = R8-8; 1 race (dismissed) |
| 2 outputs and paths | `l2_out.py` (66 rows, each lane in its own twin directory, file TREE compared): `-o /dev/stdout` with stdout a pipe / a file holding text / stdout+stderr one file / O_APPEND; `/dev/stderr`, `/proc/self/fd/1`, `/dev/fd/2 --stats`; `-o -`, `-o ''`; in place (120 KB, and decode); through a symlink to INPUT; symlink loops as INPUT and as `-o`; dangling symlink; directory; trailing slashes; missing / read-only directory; read-only file; writable file in a read-only directory; `/proc/version`, `/sys/…`, `/dev/null/x`, `/proc/self/fd/9`; `-o` untouched on 8 kinds of failure; 260-byte names and 5000-byte paths; names with LF, quote, backtick, arrow, ESC, BEL, DEL; relative paths from a sub-cwd; extension detection oddities; FIFO as INPUT and as `-o` (15 B and 120 KB); `/dev/full` | 198 | R8-2 (2 rows x 3 lanes); 9 rows x 3 = DISC-008 mode only; rest 0 |
| 2 launcher | `l2_launch.py`, `l2h.py`, `l2e.py`, `l2d.py`, `l2f.py`: symlink, symlink-to-symlink with a space, PATH lookup, other cwd, `TOON_BEND_BIN` unset / empty / relative; 19 `TOON_BEND_THREADS` values x 3 argv; 12 environment variables x 4 argv; all 7 subsets of closed descriptors x 10 argv on launcher and JS (140: 0 differences); `/dev/full` on fd 1 / fd 2 | 322 | R8-4, R8-5, R8-6; DISC-005 (3 rows), DISC-003/006 `/dev/full` rows as registered |
| 3 signals, resources | `l3_sig.py` (reader exits early under `bash -o pipefail` x 4 readers x 4 programs; SIGPIPE default and ignored; SIGINT, SIGTERM, SIGHUP, SIGUSR1, SIGALRM, SIGQUIT while blocked on stdin: -2 / -15 / -1 / -10 / -14 / -3 everywhere; SIGSTOP + SIGCONT + SIGWINCH/SIGCHLD/SIGURG then data), `l3_kill.py` (SIGTERM / SIGINT at 30% and 60% of a conversion with `-o` on an EXISTING file, and inside the write phase: the file is untouched before the write phase in both programs; rc -15 / -2), `l3_fsize.py`, `l3_fsize2.py` (RLIMIT_FSIZE 0 … 100000, SIGXFSZ default → -25 everywhere, ignored → `File too large (os error 27)` for `-o`, C-7's silent loss reproduced) | 74 | 0 beyond DISC-006's text |
| 3 time, numbers | `scale_num.py` (seed 88) 6 shapes x n = 20000, 80000, encode and decode, + SPEC at 20000; `scale_num_js.py` | 31 | 0 byte differences; R8-3 |
| 4 spec clauses | `pick_clauses.py` (`random.seed(8)`; 427 clause ids of S1, S2, S4, S9; the 40 picked: S1.11 S1.16 S1.30 S1.81 S1.83 S1.87 S1.152 S2.3 S2.9 S2.41 S2.52 S2.105 S2.108 S2.120 S2.126 S2.133 S4.45 S4.48 S4.50 S4.55 S4.61 S4.62 S4.64 S4.103 S4.106 S4.113 S4.119 S4.120 S4.131 S4.142 S4.161 S4.217 S9.2 S9.13 S9.59 S9.61 S9.72 S9.105 S9.201 S9.211), `l4_spec.py`: 1713 boundary inputs not in `goldens/cases.tsv` (per-clause counts in the script's output: e.g. S1.30 59, S2.126 104, S4.45–64 432, numbers 281); port + SPEC, every 4th on JS; the original's answers are in `l4_orig_answers.jsonl`. S1.87 = DISC-004 (re-verified, 6 byte-argv rows); S9.13 is lens 2; S9.72 is unreachable. `mini1.py` (30), `mini2.py` (declared lengths up to 2^64−1: 60) | 3,944 | 0 port/original differences; 1 spec inaccuracy (R8-7) |
| 5 repository tools | see the JSON lines below | 15,000 fuzz inputs + 14 number batches (x 400 literals, x 2 switch settings) + 36 scale rows + 48 probe rows | 0 / 0 NEW |
| extra: real files | `l6_real.py` (861): 1500 distinct real `.json` / `.map` / `.webmanifest` files of 2 B – 300 KB found under `/usr/lib`, `/usr/share`, `/etc`, package caches (read only); random options; then the original's TOON decoded; every 5th under SPEC, every 30th on JS. Exit mix of the original: 1361 x 0, 139 x 1 (BOMs, comments, invalid JSON) | 3,211 | 0 |

Totals by lens: lens 1 = 82,254 executions, 0 differences, 0 rows over the time bar; lens 2 = 817, R8-1, R8-2, R8-4, R8-5 (+ R8-6, R8-8);
lens 3 = 105, 0 byte differences, R8-3; lens 4 = 3,944, 0 differences, R8-7; lens 5 = the repository's tools, all PASS; real files = 3,211, 0 differences.

JSON summary lines of the repository tools (seeds 801–806; I used `--jobs 1`, not 2, so that one slot stayed free for my own probes: the inputs are fixed by the seed):
```
{"lens": "mutate", "seed": 801, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "docs", "seed": 802, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "argv", "seed": 803, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "expand", "seed": 804, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "collide", "seed": 805, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "numbers", "seed": 806, "inputs": 14, "differences": 0, "too_slow": 0, "switch": "TOON_SPEC=1", "verdict": "PASS"}
{"lens": "scale", "seed": 1, "inputs": 18, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}          (native, --runs 16000)
{"lens": "scale", "seed": 1, "inputs": 18, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}          (JS build, --runs 16000 --slow-floor 5; 0.25 … 2.4 s per input)
stdio-probe, native:   {"rows": 16, "same": 8, "known": [8 rows: DISC-007 x2, DISC-006 x2, DISC-003 x4], "fixed": [], "new": [], "verdict": "PASS"}
stdio-probe, launcher: {"rows": 16, "same": 8, "known": ["DISC-006: stdout is /dev/full", "DISC-003: stderr is /dev/full, a conversion error", "DISC-003: stderr is /dev/full, a usage error"], "fixed": [5 closed-descriptor rows], "new": [], "verdict": "PASS"}
stdio-probe, JS build (--timeout 20): the same line as the launcher's: 8 same, 3 known, 5 "fixed", 0 new, PASS
```
The full lines and every command are in `review_R8/floor.log` (`TMPDIR` pointed into `review_R8/tmp`, so the probe wrote nothing to `/tmp`).

## 3. DISCREPANCIES.md: what I checked

- **DISC-009 (RESOLVED): really resolved** on the native binary, through the launcher and on the JS build. Probe row SAME x 3; my own rows: an `O_NONBLOCK` pipe with nothing, with `[1,` then a pause, with a complete document and an open writer: all four programs print `Failed to read stdin: Resource temporarily unavailable (os error 11)`, exit 1 (9 executions, 0 differences).
- **DISC-012 (RESOLVED): really resolved for what it lists**, on all three: regular file at an offset and at its end, the offset left for a following `cat` (also after `head -c 8` on the same pipe and the same file), stream / SEQPACKET / DGRAM sockets, late-closing and 1-byte senders, a descriptor of a file unreadable by path; `/proc/<pid>/fd` of the native binary shows descriptor 5 as a duplicate of descriptor 0. INPUT spelled `/dev/stdin`, `/proc/self/fd/0`, `/dev/fd/0` are path opens in BOTH programs (socket → `No such device or address (os error 6)` in both): SAME. **Its resolution text is incomplete**: a descriptor 0 that is open but unreadable is not read the way the original reads it (R8-1).
- **DISC-003, DISC-006: factually wrong for the JS build** (R8-6) and silent about open descriptors of the wrong access mode, which the launcher does not repair (R8-2). Their `/dev/full` rows, the EPIPE row and the launcher's closed-descriptor repair are accurate (re-verified; all 7 subsets of closed descriptors x 10 argv through the launcher and on the JS build: 140 executions equal to the original).
- **DISC-007**: accurate: bare native binary `-e <&-` / `-d <&-` → `Failed to read stdin: Resource temporarily unavailable (os error 11)`, exit 1; the second hang (fds 0+1 closed, 66 KB) is still there on the bare binary (probe: `HANG`); the launcher repairs both.
- **DISC-001** accurate (`--help`, `--threads 2 x.json`, `x.json --threads 2`, `--gpu off`, `--gpu-build` are consumed before `--`). **DISC-002** accurate (`tn` symlink: original `Usage: tn …`, `--version` `toon 0.2.4`; port always `toon`). **DISC-004** accurate (byte argv: `--indent`, `--delimiter`, `--flatten-depth` differ as written; `--key-folding`, unknown options and paths equal). **DISC-005** accurate. **DISC-008** accurate (664 against 644 under umask 002 in all 9 file-creating rows; content, truncation, in place, symlink targets, "untouched on error" equal). **DISC-011** accurate (`VmSize` 8592046824 kB, 2 threads; `RLIMIT_AS` 4 GiB → `bend: reservation failed` for every argv, 11 GiB works; `RLIMIT_NOFILE` 4 → event loop, 5 → `Too many open files` for stdin and for an INPUT file, 6 works). **DISC-010**: not run (memory), as the entry itself says.
- **Register gaps**: R8-1, R8-2 (Platform), R8-3 (Performance: time on float-heavy input), R8-4, R8-5 (the launcher is the registered mitigation of DISC-001/003/006/007, so its own failure modes belong beside them).

## 4. Audit trail

- No file deleted; no git command that changes state; nothing written inside the repository or under `/tmp`. Special files I created under `review_R8/work/`: FIFOs, unix sockets, symlinks (two loops, one to the original named `tn`, one to the port named `tp`), directories of mode 0555, files of mode 0 and 0444, a byte copy of `bin/toon` in a simulated checkout (`work/sim/`), files named `-`, `=x` and with control characters in their names.
- Processes ended by me: only children of my own harness, through `subprocess` timeouts (the two-program FIFO-O_RDWR rows, 3 s each) and the deliberate signals of lens 3. I signalled nothing I did not start.
- The safety hook refused one shell command (a variable-named executable followed by `-e`); I allowlisted nothing and drove every run from Python files.
- Not run, by instruction: `lanes.sh`, `port-doctor.sh`, `hand-mutants.py`, cargo, `bend update`, the interpreter lane, anything in `legacy/`; `All terms check.` is unverified by me. Not testable here: an immutable `-o` path (needs root), ENOSPC on a real file system, DISC-010's port half.
