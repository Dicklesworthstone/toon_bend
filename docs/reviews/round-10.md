<!-- Round 10 of the parity gate's find-fix rounds (docs/PORT_STATE.md). The report of a NON-AUTHOR reviewer (a fresh subagent that
     did not write the port), reviewing the PUBLIC repository at commit 20735c9 from a fresh clone; copied here VERBATIM from the
     session's scratch directory after the reviewer finished. Paths under /data/tmp/... are the reviewer's scratch files and are not
     part of the repository. What was done about each finding: docs/PORT_STATE.md and the commit that names round 10. -->

# Round 10 — hostile, non-author review of toon_bend: fresh clone, claims, terminals, threads, the interpreter lane (public commit 20735c9)

Reviewer: fresh subagent; did not open `legacy/`, did not read the original's source, edited nothing in `/data/projects/toon_bend`. Date 2026-09-20.
ORIG = `/data/projects/toon_bend/oracle/toon`. CLONE = `<scratchpad>/review_R10/clone` (`git clone https://github.com/Dicklesworthstone/toon_bend`, HEAD `20735c9`).
PORT = `<scratchpad>/review_R10/build/toon -- …`, built from the clone; its sha256 `27787b9b…cd76465` EQUALS the author's `<scratchpad>/gate4/toon`, and `build/toon.js` (`1a273ccb…`) equals `gate4/toon.js`: the build is reproducible bit for bit.
JS = `python3 -B CLONE/scripts/js-lane.py build/toon.js -- …`. LAUNCHER = `CLONE/bin/toon` with `TOON_BEND_BIN=build/toon`. INTERP = `CLONE/scripts/interp-lane.sh bun /tmp/bend/bend2/main.ts CLONE/port/main.bend -- …` (`diff -r CLONE/port /data/projects/toon_bend/port`: identical, so the clone's sources were run and the author's tree was only read).
Everything I wrote is under `<scratchpad>/review_R10/` (scripts, `logs/`, `work*/`, `tmp/` = my `TMPDIR`); nothing under `/tmp`; no file deleted anywhere; no git command that changes state outside my clone.

Headline. **Conversion content: ZERO byte differences in about 29,300 compared executions of my own** (interpreter lane off the corpus, threads 1…128, repeats, terminals, environments, my own generator at volume) plus the repository's 15,000 fuzz inputs, 20 number batches, 18 scale inputs and 66 probe rows, all PASS. The fresh clone builds, the binary is bit-identical to the author's, and every gate I was asked to run prints what the state documents say, with two exceptions (`converge.sh` against PORT_REPORT, `stdio-probe.py` against PORT_STATE/README). What I found is in what the repository SAYS: the commits that README, PORT_REPORT, PORT_STATE and the board name for their lane results cannot be built from the public history, one README example shows output that neither program prints, the contributor documents are unfilled scaffold text with false statements, and the launcher has one environment dependency the original does not have.

**HIGH: 1. MEDIUM: 3. LOW: 6.**

## 1. Findings

Severity as defined in the brief: HIGH = wrong bytes or exit code on plausible input, or a repository that does not build / a gate that does not pass as claimed; MEDIUM = wrong on contrived input, a platform/terminal/environment difference not in the register, or a false claim of substance; LOW = a stale or imprecise statement.

| id | sev | lens | reproduction or file:line | expected | observed |
|---|---|---|---|---|---|
| R10-1 | HIGH (a gate that does not pass as claimed; HEAD itself builds) | 1 | `docs/PORT_REPORT.md:63-69` "Reproduce (an auditor gets the same lines): `git checkout 4c3cccc` … `$BEND_CLI port/main.bend -o ./x`"; `docs/PORT_REPORT.md:1` (the certified commit is `4c3cccc`), `:21`, `:43`; `README.md:68` ("1060/1060 each at commit `d80251a`", "At `4c3cccc` … PASS 1065/1065"); `docs/PORT_STATE.md:19`; `docs/FEATURE_PARITY.md:10`, `:66-69`. Reproduction: `git -C clone archive 4c3cccc \| tar -x -C co_4c3cccc; cd co_4c3cccc; BEND_NO_TELEMETRY=1 bun /tmp/bend/bend2/main.ts port/main.bend -o x` (same for `d80251a`; `861abf2` has the same tree shape) | the commit a gate line names can be checked out and built, so that "an auditor gets the same lines" | `Error: ENOENT: no such file or directory, lstat 'port/./stdin_open.c'` (native build) and `… 'port/./stdin_open.js'` (interpreter: `bun … port/main.bend -- --version`). `port/main.bend:91-92` imports `./stdin_open.c` and `./stdin_open.js` since `d80251a`, but `.gitignore` (`port/*.c`, `port/*.js`) kept both out of git until `a725d10` (`git ls-tree d80251a port/`, `861abf2`, `4c3cccc`: 10 files, no twin). Every lane result the documents quote (the only four-lane line, 1060/1060 at `d80251a`; 1065/1065 on three lanes at `4c3cccc`; the proof line is not affected, `PROOF.bend` does not import `main.bend`) is therefore tied to a tree that the public history cannot reproduce; PORT_REPORT's own recipe stops at its first build. PORT_STATE's Next action (`:88`) knows it ("`a725d10` is `4c3cccc`'s plus the two effect twins that `.gitignore` had hidden"); README, PORT_REPORT and the board were not corrected. Mitigation I verified: HEAD `20735c9` builds (native 20.8 s, JS 2.5 s), both artifacts are sha256-identical to the author's gate4 builds (made before `a725d10`), and `git diff --stat 4c3cccc 20735c9 -- port` is exactly the two twins, so HEAD's code is `4c3cccc`'s |
| R10-2 | MEDIUM (false example output) | 2 | `README.md:121-122`: `printf 'users[2]{id,name}:\n  1,Alice\n  2,Bob\n' \| ./toon -- --decode --indent 0` with the shown result `# {"users":[{"id":1.0,"name":"Alice"},{"id":2.0,"name":"Bob"}]}` | the line the README shows, exit 0 | PORT and ORIGINAL alike: stdout empty, stderr `Validation error at line 2: Indentation not allowed when indent size is 0, but found 2 spaces`, exit **1** (`--indent` is also the indent unit expected in the input, as `README.md:229` itself says). Same trap at `README.md:207` (`./toon -- data.toon --expand-paths safe --indent 4`): on the TOON that the README's own encode examples produce it prints `Validation error at line 2: Indentation must be exact multiple of 4, but found 2 spaces`, exit 1. (`printf 'a: 1\n' \| toon -d --indent 0` → `{"a":1.0}` does work: only unindented input can be decoded with `--indent 0`.) The other README examples I ran (lines 30, 32, 115, 125, 126, 129, 204, 205, 206, 208 and the five format blocks of 85-108) work and print what is shown |
| R10-3 | MEDIUM (environment difference, not registered) | 3 | the LAUNCHER with a `PATH` that holds no coreutils. `cd review_R10/work3/launcher_path; echo '[1,2]' \| env -i PATH= TOON_BEND_BIN=$PWD/sim/port/toon /bin/sh sim/bin/toon -e` (also `PATH=/nonexistent`; `sim/` = byte copy of `bin/toon` + the built binary at `sim/port/toon`); second row the same without `TOON_BEND_BIN`: `echo '[1,2]' \| env -i PATH= sim/bin/toon -e` | the original, same environment: stdout `[2]: 1,2`, stderr empty, exit 0 (it needs nothing from `PATH`); `PATH` UNSET is fine for both (sh falls back to its default path) | row 1: stdout `[2]: 1,2`, stderr `sim/bin/toon: 22: dirname: not found`, exit 0 (an extra stderr line on EVERY argv: 32 of 32 rows of my `PATH empty` environment, pty and pipe). Row 2: stderr `…dirname: not found` + `toon: no binary at <cwd>/../port/toon (build it: …)`, exit **127**: `here=$(… cd -- "$(dirname -- "$self")" && pwd)` yields the CURRENT directory when `dirname` is missing, and `\|\| exit 127` does not fire. `bin/toon:22` (and `readlink` at `:17` for symlinks). DISC-001/003/006/007 name the launcher as "the supported command line"; no entry mentions that it needs external commands. The bare binary and the JS build are unaffected (0 differences under `PATH=`, `env -i`, `HOME` unset) |
| R10-4 | MEDIUM (false statements in published contributor documents) | 2 | `CONTRIBUTING.md:61` "CI (`.github/workflows/port-gates.yml`) runs the same gates"; `:51` "the fast twin behind `Toon_SPEC=1`"; `:22` "Add a row to `goldens/cases.tsv`"; `:15` "**spec == fast** is law-proved: every fast twin is bound to its spec twin"; `:35` "the law owner (`<name>`)", `:44` "approved by `<approver>`"; `.github/CODEOWNERS:5-13` owners `<@harness-keeper>`, `<@law-owner>`, … ("Replace the <handles>"; `:13` assigns `.github/workflows/`, which does not exist); `docs/PARITY_RUNBOOK.md:24,83,100` `--switch Toon_SPEC=1` | documents that describe this repository | `.github/` holds only `CODEOWNERS`: no workflow exists, nothing runs any gate in CI. The port reads `TOON_SPEC` (`port/main.bend:199`), so `Toon_SPEC=1` selects nothing. `goldens/cases.tsv:2` says "GENERATED … Do not edit" and `AGENTS.md` "A case is added by editing a generator, never the TSV": following CONTRIBUTING makes `python3 cases/build-cases.py --check` report drift. `:15` contradicts `README.md:49` and `port/LAWS.bend:150-160` ("No universally quantified `fast == spec` law exists"): the very wording round 6 had removed from the README. All three files are Phase-0 scaffold output (`git log -- CONTRIBUTING.md .github/CODEOWNERS` → `c4a5235` only) that was never filled; `claims-lint.sh` is never pointed at them |
| R10-5 | LOW | 2 + 4 | `README.md:46` "judged against 1065 captured cases on four executor lanes", `:179` "1065 captured cases x 4 lanes", `:68` "`docs/PORT_STATE.md` has the four-lane line for that commit [`4c3cccc`]" | a four-lane result for the 1065-case corpus, and four lanes that are four different executions | (a) no interpreter-lane result for 1065 cases exists in the repository: `PORT_STATE.md:19` holds the four-lane line of `d80251a` (1060 cases) and `:26` says the run at `4c3cccc` "was started"; `PORT_REPORT.md:21` "in progress when this was written". (I ran the 5 cases added at `4c3cccc` on the interpreter: PASS 5 of 5, so the hole is in the record, not in the port.) (b) `c-1t` and `c-8t` are ONE execution under two labels: the native binary runs on 2 OS threads whatever `--threads` says. `python3 review_R10/l4_fds.py`: `/proc/<pid>/task` = 2 for `--threads` absent, 1, 2, 8, 64, 128, 1024; `l4_osthreads.py` samples every 50 ms DURING a 0.8 s conversion (30000-row table): thread counts seen {1, 2} at `--threads 1`, 8 and 64, equal CPU seconds (0.81 / 0.83 / 0.89). Consistent with "no bang is placed" (`AGENTS.md`), so a difference between the two native lanes cannot occur; "every lane … C at 8 threads" adds no evidence beyond C at 1 thread (my 2,100 thread-count executions, 0 differences, say the same thing the cheap way) |
| R10-6 | LOW (stale at HEAD `20735c9`, which records round 9) | 1 + 2 | `README.md:69` "20 descriptor states … Three non-author review rounds added about 210000 compared executions; they found … 25 other findings"; `:72` "the last three were dirty … The eleven divergences"; `:309` "Platform divergences, all OPEN pending the owner's approval …: DISC-001 … DISC-006"; `:320` "Three non-author review rounds … Eleven divergences"; `PORT_REPORT.md:1` "commit 4c3cccc", `:12` "the three non-author rounds found 6, 11 and 8", `:26` "11 ACCEPTED", `:31` the pasted `converge.sh` line `"rounds": 8`, `:58` (no DISC-014); `PORT_STATE.md:33` stdio-probe "(20 descriptor-state rows …) → native binary `{"rows":20,"same":10,"known":10,…}`; through `bin/toon` 10 SAME, 7 … repaired, 3 KNOWN"; `PORT_STATE.md:40` (ACCEPTED list without DISC-014) | the numbers of the commit that publishes them | `DISCREPANCIES.md`: 12 ACCEPTED (DISC-001…008, 010, 011, 013, 014), 2 RESOLVED, 0 OPEN: README:309 is false, not only stale. Four non-author rounds (6, 11, 8, 3 = 28 findings; round 9 alone 947,646 executions: `PORT_STATE.md:64-67`). `./scripts/converge.sh docs/PORT_STATE.md` prints `"rounds": 9` (PORT_REPORT pastes 8 under "computed, never asserted"). `stdio-probe.py` prints `"rows": 22, "same": 11` and `"known"` as a LIST of 11 (native), 4 (launcher), 6 (JS): PORT_STATE's line is a paraphrase with other numbers under the heading "paste, do not paraphrase" |
| R10-7 | LOW | 2 | `perf/PERF-LEDGER.md:8` "Row admission (all mandatory)", `:15-16` rule 2 (interleaved both orders, A/A pair, cv ≤ 5% "or the row is refused"), `:24` rule 6 (CPU model in the fingerprint), `:26-27` rule 7 (evidence dir with the emitted C and keep-audit before and after), `:3-6` "Only WIN rows live here"; rows `:43-45`; `:45` "> 78×"; `README.md:138` "Every number here is a capture by `scripts/incumbent-bench.sh`: AB/BA pairs, medians, a cv gate" | rows that meet the ledger's own rules; ratios that recompute | the three rows have a baseline of "ONE run … no cv" and no A/A arm (they say so themselves), so by rule 2 they are refused rows in a WIN-only table; `perf/evidence/` holds 24 JSON files and nothing else (no emitted C, no keep-audit output); `"cpu": ""` in the fingerprint of all 20 bench JSONs (the CPU model is in the ledger text only). 60.0 s / 772.3 ms = 77.7, so "> 78×" overstates the bound (310× and 87× recompute). README:138 is contradicted by its own table below it: the column "build of `3751630` (one run)" (20.5 s, 6.5 s, > 60 s, from `EXP-004.baseline-*.json`, a single timed run) and round 7's 21 s / 50 s are not incumbent-bench captures. Everything else recomputes: all 14 MEASURED ratios equal median/median of their JSON (1.6661, 1.5580, 2.1249, A/A 1.0012 / 0.9945 / 1.0042, 0.2527, 1.2718, 3.0584, regress 0.9971 / 0.9918 / 0.9938 / 1.0224), every cv and median the README prints, the six REFUSED medians, "7 to 48 percent" (7.6 … 47.9), byte sizes 167255 and 76989, the three stdout shas. Not recomputable: cv and medians themselves (the JSONs store no samples) |
| R10-8 | LOW | 1 + 2 | `AGENTS.md:578` "Bend builds … take about a second"; `AGENTS.md:126`, `:136` (the CRITICAL compiler-check block runs `~/.claude/skills/bend2-mega-skill/scripts/bend-check.sh port` and `…/explain-error.sh`); `README.md:35` "Linux and macOS"; `PORT_STATE.md:75-78` (the four review reports under `scratchpad/review_R*/REPORT.md`), `perf/evidence/*.json` (`cmd` = binaries under the author's scratchpad) | a fresh clone has what the documented commands need; statements have evidence | native build from the clone: 20.8 s and 26.7 s wall (27 s and 32 s CPU) on this host, JS build 2.5 s. The two skill scripts are outside the repository (a fresh clone cannot run AGENTS.md's compiler-check block as written; `scripts/bend-quiet.sh` exists, `bend-check.sh` does not). No macOS run is recorded anywhere (every fingerprint is `Linux-…x86_64`), and the launcher's wrong-mode repair reads `/proc/self/fdinfo` (Linux only): UNVERIFIABLE. The review reports that the convergence table rests on, and the baseline binaries of every perf capture, are not in the repository: UNVERIFIABLE by an outsider |
| R10-9 | LOW | 2 | `port/LAWS.bend:3` kind "fast == spec   binds every fast twin to its spec twin (none exists before Phase 5)"; `:8-10` "What does NOT belong here: … whole documents through the JSON reader: those stay in goldens/ and conform.sh"; `docs/FEATURE_PARITY.md:8` "Statuses: present \| present \| missing \| excluded \| n/a"; `:10` "Last gate run … at commit `d80251a` … 368 laws" | a header that describes the file | the file holds 295 `golden_<case>` laws that run whole documents through `C.run_pure` (the JSON reader included), and its own section at `:148-160` says no law binds any fast twin to its spec twin for every input. The board's status legend names `present` twice (`partial` is missing); `d80251a` has 362 laws (`git show d80251a:port/LAWS.bend \| grep -c '^law '`), 368 is `4c3cccc` and later |
| R10-10 | LOW (harness) | 1 | `./scripts/pin-check.sh docs/PIN.toml` in a fresh clone (no `oracle/`, by design) | a RED or YELLOW table that says the oracle is absent | a Python traceback ends the run: `FileNotFoundError: [Errno 2] No such file or directory: 'oracle/toon'` (`scripts/case_manifest.py:64` via `command_provenance`), exit 2, and the row `manifest_hashes` is RED with an EMPTY note, although all 3195 MANIFEST hashes do match the golden files (I recomputed them: 0 mismatches). Not one of the gates I was asked to run; noted because a clone user meets it first |

Not findings, stated so nobody re-reports them:
(a) On a terminal the original styles help and errors and the port does not: 1,583 of my 7,955 terminal/environment rows differ ONLY in ANSI sequences (DISC-005, as written); 192 `argv[0]` rows differ only in the program name of `Usage:` (DISC-002: clap prints the FILE NAME of `argv[0]`, e.g. `x` for `x/`, `to⏎on` with the newline kept). No wrapping: clap does not wrap this help at widths 1, 40, 80, 200 or with `COLUMNS=40/200/0/x`.
(b) The 'HANG' cells of my first controlling-terminal run were a reaping race in MY harness (the original "hung" as often as the port); fixed, rerun: 0 differences in 54.
(c) `pin-check.sh` is RED in any clone because the oracle is deliberately absent; only the traceback is R10-10.
(d) `git log --diff-filter` on `goldens/`: no `.out`/`.err`/`.exit` file was ever modified after its capture (only `cases.tsv` and the two MANIFEST files change), which supports "every re-capture added hashes and changed none".

## 2. What I ran

Seeds and scripts are in `review_R10/` and re-runnable; per-lens difference files are `logs/diffs_*.jsonl` / `logs/l3_*.jsonl` / `logs/l4_*.jsonl`. "executions" = port-side executions compared with the ORIGINAL (or with a golden captured from it). My parallelism: never more than 2 jobs of mine, except the one 8-process concurrency row; largest input fed to the port 7.0 MB; `free -g` showed 13–15 GB available at every sample.

### Lens 1 — fresh clone (commands run in CLONE, `TMPDIR` inside review_R10)

| command | verbatim last line | as documented? |
|---|---|---|
| `bun /tmp/bend/bend2/main.ts port/main.bend -o <build>/toon` | (no output; binary 842032 bytes, sha256 = gate4/toon) | builds; README example prints `users[2]{id,name}:` / `  1,Alice` / `  2,Bob` |
| `… -o <build>/toon.js` | (no output; sha256 = gate4/toon.js) | builds (README names no JS build command; `scripts/lanes.sh:141` is the only place that shows `-o X.js`) |
| README quick build, literally: `… -o port/toon && bin/toon --encode < users.json` | the three README lines, exit 0; `bin/toon --help` = the original's help | yes |
| `./scripts/conform.sh goldens/cases.tsv goldens --lane c-1t -- ../build/toon --threads 1 --` | `{"lane":"c-1t",…,"passed": 1065, "failed": 0, "inconclusive": 0, "stderr_compared": true, "verdict": "PASS", "oracle_identity_checked": true}` | yes (also with `TOON_SPEC=1`: PASS 1065) |
| the same, `--lane c-8t … --threads 8 --` | `…"passed": 1065, "failed": 0,…"verdict": "PASS"…` | yes (also with `TOON_SPEC=1`) |
| the same, `--lane js --timeout 20 -- python3 scripts/js-lane.py ../build/toon.js --` | `…"passed": 1065, "failed": 0,…"verdict": "PASS"…` | yes (also with `TOON_SPEC=1`) |
| extra: `--lane launcher -- bin/toon` (`TOON_BEND_BIN` set) | `…"passed": 1065, "failed": 0,…"verdict": "PASS"…` | not documented; passes |
| sanity: `--lane bogus -- /bin/echo` | `…"passed": 0, "failed": 1065,…"verdict": "FAIL"…` | the gate does bite |
| `python3 cases/build-cases.py --check` | `{"cases": 1065, "verdict": "OK"}` | yes |
| `./scripts/cases-lint.sh goldens/cases.tsv` | `{"cases": 1065, "errors": 0, "notes": 20, "classes_missing": "", "verdict": "OK"}` | yes |
| `python3 scripts/spec-lint.py docs/EXISTING_Toon_STRUCTURE.md goldens/cases.tsv` | `spec-lint: 703 clauses, 1065 cases, 1065 cases cited, 0 finding(s)` | yes |
| `./scripts/law-coverage.sh` | `{"laws": 368, "proofs": 368, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | yes |
| `./scripts/parity-board.sh docs/FEATURE_PARITY.md` | `{"rows": 33, "present": 27, "partial": 0, "missing": 0, "excluded": 6, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | yes |
| `./scripts/state-check.sh docs/PORT_STATE.md` | `state-check: 0 finding(s)` | yes |
| `./scripts/converge.sh docs/PORT_STATE.md` | `{"tier": "T2", "rounds": 9, "clean": 5, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": [], "verdict": "NOT_CONVERGED", "missing": ["clean rounds since last reset 0 < 2"]}` (exit 1) | PORT_STATE: yes. **PORT_REPORT.md:31 pastes `"rounds": 8`: differs** (R10-6) |
| `./scripts/claims-lint.sh README.md docs/PORT_REPORT.md docs/PORT_STATE.md docs/DISCREPANCIES.md docs/OPEN_QUESTIONS.md perf/*.md` | `claims-lint: 0 hit(s) in 9 file(s)` | yes |
| `python3 scripts/port-lint.py port/*.bend --laws port/LAWS.bend` | `{"files": 10, "findings": 41, "errors": 0, "warnings": 0, "infos": 41, "by_rule": {"PL-02": 41}, "laws": "port/LAWS.bend", "verdict": "OK"}` | yes |
| `(cd port && BEND_NO_TELEMETRY=1 bun /tmp/bend/bend2/main.ts PROOF.bend)` | `All terms check.` (the ONLY line; 4 min 27 s, 339 s CPU) | yes (documented 4 min 04 s) |
| `./scripts/pin-check.sh docs/PIN.toml` (not in my list) | `pin-check: RED` + traceback | RED is expected without the oracle; the traceback is R10-10 |
| historical commits: `git archive 4c3cccc` / `d80251a`, then the README build | `Error: ENOENT … 'port/./stdin_open.c'` | **R10-1** |
| built binary against the author's `gate4/toon`, 300 corpus cases sampled with seed 1003 (`l1_same300.py`) | `{"cases": 300, "differences": 0}` (the two files are byte-identical anyway) | — |

Compared executions in lens 1: 1065 × 7 conform runs (7,455) + 300 = 7,755; differences 0. `git status --short` in the clone stayed empty after every gate (the builds went to `review_R10/build/`; `port/toon` is ignored).

### Lens 2 — claims audit (what was checked, and how)

Checked by script, 0 false: 368 laws = 14 quantified + 59 closed unit + 295 `golden_` (counted from `port/LAWS.bend` by `for` clauses); 368 proofs; **all 295 golden laws equal their case byte for byte** in argv, stdin, stdout, stderr and exit code (`check_golden_laws.py` + a second parser for the 11 byte-list / `Nil{}` forms; `golden_collision_keys_encode` is case `encstr_keys_hash_collision`); board: 76 def citations, 152 case citations, 451 law citations, every one exists (`check_board.py`), all 368 laws cited; every `DISC-`, `NE-`, `EXP-` id and every bead id cited in any document exists; every repository path cited in README, AGENTS, PORT_STATE, PORT_REPORT, DISCREPANCIES, the board and `perf/*.md` exists except `.github/workflows/port-gates.yml` (R10-4); AGENTS.md's project tree: every entry exists; MANIFEST: 3195 hashes, 0 mismatches against the golden files, oracle sha256 = PIN.toml = MANIFEST; 1065 cases = 716 hand + 349 fixtures; 703 clauses; 22 mutants in `hand-mutants.py`; every def in `port/*.bend` has a clause tag above it (0 untagged); `rg '\(~' port/` empty; `@unsafe` 0; `IO` only in `main.bend`; `import Base` + the two effect twins only; corpus growth 1005 → 1053 → 1060 → 1065 equals the git history; `perf/gen-bench-inputs.py --check` OK and the four `.toon` bench inputs equal the original's encodings; every ratio, median, cv, A/A value, byte size and sha in README's performance section and in PERF-LEDGER / NEGATIVE-EVIDENCE / EXPERIMENTS against `perf/evidence/*.json` (the "40% / 36% / 53% below" of the cards recompute). README usage examples: 17 commands run (12 command lines + 5 format blocks), 1 false output and 1 that fails on the README's own data (R10-2). About 120 statements checked; false, stale or unverifiable: the ones in R10-1, R10-2, R10-4 … R10-9.

### Lens 3 — terminals and environment (`l3_pty.py`, `l3_ctty.py`, `l3_js_envi.py`, `l3_bigenv.py`)

Every row runs the ORIGINAL and the lane with the same descriptors, window size, environment and `argv[0]`; outputs are compared raw, then with CSI sequences removed (= DISC-005), then with the program name normalized (= DISC-002, `argv[0]` rows only).

| part | what | executions | differences beyond DISC-002 / DISC-005 |
|---|---|---|---|
| size | 20 argv (`--help`, `-h`, `--version`, `-V`, 6 usage errors incl. a similarity tip, missing file, JSON error, TOON count error, expansion conflict, success to stdout, success line with `-o`, `--stats`) x pty on stdout / stderr / both (two ptys) / both (one pty) x window 40x24, 80x24, 200x50, 0x0, 1x1, size never set; plus `COLUMNS` = 40, 200, 0, x, empty with `LINES=5`; native, native `--threads 1`, launcher, JS | 2,320 | 0 (820 rows ANSI only) |
| env | 37 environments x 16 argv x {one pty for both streams, pipes}: `TERM` dumb / unset / empty / unknown, `NO_COLOR` 1 / empty, `CLICOLOR` 0 / 1, `CLICOLOR_FORCE` 1 / 0 and with `NO_COLOR` / dumb, `LANG=C`, `LC_ALL` C / en_US.UTF-8 / a missing locale / tr_TR, `LANG` unset, `HOME` unset / nonexistent, `PATH` unset / empty, `TMPDIR` unset, 100 x 1000-byte variables, one 100 KB variable, `TOON_SPEC` 0 / 1 / empty / true / 01, `RUST_BACKTRACE`, `BEND_*`, `NODE_OPTIONS`, `POSIXLY_CORRECT`, `COLORTERM` + `FORCE_COLOR`, malloc knobs, the EMPTY environment (`env -i`) | 4,640 | **32 = R10-3** (launcher, `PATH` empty); 0 elsewhere (665 rows ANSI only) |
| JS under `env -i` | bun by absolute path with only the wrapper's two variables; 10 argv x 4 environments | 40 | 0 |
| huge environment | 1.0 MB and 1.9 MB (100 KB, 120 KB and 1 KB variables), `TOON_SPEC=1` last; native, launcher, JS | 36 | 0 |
| argv[0] | 17 names through `executable=` (`''`, `toon`, `tn`, `/x/y/toon`, `./toon`, `-toon`, `a b`, `tóon`, 4096 x `a`, `--help`, `--`, `-h`, `toon.js`, a name with LF, `%s%n`, `x/`, `/`) x 11 argv x {pipes, pty}; native and `--threads 1` | 748 | 0 (192 rows name only, of which my classifier mislabelled the 16 `x/` rows as DIFF: clap prints `x`; 74 ANSI only) |
| one terminal for stdin + stdout + stderr | typed document + ^D, immediate ^D, invalid JSON, `--stats`, help, usage error, no mode flag; widths 40 / 80 / 200; 4 lanes | 108 | 0 (24 ANSI only) |
| controlling terminal (`pty.fork`) | `-o /dev/tty`, INPUT `/dev/tty`, both, `--stats -o /dev/tty`, ^D twice without a newline, ^C (exit by SIGINT everywhere); widths 40 / 80; native, launcher, JS | 54 | 0 |
| launcher without coreutils | `PATH=` / `PATH=/nonexistent` / normal / unset, with and without `TOON_BEND_BIN` | 9 | R10-3 (4 rows) |

Lens 3 total: 7,955 executions; 1 finding (R10-3, 36 rows); no hang, no exit-code difference, no wrapping difference anywhere else.

### Lens 4 — threads and determinism (`l4_threads.py`, `l4_fds.py`, `l4_osthreads.py`, `l4_pairs.py`)

| part | what | executions | differences |
|---|---|---|---|
| small | 180 cases of my generator (sample seed 4001 from seeds 10000–15999; both directions, every option, errors, `-o` files in twin directories) x `--threads` 1, 2, 3, 7, 8, 16, 64, 128; stdout, stderr, exit code and the files left behind against the original | 1,440 | 0 |
| large | 20 inputs of 0.12 – 7.0 MB (30000-row table both directions, 100000 strings, 150-deep wide objects with folding, 20000 list items both directions with expansion, 20000 folded / expanded keys, 6000 floats both directions, 200000 integers with `--stats`, escape-heavy Unicode both directions, a 7.0 MB table, a JSON error in the last bytes of 2.9 MB, a count error and its `--no-strict` run on 1.4 MB, 50000 inner arrays, 1000 nesting levels, 3.8 MB pretty-printed JSON) x the same 8 thread counts | 160 | 0 (port 0.05–3.4 s per run, flat across thread counts: see R10-5 b) |
| repeat | each of the 20 large inputs 25 times at `--threads 8` | 500 | 0 (one distinct (exit, stdout sha, stderr sha) per input, equal to the original's) |
| conc | 8 conversions started at once (4 with `-o`), against their sequential runs and the original (the processes are a few ms each, so this is a weak test, run ONCE as allowed) | 8 | 0 |
| pairs | 20 pairs of the LARGE inputs, two conversions running at the same time (0.1–3.4 s each, so they really overlap), the second of each pair writing `-o`; streams, exit codes and the output files against the original | 40 | 0 |

Lens 4 total: 2,148 executions, 0 differences.

### Lens 5 — the interpreter lane off the corpus (`gen.py`, `l5_interp.py`, `l5b_edges.py`, `l5d_corpus5.py`)

Budget: 600 interpreter runs = 2 smoke runs + 589 random cases + 1 run cut off when I stopped the sample + 5 corpus cases + 3 edge shapes. The random cases are the first 589 of `random.Random(1001).sample(range(6000), 600)` applied to `gen.case(seed)`: JSON written as TEXT (three escaping styles, repeated keys, 60 number spellings, 65 nasty strings, 41 nasty keys, tabular / almost-tabular / nested / chain shapes, random whitespace), broken JSON (truncation, inserted bytes incl. invalid UTF-8, BOM, bad numbers, doubled and trailing commas, single quotes, depth 130 and 200), TOON made by the ORIGINAL under random encode options and then mutated (12 line and byte mutations, CRLF), hand-assembled TOON lines (76 forms), argv from every option with its spellings, 41 usage-error shapes, stdin / `-` / files with right, wrong and upper-case extensions, `-o` in 7 spellings (twin directories; the files left behind are compared). Each case ran on ORIG, INTERP (about 5 s each) and the native binary.

| part | runs | exit mix (original) | differences interpreter vs original | native vs original |
|---|---|---|---|---|
| random sample, seed 1001 | 589 | 361 x 0, 173 x 1, 55 x 2 | 0 (streams and files) | 0 |
| the 5 cases added at `4c3cccc` (no interpreter result recorded for them) against their goldens | 5 | 5 x 2 | 0 | — |
| edge shapes: 3000 TOON nesting levels (4.5 MB), a 450 KB string with escapes, a 2000-field tabular header | 3 | 3 x 0 | 0 (interpreter 10.1 s, 6.0 s, 5.3 s; native 0.6 s, 0.08 s, 0.01 s) | 0 |
| the same generator at volume, seeds 100000–107999, native + every 5th under `TOON_SPEC=1` + every 12th on JS (`l5c_gen_native.py`) | 10,266 | kinds: 2635 enc, 729 broken JSON, 1435 + 1109 + 1535 decode, 557 usage | — | 0 |

### Lens 6 — the repository's own tools, run LAST as a floor (`logs/floor.log`, `logs/probe.log`; port = `gate4/toon`; `python3 -B`, `TMPDIR` in review_R10)

I ran the fuzz lenses with `--jobs 1`, not 2, because my interpreter lens occupied the other slot (the inputs are fixed by the seed).
```
{"lens": "mutate", "seed": 1011, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "docs", "seed": 1012, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "argv", "seed": 1013, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "expand", "seed": 1014, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "collide", "seed": 1015, "inputs": 3000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "numbers", "seed": 1016, "inputs": 20, "differences": 0, "too_slow": 0, "switch": "TOON_SPEC=1", "verdict": "PASS"}          (--runs 4000)
{"lens": "scale", "seed": 1, "inputs": 18, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}                      (--runs 16000)
stdio-probe, native:   {"rows": 22, "same": 11, "known": [11 rows: DISC-006 x3, DISC-003 x5, DISC-014 x1, DISC-007 x2], "fixed": [], "new": [], "verdict": "PASS"}
stdio-probe, launcher: {"rows": 22, "same": 11, "known": ["DISC-014: -o /dev/fd/3, which the caller did not open", "DISC-006: stdout is /dev/full", "DISC-003: stderr is /dev/full, a conversion error", "DISC-003: stderr is /dev/full, a usage error"], "fixed": [7 rows: 2 read-only, 5 closed-descriptor], "new": [], "verdict": "PASS"}
stdio-probe, JS (--timeout 20): {"rows": 22, "same": 11, "known": [6 rows: DISC-006 x2, DISC-003 x3, DISC-014 x1], "fixed": [5 closed-descriptor rows], "new": [], "verdict": "PASS"}
```

## 3. Register entries and documents re-checked

- **DISCREPANCIES.md** (`disc_recheck.py`, the probe, lens 3): DISC-001 accurate (bare `--help` prints the runtime's usage; `x.json --threads 2` is consumed and converts, the original reports an unexpected argument). DISC-002 accurate, and complete for every `argv[0]` I tried (the name is the last path component; the port always prints `toon`; `--version` is `toon 0.2.4` in both). DISC-003 / 006 / 007 accurate as far as the probe's 22 rows go (0 NEW on three lanes). DISC-004 accurate (`--indent $'\xff'`: `invalid UTF-8 was detected` + Usage against `invalid value '�'`, both exit 2). DISC-005 accurate: across 1,583 ANSI-only rows the text between the sequences is identical, under `NO_COLOR`, `CLICOLOR`, `CLICOLOR_FORCE`, `TERM` variants and every width. DISC-008 accurate (umask 002: 0664 against 0644; 022: equal). DISC-011 accurate (`VmSize` 8592046820 kB; 2 OS threads). DISC-013 accurate (20000 random doubles, seed 88: 0.008 s against 1.28 s, x151, same bytes). DISC-014 accurate (INPUT `/dev/fd/3` never returns, `-o /dev/fd/3` → `bend: memory fault (machine stack overflow?)`, exit 1; `/dev/fd/9` equal) and its descriptor numbers do not depend on `--threads` (descriptors 0–5 at every thread count). DISC-009, DISC-012: probe rows SAME on three lanes. DISC-010: not run (memory). All 14 "Impact measured" denominators say 1065 now (round 9's R9-3 is repaired). **No entry is factually wrong; the register has one gap, R10-3** (the launcher's dependency on `dirname` / `readlink`).
- **README.md**: R10-1, R10-2, R10-5, R10-6, R10-7, R10-8. **docs/PORT_REPORT.md**: R10-1, R10-6. **docs/PORT_STATE.md**: accurate gate lines except the stdio-probe paraphrase (R10-6) and the commit named in `:19` (R10-1); its rounds table and Next action are honest about the missing four-lane line. **docs/FEATURE_PARITY.md**: every citation exists; header R10-9; lane table R10-1. **perf/PERF-LEDGER.md, NEGATIVE-EVIDENCE.md, EXPERIMENTS.md**: every number recomputes from `perf/evidence`; R10-7. **AGENTS.md**: structure and gate table accurate; R10-8. **port/LAWS.bend** header: R10-9. **CONTRIBUTING.md, .github/CODEOWNERS, docs/PARITY_RUNBOOK.md**: R10-4.

## 4. Audit trail

- No file deleted; nothing written outside `review_R10/`; nothing under `/tmp`; no git command outside my clone except read-only `git -C /data/projects/toon_bend rev-parse/status`. In the clone: `git clone`, `git archive` (into `review_R10/co_*`), read-only log/show/ls-tree; the README's `-o port/toon` build left one ignored file there.
- Processes ended by me: only children of my own harness through `subprocess` timeouts (the DISC-014 `/dev/fd/3` row, 5 s) and `p.kill()` in my pty harness's timeout path (never reached after the harness fix); one background task of mine stopped with TaskStop (the interpreter sample at 589 of 600, to keep the 600-run budget for the corpus and edge rows). I signalled nothing I did not start; the author's `lanes.sh` run was visible in `ps` and left alone.
- The safety hook refused three shell commands (a redirect to a variable path, `eval`, a variable-named executable followed by `-e`); I allowlisted nothing and ran them from Python files or with literal paths.
- Not run, by instruction: `lanes.sh`, `port-doctor.sh`, `hand-mutants.py`, `floor.sh`, cargo, `bend update`, anything in `legacy/`. `hand-mutants` "22 of 22 killed" and the floor line are therefore unverified by me. Not testable here: macOS, a quiet host for the refused captures.
