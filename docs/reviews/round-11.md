<!-- Round 11 of the parity gate's find-fix rounds (docs/PORT_STATE.md). The report of a NON-AUTHOR reviewer (a fresh subagent that
     did not write the port), reviewing the PUBLIC repository at commit 53df4c1 from a fresh clone; copied here VERBATIM from the
     session's scratch directory. Paths under /data/tmp/... are the reviewer's scratch files and are not part of the repository.
     What was done about each finding: docs/PORT_STATE.md and the commit that names round 11. -->

# Round 11 — hostile, non-author review of toon_bend: fresh clone at the new HEAD, claims, `--stats` / real files / hand-written TOON / argv permutations, the launcher (public commit 53df4c1)

Reviewer: fresh subagent; did not open `legacy/`, did not read the original's source, edited nothing in `/data/projects/toon_bend`. Date 2026-09-20.
ORIG = `/data/projects/toon_bend/oracle/toon`. CLONE = `<scratchpad>/review_R11/clone` (`git clone https://github.com/Dicklesworthstone/toon_bend`, HEAD `53df4c1` = the author's HEAD; `diff -r CLONE/port /data/projects/toon_bend/port`: identical).
PORT = `<scratchpad>/review_R11/build/toon -- …`, built from the clone (19.4 s wall); sha256 `27787b9b…cd76465`, and `build/toon.js` `1a273ccb…`: both EQUAL round 10's hashes (the code did not change since `4c3cccc`; the build is reproducible).
JS = `python3 -B CLONE/scripts/js-lane.py build/toon.js -- …`. LAUNCHER = `CLONE/bin/toon` with `TOON_BEND_BIN=build/toon`.
Harness: `review_R11/h.py` (stdout, stderr and the exit code compared byte for byte with the ORIGINAL; every difference appended to `logs/diffs_<lens>.jsonl`; summaries in `logs/summaries.jsonl`). Everything I wrote is under `<scratchpad>/review_R11/` (`tmp/` = my `TMPDIR`); nothing under `/tmp`; no file deleted anywhere.

Headline. **Conversion content: ZERO byte differences in about 74,500 compared executions of my own** (`--stats` at volume, 1742 real JSON files, 5000 hand-written TOON documents with human mistakes, 24,238 argv permutations, every 10th on the JavaScript build) plus the repository's 10,000 fuzz inputs, 10 number batches, 18 scale inputs and 66 probe rows, all PASS. The fresh clone builds, the proof prints exactly `All terms check.`, and every gate I ran at HEAD prints what `docs/PORT_STATE.md` says. What I found: (1) the LAUNCHER, which the register names as "the supported command line", throws away ALL of stderr wherever `/dev/fd/2` cannot be resolved (no `/proc`, or a bare `/dev`); (2) three descriptor kinds on stdin where the SIZE and alignment of the read are observable and the port converts what the original refuses; (3) PORT_REPORT's "Reproduce" recipe fails at its fifth line on the commit it checks out; (4) the README's newest performance paragraph quotes sixteen numbers and an evidence set that are in no commit; and eight LOW items.

**HIGH: 0. MEDIUM: 4. LOW: 8.**

## 1. Findings

Severity as defined in the brief. "Repro" commands are run from `review_R11/work*`; `<toon>` = `review_R11/build/toon`.

| id | sev | lens | reproduction or file:line | expected (the original, or the repository) | observed |
|---|---|---|---|---|---|
| R11-1 | MEDIUM (environment difference, not registered; every diagnostic lost) | 4 (launcher) | `bin/toon:45-47` (`if [ ! -e /dev/fd/2 ]; then exec 2>/dev/null; fi`). Environment: `/proc` not mounted (a chroot, an early-boot or jail environment), or a `/dev` without the `fd` link. Reproduced without root: `printf '{bad' \| bwrap --ro-bind / / --dev-bind /dev /dev --tmpfs /proc env TOON_BEND_BIN=<toon> CLONE/bin/toon -e; echo $?` (also `--tmpfs /dev --proc /proc`; also `dash`, `bash`, `busybox sh` running the launcher). Control: the same sandbox with `--proc /proc` prints the error line | the original in the same sandbox: stderr `JSON error: Failed to parse JSON: key must be a string at line 1 column 2`, exit 1. `--bogus` → clap's five lines, exit 2; `-e --stats` → the three statistics lines; `-e -o f` → the `Encoded` line; a missing INPUT → `Failed to read file …` | LAUNCHER: **stderr EMPTY in every row**, exit codes and stdout equal (7 of 10 rows differ per sandbox; the 3 equal rows have an empty stderr anyway). `/dev/fd` is a link to `/proc/self/fd`; when it dangles, the launcher takes descriptor 2 for CLOSED and redirects it to `/dev/null` (with an empty, writable `/dev` it CREATES a regular file `/dev/null` and the error text lands in it: verified, 74 bytes). The BARE native binary equals the original in all 57 sandbox rows I ran (37 of them without `/proc` or with an empty `/dev`), so the mitigation is what breaks. 24 launcher rows differ. DISC-003/006/007 say the launcher gives "exactly the original's behavior" and name only the `/proc/self/fdinfo` half as Linux-specific. Side observation, same sandbox: the JavaScript build does not start at all (bun panics, exit 134), so DISC-011's "use the JavaScript build" does not hold there either |
| R11-2 | MEDIUM (wrong on contrived input: exit 0 and a conversion where the original exits 1; not registered) | 4 (stdin effect) | stdin is a descriptor on which the read SIZE / buffer alignment is observable. (a) packet-mode pipe: `python3 -c "import os,subprocess,json;d=json.dumps({'key%d'%i:i for i in range(12)}).encode();r,w=os.pipe2(os.O_DIRECT);os.write(w,d);os.close(w);print(subprocess.run(CMD+['-e'],stdin=r,capture_output=True))"`; (b) `socket.socketpair(AF_UNIX, SOCK_SEQPACKET)`, ONE message longer than 32 bytes, sender closed; (c) a regular file on ext4 opened `O_RDONLY\|O_DIRECT` as descriptor 0 (`work/odirect.json`, 16890 bytes; 3 of 3 runs) | (a), (b): the original's first read asks for 32 bytes, the rest of the packet is discarded by the kernel: `JSON error: Failed to parse JSON: EOF while parsing an object at line 1 column 32`, exit 1 (a 40-byte array: `EOF while parsing a list at line 1 column 32`). (c): `Failed to read stdin: Invalid argument (os error 22)`, exit 1 | native, launcher and JavaScript build alike: the whole document is read (one 1 MiB read into an aligned buffer) and converted, **exit 0** (`key0: 0 …`; `[19]: 1,1,…`). 21 differing executions: (a) 3 lanes; (b) 3 message shapes (136 bytes, 2 x 60 bytes, 40 bytes) x 3 lanes; (c) 3 argv (`-e`, `-d`, `- -e`) x 3 lanes. Messages of at most 32 bytes are SAME, which is why rounds 7 and 8 saw "SEQPACKET: SAME". DISC-012's resolution says descriptor 0 "itself is read" like the original and lists SEQPACKET as SAME; `scripts/stdio-probe.py` has no such row. By the project's rule (a silent improvement is a divergence: DISC-009's history) this needs an entry. DGRAM socketpairs hang in all four programs (SAME) |
| R11-3 | MEDIUM (a recipe that does not run as written on the commit it names) | 1 + 2 | `docs/PORT_REPORT.md:62-75` "Reproduce (an auditor gets the same lines) … `git checkout c7239e2   # or any commit after it`" then `:71` `./scripts/clean-build-check.sh && ./scripts/converge.sh docs/PORT_STATE.md`, `:75` the claims lint on eleven files. Run in the export of `c7239e2` (`review_R11/tmp/clean-build.MR2ArI`, made by `clean-build-check.sh c7239e2`) | the lines PORT_REPORT pastes: `clean-build-check` PASS, `"rounds": 10` (`:32`), `claims-lint: 0 hit(s) in 11 file(s)` (`:34`) | at `c7239e2`: `./scripts/clean-build-check.sh: no such file or directory`, rc 127 (the script was added by `77a63d9`, the NEXT commit: `git ls-tree c7239e2 scripts/` has no such file), so `converge.sh` after `&&` never runs; run alone it prints `"rounds": 9`; the claims lint prints `CONTRIBUTING.md:23: later (deferral) …` and `claims-lint: 1 hit(s) in 11 file(s)`, exit 1. The recipe works only from `77a63d9` on (verified at HEAD). Round 10's HIGH finding was "cited commits do not build"; its repair cites a commit that builds but predates the gates the recipe calls |
| R11-4 | MEDIUM (false statement about evidence; numbers with no artifact) | 2 | `README.md:174`: "Two attempts of eight captures each were ALL REFUSED … (`perf/evidence/INCUMBENT.*.json` holds the second attempt: arms between 3 and 61 percent cv …)" and the sixteen "refused medians": 74.2 / 7.5, 51.5 / 22.2, 65.5 / 8.4, 168.7 / 4.4, 42.6 / 5.5, 1397.1 / 11.8 (20000 doubles), 1509.4 / 5.7 (5000 scientific), 2.3 / 1.9 ms | the JSON files in the repository hold those captures (`README.md:151`: "The JSON lines are in `perf/evidence/`") | `perf/evidence/` at `53df4c1` holds SIX `INCUMBENT.*.json`, all of the FIRST attempt (tag `INCUMBENT-1230a0d`, port binary sha `71e268dc…` = the build of `1230a0d`, dated 12:09 UTC, cv 7.6 … 47.9 %): medians 63.6 / 6.0, 48.9 / 23.9, 57.2 / 6.0, 153.1 / 3.9, 39.6 / 4.0, 0.94 / 0.93. None of README's sixteen numbers is in any committed file (`git log -- perf/evidence/INCUMBENT.tabular-enc.json`: one version, `e3f5540`); `INCUMBENT.doubles.json` and `INCUMBENT.sci.json` exist in NO commit (`git log --all -- …`: empty). `docs/PORT_STATE.md:38` and `docs/PORT_REPORT.md:13, :33, :53` say "six captures", README says eight. (Context, not part of the commit: the author's working tree holds eight uncommitted files of a LATER attempt, dated 17:44 and 18:36 UTC, three of them with verdict MEASURED, which README's "ALL REFUSED … no claim" does not describe either.) |
| R11-5 | LOW | 1 + 2 | `docs/PARITY_RUNBOOK.md:26` `./scripts/claims-lint.sh docs/*.md perf/*.md README.md     # the forbidden phrases` (section 2, "The gates, in one command each") | a gate that is clean on the published tree | `claims-lint: 10 hit(s) in 16 file(s)`, exit 1: 3 hits in `docs/AGENTS.scaffold.md:53` (the stray scaffold copy), 7 in `docs/EXISTING_Toon_STRUCTURE.md` (`:40, :314, :402, :431, :493, :513, :584`). The same gate has FOUR different file lists: `AGENTS.md:291` (9 files), `AGENTS.md` "Landing the Plane" (no PORT_REPORT), `PARITY_RUNBOOK.md:26` (16 files, fails), `PARITY_RUNBOOK.md:103` = `PORT_REPORT.md:75` (11 files, `0 hit(s)`) |
| R11-6 | LOW | 1 + 2 | `docs/PORT_REPORT.md:64` "without it [the oracle] the first, second and the last-but-one command below cannot run, the others can" | line `:73` (`stdio-probe.py -- ./x -- && diff-fuzz.py scale …`) runs without the oracle | in the clone, without `--original`: both scripts end in a Python traceback, `FileNotFoundError: … 'CLONE/oracle/toon'`, exit 1 (`logs/probe_no_oracle.err`, `logs/fuzz_no_oracle.err`). `CONTRIBUTING.md:36-38` says the opposite (and is right): both need the oracle. The traceback is round 10's R10-10 again, repaired in `pin-check.sh` only |
| R11-7 | LOW | 2 | `docs/FEATURE_PARITY.md:10` "`scripts/lanes.sh goldens/cases.tsv goldens port/main.bend --threads 8 --interpreter-timeout 120` → `{… "timeouts_seconds":{"interpreter":60.0,…}}`" | the command that produced the pasted line | `lanes.sh:150` prints the interpreter timeout it ran with: 60.0, not 120; `port-doctor.sh` (the run the line comes from, `PORT_STATE.md:20, :25`) passes no `--interpreter-timeout`. The command text AND the date `2026-09-20` are string constants in `scripts/board-refresh.py:132`, so the header is not computed from the run it describes |
| R11-8 | LOW (stale or dangling cross-references) | 2 | (a) `docs/DISCREPANCIES.md:30` "all 1053 goldens are compared unchanged"; (b) `docs/PORT_STATE.md:13` "rounds 6 to 9 are non-author subagents"; (c) `docs/PORT_STATE.md:55` open item "NE-001..004"; (d) `docs/PORT_REPORT.md:33` "NE-001..005 are provisional"; (e) `perf/NEGATIVE-EVIDENCE.md:100` "Correctness: as the PERF-LEDGER rows of EXP-004"; (f) `docs/PORT_STATE.md:56` owner "(bead `toon_bend-925`)"; (g) `docs/DISCREPANCIES.md:165` "the work it names left open as beads (`toon_bend-ngs`, …)" | — | (a) the corpus has 1065 (the `Impact measured` line below it says so): round 9's R9-3 was repaired in 14 lines and missed this one; (b) 6 to 10; (c) NE-005 exists and is PROVISIONAL; (d) NE-004 is NO_EVIDENCE, not provisional; (e) `perf/PERF-LEDGER.md:43` has no row ("(none)"): the rows moved to NE-005; (f) `.beads/issues.jsonl`: `toon_bend-925` is CLOSED ("duplicate of toon_bend-udw"), the open bead is `toon_bend-udw`; (g) `toon_bend-ngs` is CLOSED (`p47`, `okl` are open) |
| R11-9 | LOW | 1 | `README.md:32` (Quick Build): `bun /tmp/bend/bend2/main.ts port/main.bend -o port/toon && bin/toon --encode < users.json` | a block that runs in a fresh clone | `no such file or directory: users.json`, rc 1: no `users.json` exists in the repository (`git ls-files`; the same document is `cases/inputs/hand/happy_readme_users.json`). With the document piped in, the line prints the three README lines, exit 0. All other README examples I ran (16 command lines, and the 7 snippets of the format block `:82-107` decoded on native and JS, 30 executions, 0 differences: `:30`, `:115`, `:121` with its 12 output lines, `:138`, `:139`, `:142`, `:206`, `:207` on the interpreter, `:217`-`:221`, `--decode --indent 0` of unindented TOON, the five format blocks) print what is shown |
| R11-10 | LOW | 2 | `docs/PARITY_RUNBOOK.md:3` "Every command below exists in ./scripts/"; `:87` "→ `keep-audit` →"; `:57` `assets/probes/base/run-base-probes.sh`; `perf/PERF-LEDGER.md:16` "`scripts/bench-speedup.sh --aa --max-cv 5 --strict`", `:34` "`ledger-row.sh`"; `scripts/ORIGIN.md:1` "harness scripts copied from porting-to-bend2 …; update by re-copying, never by editing here" | statements that describe this repository | `git ls-files`: no `keep-audit*` script (only its three outputs under `perf/evidence/`), no `bench-speedup.sh`, no `ledger-row.sh`, no `assets/probes/` (`assets/` holds `templates/evidence-pack/` only). Seven copied harness scripts WERE edited here (one commit each after `c4a5235`): `converge.sh` (`b2dcb18`), `port-lint.py` (`3751630`), `interp-lane.sh` (`1230a0d`), `js-lane.py` (`4c3cccc`), `pin-check.sh`, `incumbent-bench.sh` (`77a63d9`), `harness-selftest.sh` (`1a5da71`). I read the seven diffs: benign, but the file's rule is not the practice (round 6, item 21, said so for `converge.sh` alone) |
| R11-11 | LOW | 2 | (a) `README.md:333` "8000 generated documents in both directions" against `docs/PORT_STATE.md:65` "structure-aware generated documents, every option, encode then decode (4000)"; (b) `docs/PARITY_RUNBOOK.md:11-12` "`--threads N` changes nothing but the runtime's idle pool" against `README.md:68` = `AGENTS.md:145` "the runtime never starts a worker pool and `--threads N` changes nothing (2 OS threads …)"; (c) `README.md:174` "47 to 70 bytes of memory per input byte" against `README.md:322` "50 to 70" | one number, one mechanism | (a) two counts for round 3, neither re-runnable (UNVERIFIABLE); (b) I measured `/proc/<pid>/task` = 2 at `--threads` absent, 1, 8, 64 (`VmSize` 8592046824 kB each): there is no pool whose size changes, README's sentence is the true one; (c) DISC-011 says 47 to 70 measured, "about 50 to 70" in its contract |
| R11-12 | LOW (contrived misconfiguration) | 4 (launcher) | `TOON_BEND_BIN` naming (a) a DIRECTORY, (b) an EMPTY file with mode 0755: `printf '[1,2]' \| TOON_BEND_BIN=$PWD/emptyexec CLONE/bin/toon -e; echo $?` | the launcher's own `toon: no binary at …`, exit 127 (what it prints for a missing, dangling or non-executable file: verified under dash, bash, busybox) | (a) `[ ! -x "$bin" ]` is true for a directory: the shell's `exec: …: Permission denied`, exit 126; (b) **exit 0, stdout EMPTY, stderr empty** under dash and busybox (`execve` gives ENOEXEC, the shell then runs the empty file as a script), bash: `cannot execute: Exec format error` and still exit 0: a conversion that reports success and prints nothing |

Not findings, stated so nobody re-reports them: (a) `logs/diffs_l3d_argv_95.jsonl` (1 row) is an artefact of my smoke run (the JS twin directory had not received files that earlier non-JS runs created; fixed before the counted seeds). (b) `logs/diffs_l3e_argv_extreme.jsonl` (2 JS rows): with 100,001 and 50,001 argv words `js-lane.py` fails with `[Errno 7] Argument list too long` while starting bun (the wrapper adds its own words and environment); the native binary equals the original on all 17 extreme command lines. (c) `pin-check.sh` is RED in a clone by design; its output is sane now (4 RED rows that name the absent original, `manifest_hashes` RED with "the captured original is absent here … the 3195 golden hashes themselves match", no traceback: R10-10 is repaired). (d) DGRAM socketpairs as stdin hang in all four programs.

## 2. What I ran

Seeds and scripts are in `review_R11/` and re-runnable (`python3 <script> <seed> <n>`; the two behavioral findings stand alone in `repro_R11-1.py` and `repro_R11-2.py`; the smaller probes of lens 4 were inline Python and are described row by row below). "executions" = port-side executions compared with the ORIGINAL (or with a golden captured from it). Parallelism: never more than 2 background jobs of mine (diff-fuzz ran ALONE with `--jobs 2`); for short probes of a few seconds each (the sandbox rows, the floor, single `t.py` comparisons) a third process of mine ran beside two background jobs. Largest input fed to the port: 9.5 MB (a `--stats` document; `free -g` showed 14 GB available). `free -g`: 11–17 GB available except one episode (another agent's 15 GB `bend` build, 1 GB available): I stopped my own real-files job (TaskStop) and resumed it with a memory guard when 17 GB were free.

### Lens 1 — fresh clone at `53df4c1` (commands run in CLONE, `TMPDIR` inside review_R11)

| command | verbatim last line | as documented? |
|---|---|---|
| `bun /tmp/bend/bend2/main.ts port/main.bend -o <build>/toon` / `… -o <build>/toon.js` | (no output; 842032 and 410985 bytes; 19.4 s and 2.3 s wall) | yes ("about 25 seconds") |
| `./scripts/conform.sh goldens/cases.tsv goldens --lane c-1t -- <toon> --threads 1 --` | `{"lane":"c-1t",…,"passed":1065,"failed":0,"inconclusive":0,"stderr_compared":true,"verdict":"PASS","oracle_identity_checked":true}` | yes |
| the same: `--lane c-8t … --threads 8 --`; `--lane js --timeout 20 -- python3 scripts/js-lane.py <toon.js> --`; all three again with `TOON_SPEC=1`; `--lane launcher -- bin/toon` | six lines, each `"passed":1065,"failed":0,…"verdict":"PASS"` | yes (README:68) |
| `(cd port && BEND_NO_TELEMETRY=1 bun /tmp/bend/bend2/main.ts PROOF.bend)` | `All terms check.` (the ONLY line; 4 min 02 s wall, 316 s CPU, 4.0 GiB peak RSS) | yes |
| `./scripts/clean-build-check.sh` (HEAD) | `{"ref":"53df4c1",…,"native":"built","js":"built","conform_c1t":{"passed":1065,"failed":0},"verdict":"PASS"}` | yes |
| `clean-build-check.sh <sha>` for the 13 other commits from `1230a0d` on | PASS: `1230a0d`, `e3f5540` (1053/1053), `a725d10`, `2d089e2`, `49f56cf`, `20735c9`, `a23d678`, `c7239e2`, `77a63d9`, `1a5da71` (1065/1065). FAIL at the native build (`ENOENT … 'port/./stdin_open.c'`): `d80251a`, `861abf2`, `4c3cccc` | yes: exactly the three commits the documents say do not build; every commit they tell a reader to use builds |
| `python3 cases/build-cases.py --check` | `{"cases": 1065, "verdict": "OK"}` | yes |
| `./scripts/cases-lint.sh goldens/cases.tsv` | `{"cases": 1065, "errors": 0, "notes": 20, "classes_missing": "", "verdict": "OK"}` | yes |
| `python3 scripts/spec-lint.py docs/EXISTING_Toon_STRUCTURE.md goldens/cases.tsv` | `spec-lint: 703 clauses, 1065 cases, 1065 cases cited, 0 finding(s)` | yes |
| `./scripts/law-coverage.sh` | `{"laws": 368, "proofs": 368, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | yes |
| `./scripts/parity-board.sh docs/FEATURE_PARITY.md` | `{"rows": 33, "present": 27, "partial": 0, "missing": 0, "excluded": 6, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | yes |
| `./scripts/state-check.sh docs/PORT_STATE.md` | `state-check: 0 finding(s)` | yes |
| `./scripts/converge.sh docs/PORT_STATE.md` | `{"tier": "T2", "rounds": 10, "clean": 5, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": [], "verdict": "NOT_CONVERGED", "missing": ["clean rounds since last reset 0 < 2"]}` (exit 1) | yes (PORT_REPORT:32) |
| `./scripts/claims-lint.sh` on the eleven files of PORT_REPORT:75 | `claims-lint: 0 hit(s) in 11 file(s)` (47 s) | yes |
| `./scripts/claims-lint.sh docs/*.md perf/*.md README.md` (PARITY_RUNBOOK:26) | `claims-lint: 10 hit(s) in 16 file(s)`, exit 1 | **no: R11-5** |
| `python3 scripts/port-lint.py port/*.bend --laws port/LAWS.bend` | `{"files": 10, "findings": 41, "errors": 0, "warnings": 0, "infos": 41, "by_rule": {"PL-02": 41}, "laws": "port/LAWS.bend", "verdict": "OK"}` | yes |
| `./scripts/pin-check.sh docs/PIN.toml` | `pin-check: RED` then one JSON line, `"verdict":"RED"`, exit 2; 4 RED rows, 1 YELLOW, 11 GREEN; no traceback | sane (note c) |
| `python3 scripts/board-refresh.py` | `{"laws": 368, "rows": 27, "rows_without_a_law": ["output framing", "I/O errors"], "lanes_pasted": false}`; `git -C clone diff` and `git status --short`: EMPTY | yes |
| `./scripts/floor.sh goldens/cases.tsv goldens --repeat 3 -- /data/projects/toon_bend/oracle/toon` | `{"repeat":3,"stable":1065,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | yes (PORT_REPORT:28) |
| PORT_REPORT "Reproduce" lines 71 and 75 in the export of `c7239e2` | rc 127; `"rounds": 9`; `claims-lint: 1 hit(s) in 11 file(s)` | **no: R11-3** |
| `stdio-probe.py` / `diff-fuzz.py scale` WITHOUT `--original` in the clone | Python traceback, exit 1 | **R11-6** |
| README examples (17 command lines, run literally; the 7 format snippets decoded) | 16 print what is shown; the snippets are valid TOON and decode identically | **R11-9** (`users.json`) |
| `$BEND_CLI port/main.bend -- --version` (README:207, the interpreter) | `toon 0.2.4` (2.7 s) | yes |

Repository tools with the oracle given explicitly (run LAST and ALONE, `--jobs 2`; log `logs/repo_fuzz.log`):
```
{"lens": "mutate", "seed": 1111, "inputs": 2000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "docs", "seed": 1112, "inputs": 2000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "argv", "seed": 1113, "inputs": 2000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "expand", "seed": 1114, "inputs": 2000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "collide", "seed": 1115, "inputs": 2000, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}
{"lens": "numbers", "seed": 1116, "inputs": 10, "differences": 0, "too_slow": 0, "switch": "TOON_SPEC=1", "verdict": "PASS"}
{"lens": "scale", "seed": 1, "inputs": 18, "differences": 0, "too_slow": 0, "switch": null, "verdict": "PASS"}          (--runs 16000)
stdio-probe, native:   {"rows": 22, "same": 11, "known": [11 rows: DISC-006 x3, DISC-003 x5, DISC-014 x1, DISC-007 x2], "fixed": [], "new": [], "verdict": "PASS"}
stdio-probe, launcher: {"rows": 22, "same": 11, "known": [4: DISC-014, DISC-006 /dev/full, DISC-003 /dev/full x2], "fixed": [7 rows], "new": [], "verdict": "PASS"}
stdio-probe, JS (--timeout 20): {"rows": 22, "same": 11, "known": [6], "fixed": [5 rows], "new": [], "verdict": "PASS"}
```
The three probe lines equal `docs/PORT_STATE.md:37` word for word (the `work` field apart).
Lens 1 compared executions: 7 conform runs x 1065 = 7,455, 0 failures; 11 clean-build conform runs = 11,691, 0 failures; floor 3 x 1065, 0 unstable; 10,028 fuzz inputs, 0 differences; 66 probe rows, 0 NEW.

### Lens 2 — claims audit

Checked, about 200 statements in README.md, CONTRIBUTING.md, PORT_REPORT.md, PORT_STATE.md, PARITY_RUNBOOK.md, FEATURE_PARITY.md, DISCREPANCIES.md, the three perf documents, AGENTS.md, CODEOWNERS and the headers of `port/LAWS.bend`, `port/main.bend`, `bin/toon`, `diff-fuzz.py`, `stdio-probe.py`, `hand-mutants.py`, `board-refresh.py`, `clean-build-check.sh`. False, stale, contradictory or unverifiable: the statements in R11-3 … R11-11 (**27 statements**). Recounted and TRUE: 368 laws = 14 quantified (`for` clauses) + 59 closed unit + 295 `golden_` (script over `port/LAWS.bend`; the board's 39 + 22 lists: every name exists, 39 − 2 + 22 = 59); 368 proof defs; 1065 cases = 716 hand + 349 fixtures; 703 clauses; MANIFEST 3195 hashes, corpus growth 678 → 1005 → 1053 → 1060 → 1065 and "every re-capture added hashes and changed none" (script over the five MANIFEST versions: 981 / 144 / 21 / 15 added, 0 changed, 0 removed); DISC register 12 ACCEPTED + 2 RESOLVED + 0 OPEN; findings per round 6 / 11 / 8 / 3 / 10 = 38 against `docs/reviews/`; executions "well over a million" (5,000 + 122,000 + 90,000 + 947,646 + 29,300); 22 mutants in `hand-mutants.py` (22 distinct ids; every mutant's site text occurs exactly ONCE in the current sources; the reduced proof has 123 laws: 73 + 50 goldens); 22 `row(` calls in `stdio-probe.py`; every cited bead id exists (5 cited; 2 are closed: R11-8); keep-audit deltas of NE-001..005 equal the column sums of the three `keep-audit.*.txt` files (246/244/1679/51 → 255/248/1687/54 → 268/298/1770/56); every ratio, median, cv and A/A value of README's two performance tables against `perf/evidence/EXP-*.json` (1.6661, 1.5580, 2.1249; 1.0012 / 0.9945 / 1.0042; 0.2527, 1.2718, 3.0584; regressions 0.9971 / 0.9918 / 0.9938 / 1.0224); `perf/gen-bench-inputs.py --check` OK and "no `random` module" true; `git diff c7239e2 HEAD -- port` touches comment lines of `LAWS.bend` only (PORT_STATE:25); all four fast-path selectors go through `F.twin.on` (`f64.bend:343, 441, 564, 915`); `import Base` plus the project's own modules and the two effect twins only; LICENSE is byte-identical to the original's at `f955c67` (3939 bytes); 2 OS threads at every `--threads`; the emitted C is 1.8 MB (`AGENTS.md:579`); no workflow file exists (CONTRIBUTING:7). NOT verified, by instruction: `hand-mutants` "22 of 22 killed", `harness-selftest` "11 of 11", the four-lane `lanes.sh` line and the doctor's GREEN (I verified three of its four lanes through `conform.sh`, the proof, the board and the floor separately).

### Lens 3 — behavior (native binary; every 10th case also on the JavaScript build)

| part | generator (seed) | compared executions | differences |
|---|---|---|---|
| (a) `--stats` at volume | `l3a_stats.py` 1131, 5000 documents: 1500 DIRECTED (J fixed on tie-prone denominators 16 … 4000 by construction, D swept: 355 exact rational ties x.x5 %, 746 distinct percent texts), 2500 random documents written as TEXT (three escaping styles; strings from pools of the 19 non-ASCII White_Space code points (U+0085, U+00A0, U+1680, U+2000–U+200A, U+2028, U+2029, U+202F, U+205F, U+3000), 12 zero-width / look-alike characters, 7 astral characters and sequences, JSON whitespace between tokens), 1000 odd ones (empty input, BOM, Unicode whitespace OUTSIDE strings, lone surrogates, depth 130, shapes where TOON is LARGER: 552 documents with T > J); 8 option sets in shuffled order; every 2nd document with `-o FILE` in twin directories (file bytes compared); every 3rd success also decoded with `--stats` (must stay silent). Smoke seeds 97–99 (391). `l3a2_stats_big.py` 1132: 160 documents with J from 1,600 to 2,000,008 (inputs up to 9.5 MB) | 7,023 (701 on JS) | 0 |
| (b) real-world JSON | `l3b_real.py`: ALL 1742 distinct `.json` files under 3 MB found under `/usr/share`, `/usr/lib`, `/usr/lib/python3*`, `/usr/local/lib`, `/etc`, `/opt`, `/snap`, `/var/lib` (111 MB, largest 2.18 MB; read only); 4 encode option sets (`[]`; TAB + `--indent 4`; `--key-folding safe`; `--key-folding=safe --flatten-depth=3 --delimiter pipe --indent 1 --stats`), alternately by path (extension detection) and on stdin; then the ORIGINAL's TOON decoded with `-d` and `-d --expand-paths safe --no-strict` (default and folded encodes) and with the matching `--indent` (the other two). Exit mix of the original in the second half: 7106 x 0, 80 x 1 | 18,934 (about 1,700 on JS, files under 300 KB) | 0 |
| (c) hand-written TOON with human mistakes | `l3c_hand.py` 1141 (3000 documents, 0–3 mistakes) and 1142 (2000 documents, 2–6 mistakes): 39 documents written by hand in the style of the specification's prose (tabular arrays, nested lists of objects, tabular arrays as first fields of list items, mixed and root arrays, quoted keys and values, all three delimiters, dotted keys, number and literal look-alikes, Unicode) + 15 foldings of 5 JSON documents by the ORIGINAL (`--key-folding safe`, flatten depth, pipe) for the round trips; re-indented to units 1, 3, 4, 8 in a fifth of the cases; 14 mistake kinds (one space too many in 4 places, missing colon / missing space after it, a count off by one, a stray tab in 4 places, a smart quote, CRLF / lone CR / CR everywhere, a trailing comma or delimiter, a duplicated line or key, blank and whitespace-only lines, a wrong indent step, header typos, a cell too many or too few, list-marker typos incl. en dash and bullet, missing final newline, BOM, unterminated quote, comment lines); each document `-d` and `-d --no-strict`, a quarter also with `--expand-paths safe` in both modes. Exit mix: 9,545 x 0, 4,551 x 1 | 15,890 (1,624 on JS) | 0 |
| (d) argv permutations of VALID option sets | `l3d_argv.py` 1121, 1122, 1123, 1124: 430 option sets (0–5 of the 10 options, encode / decode exclusive, each once) + INPUT (file, `-`, none, `-x.json`, `=v.json`, `a b.json`) + `--` in 30 %; EVERY ordering of the units up to 5 units (120), 150 seeded orderings above; values attached by `=`, by a space, `-oFILE`, `-o=FILE`, and short clusters `-eo F`, `-eoF`, `-do=F`; real `-o` files in twin directories, the files written by each run compared. 24,238 command lines; exit mix 11,541 x 0, 1,019 x 1, 11,678 x 2. Smoke seeds 94, 95 (802). `l3e`: 17 extreme command lines (100,001 words, a 100,000-character cluster, 120,000-character option names and values, 130,000-character paths) | 27,496 (2,511 on JS) | 0 (3 harness artefacts, notes a and b) |

Lens 3 total: **69,343 executions, 0 differences.**

### Lens 4 — the launcher and the stdin effect

| part | what | executions | differences |
|---|---|---|---|
| shells x IFS x descriptor states | `l4_launcher.py`: the launcher run directly (`#!/bin/sh` = dash), by `dash`, `bash`, `bash --posix`, `busybox sh`, `zsh --emulate sh`; `IFS` in the environment unset and 12 values (`:`, `f`, space, empty, LF, `0`, `flags:`, `:<TAB>`, `a`, `/`, `-`, `x\`); 35 argv (spaces, a newline in a file name, globs `*.json` and `star*.json`, leading dashes behind `--`, empty words, `$HOME`, backticks, `$(id)`, quotes, a backslash, TAB and space delimiters, runtime words `--threads 2` / `--gpu off` on both sides of INPUT, similarity tips) with pipes; 6 argv x {stdout read-only, stderr read-only, descriptor 0 / 1 / 2 closed, all three closed}; each against the ORIGINAL with the same argv, environment and descriptors | 4,674 | 0 |
| symbolic links and odd checkouts | link in a directory with a space, relative link to a link inside a directory whose name holds a newline, a third link, found through `PATH`, x 5 `IFS` values x 4 shells (125); byte copies of the launcher in checkouts named `sim<LF>`, `sim ok`, `sim<LF><LF>/x`, `-sim` (12) | 137 | 0 |
| broken `TOON_BEND_BIN` | missing, a directory, not executable, garbage bytes, an EMPTY executable, a dangling link, empty, relative, a space, a trailing space x 4 shells | 40 | sane `toon: no binary at …`, exit 127, except R11-12 |
| sandboxes (`bwrap`, no root needed) | no `/proc`; empty `/dev`; both; `--unshare-all` with a read-only root and an empty `/tmp`; a root of `/usr /lib /bin /data` only; 10 argv x native and launcher (+ 10 descriptor-path rows on the native binary, + dash / bash / busybox / JS rows) | 113 | **R11-1** (24 launcher rows; 2 JS rows: bun panics, exit 134); native binary 0 of 57 |
| novel stdin kinds | memfd at offset 0 / 5 / its end / sealed, eventfd (counter 5 and 0, non-blocking), epoll descriptor, pidfd, timerfd, a TCP loopback socket, `O_RDWR\|O_APPEND`, `O_RDONLY\|O_NONBLOCK` at offset 1, `O_DIRECT`, `/dev/null` O_RDWR, `/dev/ptmx`, a `/sys` file, an unconnected UDP socket; SEQPACKET and DGRAM socketpairs with 1–2 messages of 3 … 136 bytes; a packet-mode pipe; x `-e`, `-d`, `- -e` x native, launcher, JS | 183 | **R11-2** (21 rows); everything else SAME, EINVAL / EAGAIN texts included |

Lens 4 total: 5,147 executions; 2 findings (R11-1, R11-2) + 1 LOW (R11-12). Lenses 3 + 4: 74,490 executions of my own.

## 3. Register entries and documents re-checked

- **DISCREPANCIES.md** (by execution, `work/disc`): DISC-001 accurate (bare `x.json --threads 2` is consumed: the port reports the missing file, the original an unexpected argument; bare `--help` prints the runtime's table; through the launcher both equal the original). DISC-002 accurate (`tn` link: `Usage: tn …` against `Usage: toon …`). DISC-003 / 006 / 007 / 014 accurate as far as the probe's 22 rows go on three lanes (0 NEW), **but their sentence that the launcher gives "the original's behavior exactly" is false where `/dev/fd` does not resolve (R11-1)**. DISC-004 accurate (`--indent $'\xff'`: `invalid UTF-8 was detected` + Usage against `invalid value '�' … invalid digit found in string`). DISC-005 accurate (`CLICOLOR_FORCE=1`: styled against plain). DISC-008 accurate (umask 002: 0664 against 0644). DISC-009, DISC-012: probe rows SAME on three lanes; **DISC-012's resolution ("descriptor 0 itself is read", SEQPACKET listed as SAME) is incomplete: R11-2**. DISC-010: not run (memory). DISC-011 accurate (`RLIMIT_AS` 4 GiB: `bend: reservation failed` for `--version`; `VmSize` 8592046824 kB, 2 threads); its "use the JavaScript build" does not hold without `/proc` (R11-1, side observation). DISC-013 accurate (`perf/inputs/doubles_20000.json`: 0.008 s against 1.16 s, x143; `sci_5000.json`: 0.005 s against 1.23 s, x237; same bytes). All 14 "Impact measured" lines say 1065; one "Affected cases" line still says 1053 (R11-8a). No entry's status is wrong (12 ACCEPTED, 2 RESOLVED).
- **README.md**: R11-4, R11-9, R11-11; everything else I checked is accurate, including the Status section's commits (`c7239e2` builds and passes) and both performance tables. **docs/PORT_REPORT.md**: R11-3, R11-6, R11-8d; constants otherwise reproduce. **docs/PORT_STATE.md**: every pasted gate line I could run reproduces verbatim (probe lines included); R11-8b, c, f. **docs/PARITY_RUNBOOK.md**: R11-5, R11-10, R11-11b. **CONTRIBUTING.md**: section 2 runs as written and passes (I wrote the binary into my scratch directory instead of `/tmp`); accurate. **.github/CODEOWNERS**: accurate. **docs/FEATURE_PARITY.md**: counts and every law name accurate; header R11-7. **perf/PERF-LEDGER.md, NEGATIVE-EVIDENCE.md, EXPERIMENTS.md**: numbers reproduce from `perf/evidence`; R11-8e, R11-10. **AGENTS.md**: equals the author's working copy; structure, gate table and build times accurate; its claims-lint file list differs from three others (R11-5). **Script headers** (`diff-fuzz.py`, `stdio-probe.py`, `hand-mutants.py`, `board-refresh.py`, `clean-build-check.sh`), `bin/toon`, `port/main.bend`, `port/LAWS.bend`: accurate (usage, exit codes, row and mutant counts), except that `bin/toon`'s header promises the original's behavior for closed descriptors without the `/dev/fd` caveat (R11-1) and `scripts/ORIGIN.md` (R11-10).
- **Earlier rounds' repairs re-checked**: R10-1 (every cited commit builds: repaired, but see R11-3), R10-2 (README examples: repaired), R10-3 (`PATH=` : repaired, parameter expansion), R10-4 (scaffold files: filled and accurate), R10-6 (counts: repaired except R11-8), R10-10 (repaired for `pin-check.sh`; the same traceback remains in two other scripts, R11-6), R9-3 (one line missed, R11-8a), R8-4 / R8-5 (launcher links and thread values: hold).

## 4. Audit trail

- No file deleted anywhere; nothing written under `/tmp` or inside `/data/projects/toon_bend` (there I ran only `git rev-parse HEAD`, `git status --short`, `git ls-files`, `diff -r`, and read files; `git status` may refresh the index's stat cache, as in round 10). In my clone: `git clone`, read-only `log / show / ls-tree / diff / status`, `git archive` through `clean-build-check.sh` (14 exports kept under `review_R11/tmp/`); the README's `-o port/toon` build left one ignored file there; `git status --short` stayed empty after every gate.
- Processes ended by me: one background job of my own (TaskStop of `l3b_real.py` at file 1011 of 1742 when another agent's build left 1 GB available; resumed from file 1012), and children of my own probes through `subprocess` timeouts (the DGRAM rows, 15 s each; the probe's own HANG rows). I signalled nothing I did not start.
- The safety hook refused one shell command (a variable-named executable followed by `-e`); I allowlisted nothing and drove every run from Python files. One `sed -i` edited a file of my own in `review_R11/`.
- Special files I created under `review_R11/work/`: symbolic links (to the original as `tn`, to the port as `tp`, launcher links, one dangling), directories named with a newline / a space / a leading dash, files named `-dash.json`, `star*.json`, `nl<LF>name.json`, `-x.json`, `=v.json`, an empty executable, a mode-0644 script.
- Not run, by instruction: `lanes.sh`, `port-doctor.sh`, `hand-mutants.py`, `harness-selftest.sh`, cargo, `bend update`, anything in `legacy/`. Not testable here: macOS, DISC-010's port half (memory), a real chroot (R11-1 was reproduced with `bwrap --tmpfs /proc`, which needs no root).
