# Round 14 — non-author hostile review of the Bend 2 port of Toon

| field | value |
|---|---|
| clone | `/data/tmp/review_R14/clone` (`git clone https://github.com/Dicklesworthstone/toon_bend`) |
| `git rev-parse HEAD` | `6d7cfcc7940468d83a6a912d8191f6120df7b507` (subject: `hand-mutants: all 25 in one run -- killed 25, survived [], verdict STRONG`) |
| working tree | clean at every gate run (`git status --porcelain` empty except the build outputs `./toon`, `port/toon`, which the README's own Quick Start creates) |
| bend | `bend 2.0.16`, checkout `/tmp/bend` at `15ae0c86f3193b8f645b4bedbc438655b648d0da`, run as `bun /tmp/bend/bend2/main.ts` |
| toolchain | bun 1.4.2 · Ubuntu clang 21.1.8 · Python 3.14.4 · Linux 7.0.0-30-generic x86_64, AMD EPYC-Milan, 8 cores, SHARED host (load 4.5–5.9 throughout) |
| port binary (native, c-1t lane) | `bun /tmp/bend/bend2/main.ts port/main.bend -o bin/toon` → sha256 `27787b9babd0a8388b92070437548a1a847a9f62c30119442355b7723cd76465` (18.2 s) — **identical to the sha256 `27787b9b…` that `perf/NEGATIVE-EVIDENCE.md` NE-006 and NE-007 name: the byte-reproducible-build claim is reproduced** |
| port build (js lane) | `bun /tmp/bend/bend2/main.ts port/main.bend -o bin/toon.js` → sha256 `1a273ccba7eb435918086c37672f8f3845d304c4d25c7e500aadc27e793625a0` |
| six mutant binaries | `toon.R14a` `eb2177b9fd0a45e4…` · `toon.R14b` `66242c8d2cdbbdac…` · `toon.R14c` `fdaf52923bfe24c0…` · `toon.R14d` `d31e672467e87a34…` · `toon.R14e` `1c0813cacb39969b…` · `toon.R14f` `f09536a9876b7b82…` |
| oracle | `/data/projects/toon_bend/oracle/toon`, copied read-only to `clone/oracle/toon`; sha256 `980f2b26484f424b74cb0a0b61062183d0038d45101590a38953a82611879a6d` — **exactly `docs/PIN.toml`'s value** |
| date | 2026-09-21 local (UTC−4); the gate lines below carry whatever timestamp the gate itself printed |

**Nothing in `/data/projects/toon_bend` was modified.** The only read of it was `cp oracle/toon` (and `ls legacy/`). **Nothing in `/dp/toon_rust` was read, written or checked out.** **Nothing was deleted anywhere, at any point, including files I created myself.** **No file under `goldens/` was edited** in the clone or anywhere else. Eleven documents in MY clone were temporarily falsified to attack `scripts/claims-audit.py` (finding R14-2) and were restored by copying back byte-identical backups; `git status --porcelain` was empty afterwards and `claims-audit.py` printed the same line as before.

---

## 1. Findings

**HIGH 0 · MEDIUM 5 · LOW 10** (15 total).

By `references/PARITY-GATE.md`'s strict counting rule — "a genuine finding changes bytes, an exit code, a proof, or the corpus" — **R14-1 is the one that changes a proof**; R14-2 changes what a gate's exit code means; R14-14 and R14-15 are defects of the SPEC (a reader of the clause alone predicts the wrong bytes); the other eleven are facts in claim-bearing documents that the repository itself refutes, which is the category rounds 9–13 counted as genuine and which `docs/PORT_STATE.md`'s rounds table counts. I report both numbers rather than choose.

**Zero differences between the port and the pinned original were found in 11 638 compared executions** (§2), the ONE exception being a difference the register already owns accurately (DISC-004, R14-16 below). The port's *conversion behaviour* survived every lens I ran. Every finding below is about the proof's coverage, the gates that check the claims, or the claims themselves.

| id | sev | lens | reproduction (`file:line`) | expected | observed | class |
|---|---|---|---|---|---|---|
| **R14-1** | MEDIUM | do the laws bite? (5 new hand-written semantic mutants, FULL 374-law proof) | `python3 /data/tmp/review_R14/mut.py` then `python3 /data/tmp/review_R14/validate_mutants.py`. Sites: `port/cli.bend:930` (`lower.rev`), `port/cli.bend:315` (`seven_tenths`), `port/text.bend:93` (`is_ws`), `port/f64.bend:960` (`json.is_plain`), `port/encode.bend:23` (the CR arm of `needs_quote`), control `port/cli.bend:1045` (`est.fin`) | a mutant that breaks corpus cases is KILLED by some law, or the gap is recorded | **all five SURVIVED `All terms check.`** on the whole 374-law proof (262–274 s each) while each breaks captured cases on c-1t: R14a 1 (`auto_upper_toon_ext`), R14b 7 (`usage_enum_similar_*`, `usage_similar_arg_*`), R14c 2 (`stats_ws_each`, `encstr_ws_edge_sweep`), R14d 7 (`decnum_ints`, `decfmt_exp_upper_boundary`, …), R14e 3 (`encstr_quoting`, `encstr_escapes_in_all`, `fx_enc_primitives_13`) — full list in §2. A no-op control (R14f) passes 1065/1065 and also survives, so the method is calibrated. Five more law-coverage gaps of exactly round 13's R13-9 shape, in five DIFFERENT defs and five different clause groups | NEW |
| **R14-2** | MEDIUM | the claim-checking gates themselves | falsify, in the clone, `docs/PORT_STATE.md:19,20,22,37,43`, `docs/PORT_REPORT.md:23,32,41`, `docs/FEATURE_PARITY.md:58`; then `python3 scripts/claims-audit.py && ./scripts/claims-lint.sh <the 11 files> && ./scripts/state-check.sh docs/PORT_STATE.md` | a falsified pasted gate line or count is caught | **eleven independent lies all pass, `{"findings": 0, … "verdict": "OK"}`, `claims-lint: 0 hit(s) in 11 file(s)`, `state-check: 0 finding(s)`.** Worst of them: `docs/PORT_REPORT.md:32`'s pasted `converge.sh` line flipped to `"verdict": "CONVERGED", "missing": []}`: HOLDS` — `claims-audit.py` RUNS `converge.sh` (`scripts/claims-audit.py:135-146`) and holds its real output, but only compares the `rounds` number (`:174`). Also undetected: the proof row's unsafe count `unsafe 7 = 7 @unsafe`; the floor line as `"stable":12 … "verdict":"UNSTABLE"`; the js lane as `"verdict":"FAIL"` with `"stderr_compared":false`; `clean-build-check` as `"verdict":"FAIL"`; the self-test as `"caught":2`; the mutant tally as `"killed": 3`; the law-coverage line as `"unsafe": 9`; the ORACLE's sha256 (the one number `docs/PIN.toml` exists to pin); a gate row's date moved to 2025-01-01; and the two law counts of R14-4. **And the mechanism behind it**: `scripts/claims-audit.py:44`'s `HISTORY` regex begins `` `[0-9a-f]{7,40}` ``, so ANY line containing a backtick-quoted hex string of 7+ characters is exempt from EVERY count check — and this project's own convention puts the compiler checkout `` `15ae0c8` `` on every proof line. `docs/PORT_STATE.md:36` (the one line that states the whole law breakdown) and `docs/PORT_REPORT.md:22` both end in `bend 2.0.16 @ `15ae0c8``, so I rewrote `:36` to **"7 laws = 1 quantified + 2 closed unit laws + 4 closed whole-pipeline"** and `:22` to "999 laws" and `claims-audit.py` still printed `{"findings": 0, … "verdict": "OK"}` exit 0 (only `historical_lines_exempt` moved, 1 → 6). The pattern: **`claims-audit.py` checks numbers that appear as `<digits> <noun>` outside an exempt line, plus a handful of named JSON fields; it checks no verdict string, no sha and no date anywhere, and a commit reference buys a blanket exemption for its whole line** | NEW (same class as R13-6, disjoint holes) |
| **R14-3** | MEDIUM | perf ledger vs its own artifact | `perf/NEGATIVE-EVIDENCE.md:144` vs `scripts/incumbent-bench.sh:80-82` and `perf/EXPERIMENTS.md:222` | the "Exact command" reproduces the capture | NE-007's Exact command reads `--runs 18` and justifies the number in-line: "`--runs 18` is read back from the JSON's `samples`, one sample per arm per pair at `scripts/incumbent-bench.sh:80-95`". The cited loop is `for pair in range(args.runs): for arm in ('original','port','port','original')` — **TWO samples per arm per pair**, so `samples: 18` means `--runs 9`. The EXP-005 card's own one-line invocation at `perf/EXPERIMENTS.md:222` says `--runs 9`, and NE-001 (`--runs 9` → "18 samples per arm") and NE-006 (`--runs 15` → "30 samples per arm") use the right factor. The ledger row therefore names a command that would produce twice the samples of the artifact it cites, and states a false fact about a script while citing its lines | NEW |
| **R14-4** | MEDIUM | claims audit (law counts) | `docs/FEATURE_PARITY.md:57-58` and `docs/PORT_REPORT.md:41` vs `docs/FEATURE_PARITY.md:11`, `docs/PORT_STATE.md:36`, `port/LAWS.bend` | the proof-coverage breakdown sums to the total | `port/LAWS.bend` holds 374 laws = 14 quantified + **65** closed unit + 295 `golden_*` (I counted them: `grep -c '^law '` = 374, `^law golden_` = 295, laws with a `for` binder = 14). `FEATURE_PARITY.md:11` and `PORT_STATE.md:36` say 65. But the proof-coverage table two screens below says `laws about the fast twins \| 39` (2 of which are the quantified gate pair) + `other closed unit laws \| 22` = **59**, and `PORT_REPORT.md:41` says "**354** closed laws … **59** unit laws" where the file has 360 / 65. **The six missing are exactly round 13's six new laws** — `tab_in_value_forces_quotes`, `utf8_rejects_surrogate`, `utf8_accepts_below_surrogate`, `writer_a_keeps_del_raw`, `writer_b_escapes_del`, `writer_b_escapes_c1`. They ARE cited on the feature rows above; only the counting table and the report were left behind. `claims-audit.py` misses it because its pattern is `\b(\d+) closed unit laws\b` and both wrong statements put the number elsewhere (`\| other closed unit laws \| 22 \|`, "59 unit laws") | NEW |
| **R14-5** | MEDIUM | the pins, from a fresh clone (the state file's own Next action) | `./scripts/pin-check.sh docs/PIN.toml` in the clone vs `docs/PORT_STATE.md:43`; `.gitignore:15`; `goldens/MANIFEST.txt:6` | the claim "TWO rows are not green", with `manifest_hashes` GREEN | In a clone of the PUBLIC repository **four rows are not green**: `original_present` RED, `original_commit` RED, **`manifest_hashes` RED** (`capture integrity failed: changed input <clone>/-; captured provenance does not cover current command and case inputs`), `manifest_version` YELLOW. `PORT_STATE.md:43` asserts `manifest_hashes` GREEN and names only two non-green rows. The cause is structural, not the drifted sibling checkout: MANIFEST's capture provenance fingerprints the file `-` (sha256 `01ba4719…`, which is sha256 of `"\n"`), and `.gitignore:15` is `/-`, so **no clone of the published repository can ever produce that row GREEN** — the same `.gitignore`-hides-a-needed-input shape as round 10's HIGH finding R10-1. PORT_STATE's own Next action instructs round 14 to work from a fresh clone, so the claim it makes is not the claim an auditor can check | NEW (adjacent to R10-10, which was about the traceback, now repaired) |
| **R14-6** | LOW | claims audit (rounds) | `docs/PORT_REPORT.md:12` vs `:32`, `:25`, `:52` and `docs/reviews/round-13.md` | the verdict names every non-author round | "the **seven** non-author rounds, **6 to 12**, found 6, 11, 8, 3, 10, 12 and 13 findings … 0 differences in conversion content in rounds 9 to **12**". There are EIGHT (6–13); round 13 found 9 and its report is in the repository. The same file's pasted converge line at `:32` reads `"rounds": 13`, and its own `:25` and `:52` cite round 13's work. `claims-audit.py`'s round check compares PORT_STATE's table to the reports, never PORT_REPORT's prose | NEW |
| **R14-7** | LOW | claims audit (rounds) | `docs/PORT_STATE.md:104` vs `:10` and the rounds table at `:71-83` | the dirty-tail count agrees with the table | "Current: NOT_CONVERGED (the last **six** rounds are not clean…)". Rounds 6–13 all carry `clean? = no`: that is **eight**, which is what `:10` says ("EIGHT non-author rounds in a row were not clean"). Commit `761da45` repaired the header from seven to eight and left this sentence behind | NEW |
| **R14-8** | LOW | claims audit (dates) | `docs/PORT_REPORT.md:30` vs `:25` | the stated constant holds | The constant row is "evidence ≤ 24 h old on this commit \| **every line of this report is dated 2026-09-20**". Line 25 of the same file reads "all 25 mutants in ONE run (**2026-09-21**, 52 min)", and `:33` and `:52` carry 2026-09-21 material (the 16 REFUSED_CV files, NE-007, round 13's R13-1). The report's header date is also `2026-09-20` while its newest content is `2026-09-21` | NEW |
| **R14-9** | LOW | references (git objects) | `.beads/issues.jsonl:3` (bead `toon_bend-0m1`, `close_reason`) | a cited commit exists | `close_reason` says "Reverted in **0d35d55**"; `git cat-file -t 0d35d55` → `fatal: Not a valid object name 0d35d55` in a fresh clone. The revert that is in the published history is `2dee063`. `perf/NEGATIVE-EVIDENCE.md:149` names the same object but qualifies it honestly as "the dangling commit `0d35d55`"; the bead does not, and `.beads/issues.jsonl` is tracked. `claims-audit.py` checks bead ids and paths, never commit ids | NEW |
| **R14-10** | LOW | the state file | `docs/PORT_STATE.md:112` and `:114` | one closing paragraph | The paragraph "Why round 14 is the one thing that moves the tier…" appears **twice**, and the two copies disagree: `:112` ends "every gate a reviewer re-runs is pasted above with the commit it was produced on", `:114` ends "**the four gates** a reviewer re-runs is pasted above" (also ungrammatical). The gate table at `:17-23` has **FIVE** rows (proofs, lanes, parity, floor, kill-switch). `./scripts/state-check.sh docs/PORT_STATE.md` → `0 finding(s)` | NEW |
| **R14-11** | LOW | the state file (provenance) | `docs/PORT_STATE.md:29` vs `git diff --stat 2dee063 HEAD` | the enumeration is complete | "The commits between them changed documents (`README.md`, `docs/PORT_REPORT.md`, this file, `perf/EXPERIMENTS.md`, `perf/NEGATIVE-EVIDENCE.md`) and two scripts the doctor does not run". `git diff --stat 2dee063 HEAD` lists an eighth tracked file the sentence omits: `.beads/issues.jsonl` (8 lines). The *substantive* claim — that nothing the doctor gated changed — I verified and it HOLDS (see §2) | NEW |
| **R14-12** | LOW | the state file (gate prose) | `docs/PORT_STATE.md:39` vs `scripts/harness-selftest.sh:11-22` | the prose names the twelve mutations it pastes | The pasted line says `"mutations":12,"caught":12`; the prose list beside it enumerates ELEVEN ("an edited golden, a missing golden, an empty manifest, a renamed case, the original run as the port, an unproved law, a proof of no law, a deferral word in a ledger, an intention in this file, a `present` row without evidence, a silent re-capture"). The missing one is **M12** (`scripts/harness-selftest.sh:22`, "a case added, the documents' counts left behind → claims-audit.py FINDINGS") — the mutation that guards the very gate this session extended, and the one that was UNTESTABLE in the run `docs/PORT_REPORT.md:24` pastes | NEW (partial DUP_OF_PRIOR of R13-3, which was about the 11/11 line in PORT_REPORT) |
| **R14-14** | LOW | spec-clause prediction (207 clauses predicted, 322 oracle/port pairs) | `docs/EXISTING_Toon_STRUCTURE.md:629` (S4.226) and `:36,:44,:60` | a clause's `examples (input → output)` column predicts the program's stdout | S4.226's example reads `items[3]:⏎  - 1⏎  - a: 1⏎  - text` → `[1.0,{"a":1.0},"text"]`. The oracle (and the port) print `{⏎  "items": [⏎    1.0,⏎    {⏎      "a": 1.0⏎    },⏎    "text"⏎  ]⏎}`: the column silently writes the decoded SUB-value in compact JSON, dropping the wrapping root object and the writer's real layout. The spec declares its abbreviations in `### Notation, part A` (`:36`), `part C` (`:44`) and `part D` (`:60`) — **there is no `Notation, part B` and no `Notation, part E`**, so this one is undeclared. At least S4.220, S4.221, S4.223, S4.225, S4.226 and S4.227 use it. `python3 scripts/spec-lint.py` → `0 finding(s)`, and the spec's exit gate is a self-containment review ("ten cases predicted from the spec alone"), which this notation defeats for part E | NEW |
| **R14-15** | LOW | spec-clause prediction | `docs/EXISTING_Toon_STRUCTURE.md:137` (S1.32) vs `:136` (S1.31) | the failure reasons a clause lists are reachable as written | S1.32 lists, among `--flatten-depth`'s failure reasons, "a leading `-` (also `-0`) … → `invalid digit found in string`". A separate word `-0` never reaches that parser: it is a short cluster (S1.5c) and the oracle answers `error: unexpected argument '-0' found` + `tip: to pass '-0' as a value, use '-- -0'` + the fixed usage, exit 2 (S1.77) — confirmed against the oracle. Only `--flatten-depth=-0` produces the text S1.32 promises. S1.31 carries exactly the missing note for `--indent` ("`-0` (writable only as `--indent=-0`, S1.7)"); S1.32 omits the analogue. The port matches the oracle on both spellings | NEW |
| **R14-16** | — (not a finding: confirmation) | spec-clause prediction | `--indent $'\xff'`, `--delimiter $'\xff'`, `--flatten-depth $'\xff'`, `--indent=$'\xff'` | the register describes the divergence accurately | The ONLY port/oracle byte difference in the lens's 322 pairs: oracle `error: invalid UTF-8 was detected in one or more arguments` + blank + `Usage: toon [OPTIONS] [INPUT]` + blank + the help hint, exit 2; port `error: invalid value '<U+FFFD>' for '--indent <INDENT>': invalid digit found in string` + the help hint, exit 2. This is **DISC-004** (Platform, ACCEPTED) and **OQ-A7**, and the register at `docs/DISCREPANCIES.md` records **both texts verbatim with the right cause** and the right boundary — `--key-folding`/`--expand-paths` (S1.84), INPUT and `-o` (S1.22, S9.18) still match byte for byte, which I re-confirmed. **The register is ACCURATE.** Recorded here so the next round does not re-raise it | DUP_OF_PRIOR (R7-10) |
| **R14-13** | LOW (nit) | README examples | `README.md:37` (`bun … port/main.bend -o toon`) vs `.gitignore` | following the README leaves a clean tree | `.gitignore` ignores `port/toon` but not a bare `toon`, so the README's first build command drops an untracked 822 KB binary at the repository root that `git status` then reports — in a project whose session protocol begins with `git status`. Either ignore `/toon` or have the Quick Start build into `port/toon` as the second block already does | NEW |

### Findings I looked for and did NOT find (the claims that held)

Each of these is a claim I tried to break and could not; they are listed so the next round does not re-spend the budget.

| claim | how I checked | verdict |
|---|---|---|
| EXP-005 is fully reverted; `port/` is byte-identical to `3b67865` | `git diff --stat 3b67865 HEAD -- port/` empty; `git diff --stat 2dee063 HEAD` lists no `port/` path | **HOLDS** |
| nothing still references `pow10.by4` | `grep -rn 'pow10.by4\|pow10_by4\|by4'` over the whole clone: only `.beads/issues.jsonl`, `perf/EXPERIMENTS.md` (the card), `perf/NEGATIVE-EVIDENCE.md` (NE-007) and `perf/evidence/EXP-005.ab.json`. **No law, no lint rule, no board row, no script, no `port/` file** | **HOLDS** |
| `perf/evidence/EXP-005.ab.json` says what NE-007 says it says | medians 1312.1871 → 1208.7233 (NE-007: 1312.19 → 1208.72 ✓), cv 3.5640 / 5.4502 (✓ 3.56 / 5.45, the port arm above the 5.0 `max_cv`), `samples: 18` each (✓), `"ratio": null` (✓ "no ratio is claimed"), stdout sha `494b6050…` and stderr sha identical on both arms (✓), `exit: 0` both, baseline executable sha256 `27787b9b…` and lever `c0393686…` (✓ both quoted correctly) | **HOLDS** |
| NE-007's arithmetic | (1312.19 − 1208.72)/1312.19 = 7.88% → "about 8% apart" ✓; 8% ≪ the card's precommitted ≥ 25% ✓; the four independent refusal reasons are each verifiable, including the PL-11 rule in `scripts/port-lint.py` | **HOLDS** (except the `--runs` number, R14-3) |
| NE-007 claims no ratio | the entry says so three times and the JSON's `ratio` is `null`; no document quotes an EXP-005 ratio | **HOLDS** |
| **16** files in `perf/evidence/` carry `REFUSED_CV`, and the split in `docs/PORT_REPORT.md:33` | `grep -l REFUSED_CV perf/evidence/*.json \| wc -l` = 16. File by file: 5 INCUMBENT (`startup`, `strings`, `decimals`, `ints`, `sci`) + 4 one-lever arms (`EXP-001.one-lever.{aa,ab}`, `EXP-002.one-lever.{aa,ab}`) + 1 folding (`EXP-004.fold-vs-original`) + 2 pinned-build re-captures (`INCUMBENT-Z.{wide-rows,expand}-recapture`) + 3 opt-level=3 (`INCUMBENT-O3.fold`, `INCUMBENT-O3.tabular-enc`, `INCUMBENT-O3.vs-z-tabular-enc`) + 1 EXP-005 = **16, exactly the stated partition**, and no ratio is claimed from any of them | **HOLDS** |
| no golden, lane, floor or parity line depends on the drifted `/dp/toon_rust` checkout | `goldens/MANIFEST.txt:2-6` names `./oracle/toon` with sha256 `980f2b26…` and nothing else; 0 rows of `goldens/cases.tsv` mention `legacy` or `toon_rust`; `grep -rn legacy scripts/ cases/` reaches only `hook-guard.py` (the Phase-3 read guard), `harness-selftest.sh` (links it when present), `diff-explore.sh` (a default out-dir) and doc strings; in `docs/PIN.toml` only `original.path` and `original.build_cmd` (a REBUILD) read it. Every gate I re-ran below produced its line with `legacy/Toon` absent altogether | **HOLDS — the claim is correct** |
| the doctor table's inputs are byte-identical at HEAD although the run started on `2dee063` | `git diff --stat 2dee063 HEAD` touches only documents, `.beads/issues.jsonl` and two scripts; I read both script diffs in full: `scripts/hand-mutants.py` gained ONLY a docstring paragraph and two comment blocks, `scripts/claims-audit.py` gained the `refused` fact and one `counts` row — neither is run by `port-doctor.sh`. No script the doctor runs changed | **HOLDS** (enumeration nit: R14-11) |
| every date follows the rule "the five gate rows are UTC, the prose is local" | the commits are all `-0400`; the doctor run's local 20:46–22:26 on 2026-09-21 is UTC 00:46–02:26 on 2026-09-22, which is what the five rows print ✓; `EXP-005`/`EXP-006` `created (UTC) \| 2026-09-20` = commit `c7239e2`, 2026-09-20 12:54 −0400 = 16:54 UTC ✓; NE-007's header `[2026-09-21 …]` is local and says so at `:141`, and its reconstruction of `91927dd` as "Sun Sep 20 20:51:43 2026 -0400" is exactly `git show -s --format=%ai 91927dd` ✓ | **HOLDS** except `docs/PORT_REPORT.md:30` (R14-8) |
| every README example and every command the documents tell a reader to run | all seven runnable README examples reproduce byte for byte, including the launcher path `bin/toon --encode < cases/inputs/hand/happy_readme_users.json` and the interpreter `$BEND_CLI port/main.bend -- --version` → `toon 0.2.4`; `README.md`'s `670376` bytes for the pinned oracle is `stat -c %s oracle/toon` exactly; 349 `fx_*` cases = 349 vendored fixture files | **HOLDS** |
| `hand-mutants.py`: 25 mutants, 25 killed, 22 distinct laws | `len(MUTANTS)` = 25 with ids `M01..M12`, `M14..M26` (no `M13`, as the docstring now says); the arithmetic 25 kills − 2 (one law killing three) − 1 (one law killing two) = 22 distinct ✓. `M24`/`M25`/`M26`'s killing laws (`writer_b_escapes_del`, `utf8_rejects_surrogate`, `tab_in_value_forces_quotes`) are non-golden and therefore inside the reduced law set the JSON discloses (`"reduced": true`) | **HOLDS** (I did not re-run the 52-minute set; I wrote five NEW mutants instead — R14-1) |
| MANIFEST holds 3195 hashes | `grep -c '^  [0-9a-f]\{64\}  ' goldens/MANIFEST.txt` = 3195 | **HOLDS** |
| `docs/reviews/round-13.md` lists 9 findings and the table says 9 | 9 `R13-` rows | **HOLDS** |
| the rounds table, OQ register and DISC register are structurally complete | 27 OQ rows, every one `RESOLVED`/`SUPERSEDED`; 15 DISC entries, 13 ACCEPTED + 2 RESOLVED, 0 OPEN, every ACCEPTED with a scoped `Resolution:` and the delegation quoted; every `Impact measured:` row says `0 of 1065 cases` with the corpus commit named, so the stale-denominator defect of rounds 6/9 stays repaired | **HOLDS** |
| DISC-013's range is consistent everywhere | "45 to 306" in `docs/DISCREPANCIES.md:156` and in `docs/PORT_REPORT.md:65`; no "45 to 300" survives in any document (R12-10 / R13-5 repaired) | **HOLDS** |
| `scripts/port.sh claims` is green on the published tree | `0 hit(s) in 11 file(s)`, exit 0 (R12-9 repaired) | **HOLDS** |

---

## 2. What I ran

Every line below is this round's own output on this clone, not a paste from the documents. Exit codes are the command's.

### The documented gates, re-run

| gate | last line | exit |
|---|---|---|
| `(cd port && bun /tmp/bend/bend2/main.ts PROOF.bend)` | `All terms check.` — 3 min 59.5 s wall (311 s user) | 0 |
| `./scripts/law-coverage.sh` | `{"laws": 374, "proofs": 374, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | 0 |
| `./scripts/conform.sh goldens/cases.tsv goldens --lane c-1t -- ./toon --` | `…"passed":1065,"failed":0,"inconclusive":0,"stderr_compared":true,"verdict":"PASS","oracle_identity_checked":true}` | 0 |
| `./scripts/conform.sh … --lane js -- python3 scripts/js-lane.py bin/toon.js --` | `…"passed":1065,"failed":0,"inconclusive":0,"stderr_compared":true,"verdict":"PASS","oracle_identity_checked":true}` | 0 |
| `TOON_SPEC=1 ./scripts/conform.sh … --lane c-1t -- ./toon --` (kill-switch parity) | `…"passed":1065,"failed":0,"inconclusive":0,"stderr_compared":true,"verdict":"PASS","oracle_identity_checked":true}` | 0 |
| `./scripts/parity-board.sh docs/FEATURE_PARITY.md` | `{"rows": 33, "present": 27, "partial": 0, "missing": 0, "excluded": 6, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | 0 |
| `./scripts/converge.sh docs/PORT_STATE.md` | `{"tier": "T2", "rounds": 13, "clean": 5, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": [], "verdict": "NOT_CONVERGED", "missing": ["clean rounds since last reset 0 < 2"]}` | 1 |
| `./scripts/state-check.sh docs/PORT_STATE.md` | `state-check: 0 finding(s)` | 0 |
| `python3 scripts/claims-audit.py` | `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 1, "laws": 374, "cases": 1065, "disc": {"accepted": 13, "resolved": 2, "open": 0}, "verdict": "OK"}` | 0 |
| `./scripts/claims-lint.sh README.md CONTRIBUTING.md docs/PORT_REPORT.md docs/PORT_STATE.md docs/PARITY_RUNBOOK.md docs/DISCREPANCIES.md docs/OPEN_QUESTIONS.md perf/*.md` | `claims-lint: 0 hit(s) in 11 file(s)` | 0 |
| `./scripts/pin-check.sh docs/PIN.toml` | `pin-check: RED` — **four rows not green in a clone**: `original_present` RED, `original_commit` RED, `manifest_hashes` RED, `manifest_version` YELLOW (R14-5) | 2 |
| `./scripts/harness-selftest.sh -- ./oracle/toon` | `{"schema":"p2b.harness-selftest.v1","sha":"6d7cfcc","bend":"bend 2.0.16","host":"Linux-x86_64","lane":"c-1t","mutations":12,"caught":12,"leaked":[],"untestable":[],"scratch":"/data/tmp/review_R14/tmp/hst.EwwsAZ","verdict":"OK"}` — 12/12 CAUGHT, 0 untestable, **on HEAD** (the pasted line is from `77a63d9`) | 0 |
| `python3 scripts/stdio-probe.py -- ./toon --` | `{"rows": 27, "same": 13, "known": [14 rows: DISC-003 ×5, DISC-006 ×4, DISC-007 ×2, DISC-014 ×1, DISC-015 ×3], "fixed": [], "new": [], "verdict": "PASS"}` — **byte-identical to the line `docs/PORT_STATE.md:44` pastes for the native binary** | 0 |
| `python3 scripts/port-lint.py port/*.bend --laws port/LAWS.bend` | `{"files": 10, "findings": 41, "errors": 0, "warnings": 0, "infos": 41, "by_rule": {"PL-02": 41}, "laws": "port/LAWS.bend", "verdict": "OK"}` | 0 |
| `python3 scripts/spec-lint.py docs/EXISTING_Toon_STRUCTURE.md goldens/cases.tsv` | `spec-lint: 703 clauses, 1065 cases, 1065 cases cited, 0 finding(s)` | 0 |
| `python3 cases/build-cases.py --check` | `{"cases": 716, "drift": [], "verdict": "OK"}` / `{"cases": 349, "drift": [], "verdict": "OK"}` / `{"cases": 1065, "verdict": "OK"}` | 0 |
| `./scripts/cases-lint.sh goldens/cases.tsv` | `{"cases": 1065, "errors": 0, "notes": 20, "classes_missing": "", "verdict": "OK"}` | 0 |
| `./scripts/port.sh claims` | `claims-lint: 0 hit(s) in 11 file(s)` | 0 |
| `./scripts/conform.sh … --lane c-1t` × 6 mutant binaries (R14-1's validity check) | `1064/1`, `1058/7`, `1063/2`, `1058/7`, `1062/3` failed, and `1065/0` for the no-op control | 1 ×5, 0 ×1 |

I did **not** re-run the full `port-doctor.sh` (its own documents put it at ~1 h 40 m and the host was shared with another agent throughout); I ran each of its five constituent gates separately, above, plus the interpreter lane's substitute (the proof) — every one reproduced the pasted line. I did **not** re-run `floor.sh` (it re-runs the oracle 1065 × K times) nor the 52-minute `hand-mutants.py` set; I spent that budget on five NEW mutants instead, which is where R14-1 came from.

### Differential lenses (port vs the pinned oracle, byte for byte on stdout, stderr and exit)

| lens | executions compared | differences |
|---|---|---|
| `scripts/diff-fuzz.py mutate --seed 1414 --runs 1200` | 1200 | 0 |
| `scripts/diff-fuzz.py docs --seed 1414 --runs 1200` | 1200 | 0 |
| `scripts/diff-fuzz.py expand --seed 1414 --runs 1200` | 1200 | 0 |
| `scripts/diff-fuzz.py collide --seed 1414 --runs 1200` | 1200 | 0 |
| `scripts/diff-fuzz.py argv --seed 1414 --runs 1200` | 1200 | 0 |
| `scripts/diff-fuzz.py numbers --seed 1414 --runs 1200` | 1200 number texts in 6 documents | 0 |
| **my own** `lens.py ws` — all 25 Unicode White_Space scalars + 4 near-misses (U+180E, U+200B, U+FEFF, U+2060) in values, keys, list items, tabular cells, under `--encode`, `--stats`, `--key-folding safe`, `--delimiter pipe` | 812 | 0 |
| **my own** `lens.py numbers` — 195 number texts across the JSON writers' plain/exponent boundary (1e−10…1e21, 1e15/1e16/1e17, `5e-324`, `2.2250738585072014e-308`, `1.7976931348623157e308`, a 30-digit integer), encode and decode, `--indent 0`, `--expand-paths safe` | 880 | 0 |
| **my own** `lens.py ext` — 14 case spellings of the `.toon`/`.json` extension as real files, with and without `--stats` | 56 | 0 |
| **my own** `lens.py ctrl` — every C0 control, DEL, U+0080, U+0085, U+009F, U+00A0 in a value, a key and a tabular cell, under three delimiters/options | 333 | 0 |
| corpus conformance, three lanes (c-1t, js, c-1t under `TOON_SPEC=1`) | 3195 case comparisons | 0 |
| `scripts/stdio-probe.py` descriptor states | 27 paired states | 0 NEW, 0 FIXED |
| README examples run verbatim | 7 | 0 |
| spec-clause prediction lens: 207 distinct clauses of S1, S2, S4, S5, S6, S9, S10, each turned into an input and run on both programs | 322 | **1**, and it is DISC-004 (R14-16) — 0 unregistered |
| **total** | **11 638 compared executions** | **0 unregistered differences in conversion content** |

### The five new law mutants (the round's centrepiece)

Each is ONE exact-text replacement, applied to a fresh copy of `port/`, with the **whole 374-law proof** run in the copy (not the reduced 129-law set `hand-mutants.py` uses by default). Then the mutated port was built and `scripts/conform.sh --lane c-1t` run over all 1065 cases, so "the mutant changes bytes" is measured, not asserted.

| id | module:def | the mutation | spec | proof | corpus |
|---|---|---|---|---|---|
| R14a | `port/cli.bend:930` `lower.rev` | `U32.is_le(c, 90)` → `U32.is_le(c, 83)`: the extension lowercaser stops folding `T`…`Z`, so a `.TOON` path is no longer detected as TOON | S4.301–S4.303 | **SURVIVED** (268.8 s) | 1: `auto_upper_toon_ext` (stdout, stderr AND exit all differ — the file is encoded as JSON and fails) |
| R14b | `port/cli.bend:315` `seven_tenths` | `[7]` → `[8]`: clap's similarity threshold moves from 0.7 to 0.8, so borderline tips vanish | S1.74, S1.75 | **SURVIVED** (271.6 s) | 7 (stderr): `usage_enum_similar_picks_best`, `usage_enum_similar_tie`, `usage_enum_similar_boundary_30`, `usage_similar_arg_help`, `usage_similar_arg_nope`, `usage_similar_arg_tie`, `usage_similar_arg_after_commit` |
| R14c | `port/text.bend:93` `is_ws` | `U32.is_eq(c, 5760)` → `U32.is_eq(c, 5761)`: U+1680 OGHAM SPACE MARK stops being White_Space and U+1681 wrongly starts | S4.8, S2.106, S4.311 | **SURVIVED** (262.7 s) | 2: `stats_ws_each` (stderr), `encstr_ws_edge_sweep` (stdout) |
| R14d | `port/f64.bend:960` `json.is_plain` | `Nat.is_le(p, 16n)` → `Nat.is_le(p, 15n)`: the JSON writers' plain/exponent boundary moves from 1e16 to 1e15 | S4.173, S5.37–S5.40 (OQ-001) | **SURVIVED** (263.8 s) | 7 (stdout): `decnum_ints`, `decnum_exponents`, `decnum_long_mantissa`, `decfmt_exp_upper_boundary`, `decfmt_tie_even_1`, `decfmt_shortest_ties`, `decnum_exact_halfway` |
| R14e | `port/encode.bend:23` `needs_quote` | `Bool.or(U32.is_eq(c, 13), U32.is_eq(c, 9))` → `Bool.or(U32.is_eq(c, 9), U32.is_eq(c, 9))`: a CR inside a value no longer forces quotes (M26's TAB arm deliberately KEPT, so this is not M26 again) | S4.12–S4.16 | **SURVIVED** (259.2 s) | 3 (stdout): `encstr_quoting`, `encstr_escapes_in_all`, `fx_enc_primitives_13` |
| R14f | `port/cli.bend:1045` `est.fin` | `Nat.div(c, 4n), w` → `Nat.div(c, 4n), Nat.div(w, 1n)`: **no-op control** | — | SURVIVED (259.2 s) | **0 cases break** ✓ |

Note which cases those are: `decnum_ints` and `decfmt_exp_upper_boundary` are the cases `docs/FEATURE_PARITY.md:32` names in the goldens column of the row "JSON number text (zmij…)", and `encstr_quoting` is one of the goldens of the row at `:21`. So the breakages are not in some corner the board forgot — they are in the cases the board itself points at, and none of those cases has a `golden_<case>` law.

The control matters: R14f proves the pipeline is calibrated (a mutant that changes no observable byte must survive, and does), so the five survivals above are law-coverage gaps and not an artifact of my harness. Note also that all six binaries differ in sha256 from the baseline, R14f included, so "the emitted bytes changed" is not evidence of a semantic change — only the corpus run is.

What they mean, stated the way `references/LAWS-FROM-SPEC.md` asks: `bend PROOF.bend` covers what its 374 laws say and no more. Five behaviours on rows `docs/FEATURE_PARITY.md` marks `present` with named laws — mode detection by extension case, clap's similarity threshold, the Unicode White_Space table, the JSON writers' layout boundary, and CR in the quoting test — rest on goldens ALONE. That is legitimate (the board's rule is "goldens and/or laws"), but the "Proved" section of `docs/PORT_REPORT.md:39-42` and `docs/PORT_STATE.md:37`'s STRONG verdict should not be read as covering them. The fix is six lines of `port/LAWS.bend`, as round 13's was.

### Attacking `scripts/claims-audit.py` (finding R14-2), in order

Eleven falsifications, applied cumulatively to MY clone and then restored:

| # | file:line | the lie | caught? |
|---|---|---|---|
| 1 | `docs/FEATURE_PARITY.md:58` | `other closed unit laws \| 999` | no |
| 2 | `docs/PORT_REPORT.md:41` | `3 closed laws … 9 unit laws` | no |
| 3 | `docs/PORT_STATE.md:20` | the js lane `"verdict":"FAIL"`, `"stderr_compared":false` | no |
| 4 | `docs/PORT_STATE.md:22` | floor `"stable":12`, `"oracle_identity_checked":false`, `"verdict":"UNSTABLE"` | no |
| 5 | `docs/PORT_STATE.md:19` | `All terms check. (unsafe 7 = 7 @unsafe + 0 template instances; bend 2.0.16)` | no |
| 6 | `docs/PORT_STATE.md:43` | the oracle's sha256 changed to `dead0000…` | no |
| 7 | `docs/PORT_STATE.md:37` | `"mutants": 25, "killed": 3, "survived": [], … "verdict": "STRONG"` | no |
| 8 | `docs/PORT_STATE.md:19` (date column) | the proof row's date moved to `2025-01-01` | no |
| 9 | `docs/PORT_REPORT.md:32` | the pasted `converge.sh` line → `"verdict": "CONVERGED", "missing": []}`: HOLDS` | **no** |
| 10 | `docs/PORT_REPORT.md:23` | law-coverage `"unsafe": 9, "unsafe_annotations": 9, "verdict": "OK"` | no |
| 11 | `docs/PORT_STATE.md:39` | self-test `"mutations":12,"caught":2` and clean-build `"verdict":"FAIL"` | no |

After all eleven: `claims-audit.py` → `{"findings": 0, … "verdict": "OK"}` exit 0; `claims-lint.sh` → `0 hit(s) in 11 file(s)` exit 0; `state-check.sh` → `0 finding(s)` exit 0; `parity-board.sh` → `DEBT` exit 0.

A twelfth and thirteenth attempt then found the *mechanism*, which is the part worth fixing:

| # | file:line | the lie | caught? |
|---|---|---|---|
| 12 | `docs/PORT_REPORT.md:22` | `374 laws` → `999 laws` | **no** — the line contains `` `15ae0c8` `` |
| 13 | `docs/PORT_STATE.md:36` | `374 laws = 14 quantified + 65 closed unit laws + 295 closed whole-pipeline` → `7 laws = 1 quantified + 2 closed unit laws + 4 closed whole-pipeline` | **no** — same reason |

`scripts/claims-audit.py:44`: `HISTORY = re.compile(r"`[0-9a-f]{7,40}`|\bhistorical\b|…")`. The intent is sound — "a line that records HISTORY (a commit `abc1234` in backticks …) is exempt from the count checks" — but `AGENTS.md`'s own rule is that every proof claim states `bend --version` **and** the checkout sha, so the two lines that carry the port's law counts each end in `` bend 2.0.16 @ `15ae0c8` `` and are therefore exempt from every count check forever. With both falsified, `claims-audit.py` prints `{"files": 16, "absent": [], "findings": 0, "historical_lines_exempt": 6, "laws": 374, … "verdict": "OK"}`, exit 0: it *computed* 374 and printed it in the same line as the verdict it gave to a document saying 7.

Everything was restored by copying back byte-identical backups from `/data/tmp/review_R14/backup/`; `git status --porcelain` shows only `?? toon` (the README's own build output, R14-13); `claims-audit.py` prints `"historical_lines_exempt": 1, … "verdict": "OK"` again, its original line.

**What the gate does check and I could not fool** (verified by falsifying each and watching it go red): the REFUSED_CV count — the check this session added genuinely works, `16` → `15` gives `docs/PORT_REPORT.md:33: REFUSED_CV captures: says 15, the repository has 16`, exit 1; the board's goldens column — a ghost case gives `docs/FEATURE_PARITY.md:16: the goldens column names ghost_case_xyz, which is not a case of goldens/cases.tsv`; and, from reading the code, the case counts, MANIFEST hashes, clause count, `"mutants"` count, every DISC/NE/EXP/OQ/bead/law/case/clause/report/path reference, README's performance ratios/medians/cvs against `perf/evidence/`, and the rounds table against `docs/reviews/` (all of which `scripts/harness-selftest.sh` M12 also exercises, 12/12 CAUGHT on HEAD).

### Execution counts per lens

`diff-fuzz` 6 lenses: 6006 documents (the `numbers` lens batches 400 generated number texts per document, so 1200 number texts sit inside 6 of those) · my four targeted lenses: 2081 · corpus conformance on three lanes (c-1t, js, c-1t under `TOON_SPEC=1`): 3 × 1065 = 3195 · spec-clause predictions: 322 · descriptor states: 27 · README examples: 7 · mutant corpus runs: 6 × 1065 = 6390 (not counted in the total below, they compare a MUTANT against the goldens, not the port) · full proofs run: 7 (1 baseline + 6 mutants, ~31 minutes of checker time) · **port-vs-oracle / port-vs-golden comparisons: 11 638; differences: 1, which is DISC-004, already ACCEPTED and accurately described**.

---

## 3. What would make round 15 clean

1. Six closed laws for R14-1's five behaviours, and the five mutants added to `scripts/hand-mutants.py` as `M27`…`M31` (round 13's precedent).
2. Extend `scripts/claims-audit.py` with (a) verbatim comparison of every pasted gate JSON against the gate's live output for the keys the gate emits — not four hand-picked numeric fields — and at minimum the `verdict` strings; (b) a check that the oracle sha256 quoted anywhere equals `docs/PIN.toml`'s; (c) the law breakdown as a SUM (quantified + closed + golden = total), which catches R14-4 by construction.
3. Correct `perf/NEGATIVE-EVIDENCE.md:144` to `--runs 9` and delete the sentence that misreads `incumbent-bench.sh:80-95`.
4. Either track a copy of `-` (or re-capture with `--repin` after the owner authorises its removal) so `pin-check`'s `manifest_hashes` can be GREEN in a clone, or say in `docs/PORT_STATE.md:43` that the row is GREEN only in a tree that holds the untracked `-`.
5. A `### Notation, part B` and a `### Notation, part E` block in `docs/EXISTING_Toon_STRUCTURE.md`, saying that part E's `examples` column writes the decoded sub-value in compact JSON and not the program's stdout (R14-14); and S1.32's missing `-0` note (R14-15). Both are one sentence each; neither touches code.
6. The prose corrections: `PORT_REPORT.md:12,30`, `PORT_STATE.md:29,39,104,112-114`, the bead `toon_bend-0m1`'s `close_reason`, and `/toon` in `.gitignore`.

A note on what this round did NOT find, because it bears on whether round 15 can be clean: after 11 638 compared executions across seven independent lenses — six seeded `diff-fuzz` lenses on a fresh seed, four hand-written lenses aimed at the exact defs I then mutated, 207 spec-clause predictions run on both programs, all three reproducible lanes over the corpus, the kill-switch arm, the descriptor probe and the README's own examples — **the port's conversion behaviour produced not one unregistered difference from the pinned original**. Rounds 9 to 14 now stand at ~1.5 million compared executions with zero unregistered content differences. The remaining risk in this port is not in `port/`; it is in the two places every one of my findings lands: the laws' coverage of `port/`, and the gates that check what the documents say about both.

---

files_written:
- `/data/tmp/review_R14/round-14.md` (this report)
- `/data/tmp/review_R14/mut.py` (the five mutants + the control, full-proof runner)
- `/data/tmp/review_R14/validate_mutants.py`, `/data/tmp/review_R14/mutant_cases.py` (mutant validity: does it break a case?)
- `/data/tmp/review_R14/lens.py` (the four targeted differential lenses)
- `/data/tmp/review_R14/clause_pred_lens.py`, `clause_pred_lens2.py`…`clause_pred_lens5.py`, `utf8_probe.py` (the spec-clause prediction lens, 207 clauses / 322 pairs)
- `/data/tmp/review_R14/clone/` (the fresh clone; `./toon`, `port/toon`, `oracle/toon` added as build/oracle inputs, all gitignored or untracked, no tracked file changed)
- `/data/tmp/review_R14/bin/{toon,toon.js,toon.R14a..R14f}` (the binaries)
- `/data/tmp/review_R14/backup/{PORT_STATE,PORT_REPORT,FEATURE_PARITY,README}.md` plus `{PS2,PR2,FP2}.md` (the byte-identical backups used to restore the clone after R14-2; `/data/projects/toon_bend` was verified clean afterwards: `git status --porcelain` empty at `6d7cfcc`)
- `/data/tmp/review_R14/{js-lane.out,spec1.out,selftest.out,all_cases.txt,goldenlaw_cases.txt}`, `/data/tmp/review_R14/__pycache__/`, `/data/tmp/review_R14/tmp/**` (gate logs, the case/law cross-reference lists, and the mutants' temp copies — all KEPT, nothing deleted)

goldens_touched: false
ran_original: true
